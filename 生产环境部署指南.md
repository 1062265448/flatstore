# 生产环境部署指南

> 最后更新：2026-05-13 · 部署版本：v1.18.0

---

## 🏗 系统架构

```
浏览器/手机 ──→ Nginx (:80) ──→ 前端静态文件 (Vue SPA)
                          ──→ /api/* ──→ NestJS (:3002) ──→ MySQL (:3306)
```

---

## 📦 一、服务器环境

### 1.1 硬件需求

| 项目 | 最低 | 推荐 |
|------|------|------|
| 内存 | 2GB | 4GB+ |
| 磁盘 | 20GB | 40GB+ |
| 系统 | Ubuntu 20.04+ | Ubuntu 24.04 |

### 1.2 安装依赖

```bash
apt update
apt install -y nginx mysql-server
npm install -g pm2
```

---

## 🗄 二、数据库

### 2.1 创建数据库

```bash
mysql -u root -p
```

```sql
CREATE DATABASE flat_library CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'flat_user'@'localhost' IDENTIFIED BY '你的强密码';
GRANT ALL PRIVILEGES ON flat_library.* TO 'flat_user'@'localhost';
FLUSH PRIVILEGES;
```

### 2.2 性能优化（3.6GB 内存）

```sql
SET GLOBAL innodb_buffer_pool_size = 134217728;  -- 128MB
```

---

## 🚀 三、部署后端

### 3.1 拉取代码

```bash
cd /opt
git clone https://github.com/1062265448/flatstore.git flatstore
```

### 3.2 配置环境变量

```bash
cd /opt/flatstore/backend
cp .env.example .env
```

编辑 `.env`：

```env
DATABASE_URL="mysql://flat_user:你的密码@localhost:3306/flat_library"
JWT_SECRET="生成随机64位字符串"
QWEN_API_KEY="你的千问API密钥"
FRONTEND_URL="http://你的域名或IP"
PORT=3002
```

### 3.3 安装 & 构建

```bash
npm install
npx prisma generate
npx prisma db push
npm run build
```

### 3.4 启动（PM2）

```bash
pm2 start dist/main.js --name flatstore-api --log /opt/flatstore/logs/api.log
pm2 save
pm2 startup
```

### 3.5 管理命令

```bash
pm2 status             # 查看状态
pm2 logs flatstore-api # 查看日志
pm2 restart flatstore-api # 重启
pm2 stop flatstore-api    # 停止
```

---

## 🌐 四、部署前端

### 4.1 配置

```bash
cd /opt/flatstore/frontend
cat > .env.production << 'EOF'
VITE_API_BASE_URL=http://你的域名或IP/api
EOF
```

### 4.2 构建

```bash
npm install && npm run build
# 产物在 dist/ 目录
```

### 4.3 Nginx 配置

```nginx
# /etc/nginx/sites-available/flatstore
server {
    listen 80 default_server;
    server_name _;

    root /opt/flatstore/frontend/dist;
    index index.html;

    # 前端 SPA
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理
    location /api/ {
        proxy_pass http://localhost:3002/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 120s;
    }

    # Swagger
    location = /api {
        proxy_pass http://localhost:3002/api;
        proxy_set_header Host $host;
    }

    # 上传文件
    location /uploads/ {
        alias /opt/flatstore/backend/uploads/;
    }
}
```

激活配置：

```bash
ln -sf /etc/nginx/sites-available/flatstore /etc/nginx/sites-enabled/
rm -f /etc/nginx/sites-enabled/default
nginx -t && systemctl reload nginx
```

---

## 🔥 五、防火墙

腾讯云控制台 → 轻量应用服务器 → 防火墙 → 添加规则：

| 端口 | 协议 | 策略 |
|------|------|------|
| 80 | TCP | 允许 |
| 443 | TCP | 允许（HTTPS，可选）|

---

## 🔐 六、HTTPS（可选）

```bash
apt install certbot python3-certbot-nginx
certbot --nginx -d 你的域名
```

---

## 📱 七、移动端 APK 构建

### 7.1 准备

- 安装 [Android Studio](https://developer.android.com/studio)
- SDK Manager 确认安装 Android 13 (API 33)

### 7.2 修改 API 地址

编辑 `mobile/src/api/request.ts`：

```typescript
const BASE_URL = Capacitor.isNativePlatform()
  ? 'http://你的服务器IP/api'  // APK 直连
  : (import.meta.env.VITE_API_BASE_URL || 'http://你的服务器IP/api')
```

### 7.3 构建

```bash
cd mobile
npm install && npm run build
npx cap sync android
npx cap open android
```

### 7.4 生成 APK

Android Studio → Build → Build APK(s) → 等2-3分钟 → locate

---

## 🔄 八、更新流程

```bash
cd /opt/flatstore
git pull

# 后端
cd backend && npm install && npx prisma generate && npm run build
pm2 restart flatstore-api

# 前端
cd ../frontend && npm install && npm run build
```

---

## 📊 九、监控

```bash
pm2 ls                    # 进程状态
pm2 logs flatstore-api    # 实时日志
systemctl status nginx    # Nginx 状态
systemctl status mysql    # MySQL 状态
df -h                     # 磁盘
free -h                   # 内存
```

---

## 🔑 十、默认账号

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | admin123 | 管理员 |

> ⚠️ 上线后请立即修改密码

---

## 🆘 故障排查

### API 返回 502
```bash
pm2 restart flatstore-api && systemctl reload nginx
```

### 前端白屏
```bash
cd /opt/flatstore/frontend && npm run build
```

### 数据库连接失败
```bash
systemctl status mysql
# 检查 .env 中 DATABASE_URL 是否正确
```

### 端口被占用
```bash
lsof -i :3002
lsof -i :80
```

---

*部署环境：腾讯云轻量应用服务器 · Ubuntu 24.04*
