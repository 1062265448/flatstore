# 平面库配货模块 — 项目评估与优化建议

> 评估日期：2026-04-16  
> 评估范围：前后端源码 + 四份文档（CLAUDE.md、前端开发日志、后端开发日志、启动操作指南、开发文档） + **全量前后端代码审查**  
> 版本基线：v1.5.2（37 项历史修复经验已纳入）

---

## 一、项目总览

| 维度 | 评分 | 说明 |
|------|------|------|
| **架构设计** | ⭐⭐⭐⭐ | 单模块内聚合理，事务处理规范，但缺乏模块化拆分空间 |
| **代码质量** | ⭐⭐⭐⭐ | TypeScript 严格类型、DTO 验证到位，个别逻辑可优化 |
| **文档质量** | ⭐⭐⭐⭐⭐ | 四份文档 + CLAUDE.md 极其详尽，37 项经验沉淀完整 |
| **安全性** | ⭐⭐ | 无认证/授权，API Key 明文，无速率限制 |
| **可测试性** | ⭐⭐ | 零单元测试、零集成测试 |
| **可维护性** | ⭐⭐⭐⭐ | 单模块起步可接受，需预留拆分路径 |
| **用户体验** | ⭐⭐⭐ | 功能完整，但 UI 停留在 Element Plus 默认风格 |

**综合评级：B+** — 功能实现扎实，文档优秀，但安全、测试、生产化能力是短板。

### 技术栈对比

| 维度 | 后端 | 前端 |
|------|------|------|
| 框架 | NestJS 10.3 | Vue 3.4 + Vite 5 |
| ORM/状态 | Prisma 6.19 | Pinia 2.1 |
| 数据库 | MySQL 8.0 | — |
| UI 库 | Swagger | Element Plus 2.5 + ECharts 5.4 |
| 测试 | Jest（配置已有，无实文件） | Vitest + Playwright |
| 构建 | TypeScript 5.3 | TypeScript 5.3 |

---

## 二、优势分析（做得好的）

### 2.1 数据库设计扎实
- Prisma Schema 定义清晰，5 个实体关系正确（1:1 复刻 workshop-mes）
- 索引设计合理：`@@index([status, grade])` 和 `@@index([customerId, deletedAt, status])` 覆盖高频查询
- 软删除规范：Customer 和 DistributionOrder 使用 `deletedAt`
- 级联删除：`DistributionOrderItem` 正确配置 `onDelete: Cascade`
- 字符集意识：全链路 utf8mb4

### 2.2 后端核心逻辑可靠
- **事务处理到位**：创建/取消/完成配货单均使用 `prisma.$transaction`，确保原子性
- **N+1 查询已优化**：使用 `findMany({ where: { id: { in: ... } } })` + Map 缓存
- **聚合查询优化**：统计接口使用 `groupBy` 一次获取所有数据
- **状态保护**：已发货/预留库存不可修改，已发货/取消订单不可操作
- **引用校验**：删除库存前检查是否被有效配货单引用

### 2.3 前后端约定一致
- API 响应格式统一：`{ data: [], total: N, page: N, pageSize: N }`
- Axios 响应拦截器解包正确，且有注释说明
- TypeScript 类型在前后端之间严格对齐（DTO ↔ Interface）
- `PaginatedResponse<T>` 类型定义明确

### 2.4 AI 识别方案成熟
- 7 层防线设计：Prompt 工程 → JSON 容错 → 超时控制 → 文件规范 → 加载提示 → 历史兜底 → 字符集保障
- 成功/失败均记录到 `ai_recognition_history`
- `AbortSignal.timeout(120000)` 双重超时保护

### 2.5 基础设施完备
- Swagger API 文档自动生成
- 全局异常过滤器 + 结构化日志拦截器
- 文件上传白名单校验（MIME 类型 + 10MB 限制）
- CORS 按环境变量配置

### 2.6 文档质量极高
- **CLAUDE.md**：37 项注意事项，覆盖所有历史坑点
- **开发文档**：完整 ER 图、API 清单、状态机流程图、前后端代码示例
- **开发日志**：前端 + 后端分别记录，含启动调试记录
- **启动操作指南**：面向开发者的完整操作手册

---

## 三、问题与风险

### 3.1 🔴 严重问题（P0 — 建议立即实施）

#### 3.1.1 全链路缺少认证与授权
- **后端**：所有 API 端点完全开放，无登录、无权限校验。任何人可直接调用增删改接口。
- **前端**：`router.beforeEach` 仅设置页面标题，未做登录校验。未登录用户可直接访问所有页面。

**建议**：
- 后端引入 JWT 认证（`@nestjs/jwt` + `@nestjs/passport`），创建 `AuthGuard` 和 `RolesGuard`，对敏感操作（删除、AI 识别）做角色限制
- 前端增加 JWT Token 认证流程，路由守卫校验 Token 有效性，未登录跳转登录页
- 前端 Axios 请求拦截器注入 Token，后端对应 Guard 校验

```typescript
// 前端 request.ts — Token 注入
request.interceptors.request.use((config) => {
  const token = localStorage.getItem('token')
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})

// 响应拦截器 — 401 处理
if (error.response?.status === 401) {
  localStorage.removeItem('token')
  router.push('/login')
  return Promise.reject(new Error('登录已过期'))
}
```

#### 3.1.2 API Key 明文暴露
- `.env` 和 `.env.example` 中 `QWEN_API_KEY` 明文存储
- 后端开发日志中直接硬编码了 API Key
- 无启动时校验，无效时只在运行时失败

**建议**：
- 立即轮换已泄露的 API Key
- 在模块 `onModuleInit` 时校验关键环境变量，缺失则拒绝启动
- 使用 `@nestjs/config` 的 `ConfigModule` 管理环境变量
- 生产环境使用密钥管理服务

#### 3.1.3 AI 识别端点无限流
- `POST /distribution/inventory/ai-recognize` 无请求频率限制
- 恶意调用可产生大量 Qwen API 费用，或触发 DOS

**建议**：安装 `@nestjs/throttler`，对该端点设置限流（如 10 次/分钟/用户）

#### 3.1.4 配货单号碰撞风险
- `orderNo = ORD-${Date.now()}-${Math.random().toString(36).substring(2, 7)}` 高并发下不保证唯一性

**建议**：使用 UUID（`import { v4 as uuidv4 } from 'uuid'`）或 NanoID 或雪花算法

#### 3.1.5 API 测试页面暴露
- `/test` 路由指向 API 测试页面，生产环境应移除

**建议**：`import.meta.env.DEV` 条件控制或生产构建时移除

#### 3.1.6 零测试覆盖
- 无任何单元测试、集成测试、E2E 测试
- 每次修改都是盲改，回归风险极高

---

### 3.2 🟠 高优先级问题（P1）

#### 3.2.1 库存锁定存在竞态条件
- `createOrder` 先查询可用库存 → 校验 → 在事务中锁定。查询与锁定之间有时间窗口
- 并发请求可能将同一库存分配给多个订单

**建议**：在事务内二次检查并锁定：
```typescript
return this.prisma.$transaction(async (tx) => {
  const available = await tx.inventoryStock.findMany({
    where: { id: { in: stockIds }, status: 'available' },
  })
  // 校验后锁定
  await tx.inventoryStock.updateMany({ ... })
})
```

#### 3.2.2 分页参数无校验
- `page` 和 `limit` 直接从 Query 传入，无边界检查
- `page=-1` 或 `limit=10000` 可拖垮数据库

**建议**：
```typescript
const page = Math.max(1, params.page || 1)
const limit = Math.min(100, Math.max(1, params.limit || 20))
```

#### 3.2.3 Decimal 精度处理
- `weight` 为 `Decimal(10,3)`，Service 中用 `Number()` 转换后做算术
- JavaScript 浮点数精度问题

**建议**：使用 `decimal.js` 或 Prisma 的 `Decimal` 类型进行计算

#### 3.2.4 软删与关联数据不一致
- `DistributionOrder` 使用软删，`DistributionOrderItem` 使用 `onDelete: Cascade` 硬删
- 删除订单后明细丢失，无法追溯历史

**建议**：明细表也增加 `deletedAt`，或统一为硬删

#### 3.2.5 前端订单创建一次性加载 1000 条库存
- `handleCreate` / `handleEdit` 中 `limit: 1000` 加载全部可用库存
- 库存量大时接口慢、前端渲染压力大

**建议**：使用 `el-select` 的 `remote` + `remote-method` 按需搜索加载

#### 3.2.6 前后端超时设置不一致
- 前端 AI 接口 `timeout: 180000`（3 分钟），后端 `AbortSignal.timeout(120000)`（120 秒）
- **建议**：统一为 120 秒

#### 3.2.7 前端 `aiRecognize` Content-Type 矛盾
- 注释说"不设置 Content-Type"，但实际设置了 `headers: { 'Content-Type': 'multipart/form-data' }`
- 手动设置会丢失 boundary

**建议**：移除手动设置的 `Content-Type`，让浏览器自动添加

---

### 3.3 🟡 中优先级问题（P2）

#### 3.3.1 订单列表过度关联查询
- `getOrderList` 对每条订单 `include: { items: { include: { stock: true } } }`，数据量大时大量 JOIN
- **建议**：默认不 include 明细，仅在 `?includeItems=true` 时关联

#### 3.3.2 统计接口无缓存
- `getStatistics` 每次都执行 3 组聚合查询
- **建议**：内存缓存（TTL 30-60 秒），长期用 Redis 缓存

#### 3.3.3 前端 Dashboard 数据不响应
- 使用 `ref(statisticsStore.stats)` 拷贝了初始值，Store 更新后不会自动同步
- **修复**：使用 `computed(() => statisticsStore.stats)`

#### 3.3.4 ECharts 主题切换重绘而非更新
- `watch(() => themeStore.isDark)` 调用 `initCharts()` 重绘整个图表
- **建议**：使用 `setOption` 增量更新颜色配置

#### 3.3.5 前端统计无自动刷新
- `Dashboard.vue` 仅 `onMounted` 请求一次
- **建议**：`setInterval` 定时刷新（30 秒），或在相关操作成功后手动触发

#### 3.3.6 批量删除使用循环
- `batchDeleteOrders` 在 `$transaction` 中逐条处理
- **建议**：先收集所有 stockIds，再一次性 `updateMany` + `updateMany` 标记删除

#### 3.3.7 Store 全量刷新替代局部更新
- 每个 Store 的 CRUD 方法都 `await fetchXxx()` 全量刷新列表
- **建议**：创建/更新时局部替换，删除时 `splice`，仅批量操作后才全量刷新

#### 3.3.8 `where: any` 破坏 Prisma 类型安全
- `distribution.service.ts` 多处 `const where: any = {}`
- **建议**：使用 `Prisma.InventoryStockWhereInput` 等具体类型

#### 3.3.9 手动构建 updateData 冗余
- 手动遍历 DTO 键构建 `updateData` 对象
- **建议**：DTO 已有 `@IsOptional()` 标记，直接传 DTO 即可

#### 3.3.10 同步文件读取阻塞事件循环
- `fs.readFileSync(file.path)` → `await fs.promises.readFile(file.path)`

#### 3.3.11 AI 识别失败后临时文件未清理
- AI 调用失败时，上传文件残留磁盘
- **建议**：catch 块中 `fs.promises.unlink(file.path)`

#### 3.3.12 文件上传 MIME 伪造防护缺失
- 仅检查 `file.mimetype`，攻击者可伪造 Content-Type
- **建议**：使用 `file-type` 库检查文件魔数

#### 3.3.13 前端 `availableStocks` 使用 `any[]`
- `const availableStocks = ref<any[]>([])` → `ref<InventoryStock[]>([])`

#### 3.3.14 `handleStockSelect` 索引参数类型不安全
- `@change="handleStockSelect($index)"` 中 `$index` 不是标准参数
- **建议**：改为闭包方式 `@change="(val) => handleStockSelect(index, val)"`

#### 3.3.15 `__dirname` 在 ESM 中不推荐
- `vite.config.ts` 使用 `resolve(__dirname, 'src')`
- **建议**：使用 `fileURLToPath(new URL('./src', import.meta.url))`

#### 3.3.16 搜索重置后页码未回第 1 页
- `Inventory.vue` 和 `Orders.vue` 的 `handleReset` 只清空筛选条件，`queryForm.page` 未重置
- **修复**：`queryForm.page = 1`

#### 3.3.17 ObjectURL 内存泄漏
- 用户未手动移除预览图就导航离开时，ObjectURL 未释放
- **建议**：`onUnmounted` 中统一清理

---

### 3.4 🟢 低优先级问题（P3-P4）

| # | 问题 | 端 | 建议 |
|---|------|------|------|
| 1 | 前端无 ESLint/Prettier 配置 | 前端 | 统一代码风格 |
| 2 | 后端无 Dockerfile | 后端 | 容器化部署 |
| 3 | 前端无 `.env.example` | 前端 | 增加示例配置 |
| 4 | 无 API 版本管理 | 全栈 | URL 前缀 `/api/v1/` |
| 5 | 前端无 PWA 支持 | 前端 | 离线缓存 |
| 6 | 前端缺少 `.env.example` | 前端 | 增加示例配置 |
| 7 | 前端无国际化 | 前端 | i18n 预留 |
| 8 | Vite 缺少 `base` 配置 | 前端 | 部署到非根路径时 404 |
| 9 | Vite 构建缺少 sourcemap 控制 | 前端 | 生产环境关闭 |
| 10 | `ApiResponse` 类型定义未使用 | 前端 | 统一使用或移除 |
| 11 | `InventoryStock.weight` 联合类型 | 前端 | 统一转为 `number` 或明确使用 `string` |
| 12 | 删除操作无 loading 反馈 | 前端 | 增加 `v-loading` |
| 13 | 深色模式不完整 | 前端 | 引入 Element Plus 官方暗色主题 |
| 14 | 库存列表无导出功能 | 前端 | 前端 `xlsx` 库或后端接口 |
| 15 | 单模块架构的扩展瓶颈 | 后端 | Service 超过 500 行时拆分 |

---

## 四、工程化与 DevOps

| 项目 | 后端现状 | 前端现状 | 建议 |
|------|----------|----------|------|
| Docker | 无 | 无 | 前后端各自增加 `Dockerfile`，前端用 nginx 托管 |
| CI/CD | 无 | 无 | GitHub Actions：lint → test → build |
| 日志 | console.log | — | 后端集成 `pino`/`winston` 结构化日志 |
| 监控 | 无 | — | 增加 Prometheus 指标或健康检查探针 |
| 环境变量 | .env | .env | 增加 `.env.example`，后端使用 `@nestjs/config` 类型化管理 |
| Vite base | — | 未配置 | `base: process.env.NODE_ENV === 'production' ? '/flatstore/' : '/'` |
| sourcemap | — | 未配置 | 生产构建关闭 |
| 健康检查 | 无 | — | `GET /health` 返回 `{ status: 'ok', db: 'connected' }` |
| 优雅关闭 | 未处理 | — | `app.enableShutdownHooks()` + Prisma `onModuleDestroy` |
| 数据库迁移 | `prisma db push` | — | 切换到 `prisma migrate` |
| E2E 测试 | — | Playwright 配置存在但目录为空 | 创建基础 E2E 测试：登录→列表→订单创建 |
| 单元测试 | Jest 配置已有，无实际文件 | 有 inventory/order Store 测试 | 补充 customer/statistics Store + 组件测试 |

---

## 五、缺失功能规划

| # | 功能 | 优先级 | 说明 |
|---|------|--------|------|
| 1 | 登录/注册页面 | P0 | 安全基础 |
| 2 | 操作日志/审计 | P2 | 追踪关键操作 |
| 3 | Excel 批量导入库存 | P2 | 提升效率 |
| 4 | 消息通知 | P3 | 订单状态变更等 |
| 5 | 数据看板导出 | P3 | PDF/Excel 格式 |

---

## 六、优化路线图（合并版）

### Phase 1：立即修复（1-2 天）

| # | 任务 | 端 | 工作量 |
|---|------|------|--------|
| 1.1 | 轮换泄露的 QWEN API Key + 启动校验 | 后端 | 30 分钟 |
| 1.2 | 修复 `aiRecognize` Content-Type 矛盾 | 前端 | 5 分钟 |
| 1.3 | 修复 Dashboard `stats` 响应性（`computed`） | 前端 | 30 分钟 |
| 1.4 | 修复 ECharts 内存泄漏（`onUnmounted`） | 前端 | 30 分钟 |
| 1.5 | 统一前后端超时设置（120s） | 全栈 | 15 分钟 |
| 1.6 | `.env` 加入 `.gitignore` | 全栈 | 5 分钟 |
| 1.7 | 搜索重置 `page = 1` | 前端 | 各 1 行 |
| 1.8 | ObjectURL 清理 | 前端 | 3 行 |
| 1.9 | `__dirname` → `import.meta.url` | 前端 | 2 行 |

### Phase 2：安全加固（3-5 天）

| # | 任务 | 端 | 工作量 | 说明 |
|---|------|------|--------|------|
| 2.1 | JWT 认证系统 | 全栈 | 2 天 | 用户注册/登录/Token 刷新 |
| 2.2 | 401 处理 + Token 过期跳转 | 前端 | 30 分钟 | 路由跳转 |
| 2.3 | NestJS Guards 权限控制 | 后端 | 1 天 | admin/operator/viewer 角色 |
| 2.4 | API 速率限制 | 后端 | 0.5 天 | `@nestjs/throttler` |
| 2.5 | ConfigModule 管理环境变量 | 后端 | 0.5 天 | 替代 `process.env` |
| 2.6 | CORS 收紧 | 后端 | 0.5 天 | 按部署域名配置 |
| 2.7 | 移除 API 测试页面（生产环境） | 前端 | 15 分钟 | DEV 条件控制 |
| 2.8 | 输入 sanitization | 后端 | 0.5 天 | 防止 XSS/SQL 注入 |

### Phase 3：可靠性与数据库（1 天）

| # | 任务 | 端 | 工作量 |
|---|------|------|--------|
| 3.1 | 库存竞态条件修复（事务内二次检查） | 后端 | 30 分钟 |
| 3.2 | 分页参数边界校验 | 后端 | 15 分钟 |
| 3.3 | Decimal 精度改用 `decimal.js` | 后端 | 30 分钟 |
| 3.4 | 软删/硬删一致性（明细表加 deletedAt） | 后端 | 30 分钟 |
| 3.5 | 复合索引补充 | 后端 | 15 分钟 |
| 3.6 | NanoID 订单号 | 后端 | 0.5 天 |
| 3.7 | 切换到 `prisma migrate` | 后端 | 0.5 天 |

### Phase 4：代码质量（1 天）

| # | 任务 | 端 | 工作量 |
|---|------|------|--------|
| 4.1 | `where: any` → Prisma 类型 | 后端 | 30 分钟 |
| 4.2 | 手动 updateData → 直接传 DTO | 后端 | 15 分钟 |
| 4.3 | `fs.readFileSync` → `promises.readFile` | 后端 | 10 分钟 |
| 4.4 | AI 失败临时文件清理 | 后端 | 10 分钟 |
| 4.5 | MIME 伪造防护（`file-type`） | 后端 | 30 分钟 |
| 4.6 | `any[]` → 具体类型 | 前端 | 15 分钟 |
| 4.7 | `handleStockSelect` 索引参数修复 | 前端 | 15 分钟 |
| 4.8 | ESLint + Prettier 配置 | 前端 | 30 分钟 |
| 4.9 | DTO 增强验证（`@ArrayMinSize` 等） | 后端 | 30 分钟 |

### Phase 5：性能优化（1 天）

| # | 任务 | 端 | 工作量 |
|---|------|------|--------|
| 5.1 | 库存远程搜索（替代 1000 条全加载） | 前端 | 1 小时 |
| 5.2 | 订单列表条件 include | 后端 | 30 分钟 |
| 5.3 | 统计接口缓存（内存 TTL） | 后端 | 30 分钟 |
| 5.4 | ECharts setOption 增量更新 | 前端 | 30 分钟 |
| 5.5 | Dashboard 自动刷新 | 前端 | 30 分钟 |
| 5.6 | 批量删除改为批量操作 | 后端 | 30 分钟 |
| 5.7 | Store 局部更新替代全量刷新 | 前端 | 1 小时 |
| 5.8 | 单模块拆分（当 Service > 500 行时） | 后端 | 2 天 |

### Phase 6：用户体验 & DevOps（1-2 天）

| # | 任务 | 端 | 工作量 |
|---|------|------|--------|
| 6.1 | 深色模式完整性（Element Plus dark CSS vars） | 前端 | 30 分钟 |
| 6.2 | 健康检查端点 + 优雅关闭 | 后端 | 30 分钟 |
| 6.3 | 统一响应格式 + 请求 ID 追踪 | 后端 | 30 分钟 |
| 6.4 | Docker 部署配置（前后端） | 全栈 | 2 小时 |
| 6.5 | CI/CD（GitHub Actions） | 全栈 | 2 小时 |
| 6.6 | 测试体系建立 | 全栈 | 5-7 天 |
| 6.7 | Vite base + sourcemap 配置 | 前端 | 15 分钟 |
| 6.8 | `.env.example` 增加 | 全栈 | 10 分钟 |

### Phase 7：UI 升级（可选，5-7 天）

开发文档中已有完整的 macOS Sonoma 风格设计规范和组件代码库（GlassCard、StatusTag、SkeletonTable 等），但当前 Dashboard 仍使用 Element Plus 默认样式。

| 任务 | 说明 |
|------|------|
| 全局主题系统 | CSS Variables + 暗色模式 |
| 毛玻璃卡片组件 | GlassCard 替换 el-card |
| 骨架屏组件 | 替换 Loading Spinner |
| 页面切换动画 | Vue Transition |
| 响应式适配 | 平板/手机端适配 |

**总计**：约 7-10 个工作日完成全部优化（不含 UI 升级）。

---

## 七、快速修复清单（5 分钟内可完成）

| # | 修改 | 文件 | 行代码 |
|---|------|------|--------|
| 1 | 订单号改 UUID | `distribution.service.ts` | 1 行 |
| 2 | 分页参数 clamp | `distribution.service.ts` | 3 行 |
| 3 | `fs.readFileSync` → `promises.readFile` | `distribution.service.ts` | 1 行 |
| 4 | 搜索重置 `page = 1` | `Inventory.vue`, `Orders.vue` | 各 1 行 |
| 5 | `availableStocks` 类型 | `Orders.vue` | 1 行 |
| 6 | ObjectURL 清理 | `AI.vue` | 3 行 |
| 7 | `__dirname` 修复 | `vite.config.ts` | 2 行 |
| 8 | 统一 AI 超时为 120s | `distribution.ts` | 1 行 |
| 9 | 移除 `Content-Type` 手动设置 | `distribution.ts` | 1 行 |

---

## 八、代码级修复示例

### 8.1 修复 Dashboard 数据响应性
```typescript
// ❌ 当前代码 — 初始拷贝，不响应
const stats = ref(statisticsStore.stats)

// ✅ 修复 — 使用 computed
import { computed } from 'vue'
const stats = computed(() => statisticsStore.stats)
```

### 8.2 修复 ECharts 内存泄漏
```typescript
// ✅ 添加清理逻辑
onUnmounted(() => {
  inventoryChart?.dispose()
  orderChart?.dispose()
  window.removeEventListener('resize', resizeCharts)
})
```

### 8.3 修复 aiRecognize Content-Type + 超时
```typescript
// ❌ 当前 — 手动设置会丢失 boundary + 超时不一致
export const aiRecognize = (file: File) => {
  const formData = new FormData()
  formData.append('file', file)
  return request.post('/distribution/inventory/ai-recognize', formData, {
    headers: { 'Content-Type': 'multipart/form-data' },  // ← 删除此行
    timeout: 180000,  // ← 3 分钟，不一致
  })
}

// ✅ 修复
export const aiRecognize = (file: File) => {
  const formData = new FormData()
  formData.append('file', file)
  return request.post('/distribution/inventory/ai-recognize', formData, {
    timeout: 120000,  // 统一为 120s
  })
}
```

### 8.4 添加订单号防碰撞
```typescript
// backend/src/distribution/distribution.service.ts
import { customAlphabet } from 'nanoid';
const nanoid = customAlphabet('1234567890ABCDEFGHIJKLMNOPQRSTUVWXYZ', 10);

// 创建订单时
const orderNo = `ORD-${nanoid()}`;  // 碰撞概率极低
```

### 8.5 DTO 增强验证
```typescript
// backend/src/distribution/dto/order.dto.ts
import { IsArray, ArrayMinSize } from 'class-validator';

export class CreateOrderDto {
  @IsNumber()
  customerId: number;

  @IsArray()
  @ArrayMinSize(1, { message: '配货明细至少需要一条' })
  @ValidateNested({ each: true })
  @Type(() => OrderItemDto)
  items: OrderItemDto[];
}
```

### 8.6 分页参数校验
```typescript
// distribution.service.ts — 所有查询方法
const page = Math.max(1, params.page || 1)
const limit = Math.min(100, Math.max(1, params.limit || 20))
```

### 8.7 前端 Token 注入 + 401 处理
```typescript
// frontend/src/api/request.ts

// 请求拦截器
request.interceptors.request.use((config) => {
  const token = localStorage.getItem('token')
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})

// 响应拦截器
request.interceptors.response.use(
  (response) => response.data,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      router.push('/login')
      return Promise.reject(new Error('登录已过期'))
    }
    const message = error.response?.data?.message || error.message || '请求失败'
    ElMessage.error(message)
    return Promise.reject(error)
  }
)
```

---

## 九、总结

### 项目亮点
1. **文档先行**：37 项历史修复经验全部文档化，新项目可直接复用
2. **事务处理规范**：所有库存状态变更均在事务中完成
3. **性能意识**：N+1 优化、聚合查询、索引设计均到位
4. **类型安全**：前后端 TypeScript 严格对齐，减少运行时错误

### 最大短板
1. **安全性**：零认证/授权，生产环境不可用
2. **测试**：零测试覆盖，每次修改都是赌博
3. **部署**：无容器化、无 CI/CD、无监控

### 给开发者的建议
- **先修 P0**：API Key 轮换 + Content-Type 修复 + .gitignore，30 分钟搞定
- **再补安全**：JWT + 角色控制，这是上线的最低要求
- **建立测试**：至少覆盖核心 Service 的状态机逻辑
- **渐进优化**：不要一次性重构，按 Phase 逐步推进

---

_最后更新：2026-04-16 16:21 GMT+8_
