# 平面库配货模块 — 开发日志

## 项目概览

- **开发日期**：2026-04-16
- **前端**：Vue 3 (Composition API) + TypeScript + Vite 5 + Element Plus + Pinia + ECharts
- **后端**：NestJS 10 + Prisma 6.19.3 + MySQL 8.0 + 阿里云千问 qwen-vl-plus
- **项目路径**：`D:\Flatstore\`

### 目录结构
```
Flatstore/
├── frontend/                    # 前端（Vue 3）
│   ├── src/
│   │   ├── main.ts              # 应用入口
│   │   ├── App.vue              # 根组件（侧边栏 + 主题切换）
│   │   ├── api/
│   │   │   ├── request.ts       # Axios 封装
│   │   │   └── distribution.ts  # 业务接口
│   │   ├── stores/              # Pinia 状态管理
│   │   ├── types/               # TypeScript 类型定义
│   │   ├── router/              # 路由配置
│   │   └── views/               # 页面组件
│   │       ├── Dashboard.vue    # 仪表盘（统计卡片 + ECharts）
│   │       ├── Inventory.vue    # 库存管理
│   │       ├── Customers.vue    # 客户管理
│   │       ├── Orders.vue       # 配货单管理
│   │       ├── AI.vue           # AI 图像识别
│   │       └── Test.vue         # API 测试窗口
│   ├── .env                     # VITE_API_BASE_URL=http://localhost:3002
│   ├── vite.config.ts
│   └── package.json
│
├── backend/                     # 后端（NestJS）
│   ├── src/
│   │   ├── main.ts              # 应用入口（CORS/Swagger/静态文件）
│   │   ├── app.module.ts
│   │   ├── distribution/        # 配货模块
│   │   │   ├── distribution.controller.ts
│   │   │   ├── distribution.service.ts
│   │   │   ├── distribution.module.ts
│   │   │   └── dto/
│   │   │       ├── inventory.dto.ts
│   │   │       ├── order.dto.ts
│   │   │       └── customer.dto.ts
│   │   ├── common/
│   │   │   ├── filters/global-exception.filter.ts
│   │   │   ├── interceptors/logging.interceptor.ts
│   │   │   └── services/qwen-ai.service.ts
│   │   └── prisma/prisma.service.ts
│   ├── prisma/schema.prisma
│   ├── .env                     # DATABASE_URL / QWEN_API_KEY / PORT
│   └── package.json
│
├── uploads/inventory/           # 上传文件目录
├── .gitignore
└── CLAUDE.md
```

---

## 一、数据库设计

### 1.1 Prisma Schema
```prisma
datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Customer {
  id        Int       @id @default(autoincrement())
  name      String
  contact   String?
  phone     String?
  address   String?
  remark    String?
  deletedAt DateTime?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt

  @@index([deletedAt])
  @@map("customer")
}

model InventoryStock {
  id          Int      @id @default(autoincrement())
  batchNo     String
  grade       String
  specification String?
  productType String?
  weight      Float
  pieceCount  Int
  status      String   @default("available")
  location    String?
  nickelContent Float?
  remark      String?
  sourceType  String   @default("manual")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  orderItems  DistributionOrderItem[]

  @@index([batchNo])
  @@index([grade])
  @@index([status])
  @@index([status, grade])
  @@map("inventory_stock")
}

model DistributionOrder {
  id          Int      @id @default(autoincrement())
  orderNo     String
  customerId  Int
  customerName String?
  productSpec String?
  targetGrade String?
  remark      String?
  totalWeight Float
  totalPieces Int
  status      String   @default("draft")
  driverName  String?
  vehicleNo   String?
  shippedAt   DateTime?
  deletedAt   DateTime?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  customer    Customer? @relation(fields: [customerId], references: [id])
  items       DistributionOrderItem[]

  @@index([orderNo])
  @@index([status])
  @@index([customerId])
  @@index([customerId, deletedAt, status])
  @@map("distribution_order")
}

model DistributionOrderItem {
  id        Int      @id @default(autoincrement())
  orderId   Int
  stockId   Int
  weight    Float
  pieceCount Int

  order     DistributionOrder @relation(fields: [orderId], references: [id])
  stock     InventoryStock    @relation(fields: [stockId], references: [id])

  @@index([orderId])
  @@index([stockId])
  @@map("distribution_order_item")
}

model AiRecognitionHistory {
  id           Int       @id @default(autoincrement())
  imageUrl     String?
  result       String?
  itemCount    Int       @default(0)
  status       String    @default("success")
  errorMessage String?
  batchNo      String?
  grade        String?
  date         DateTime?
  createdAt    DateTime  @default(now())

  @@index([status])
  @@index([createdAt(sort: Desc)])
  @@map("ai_recognition_history")
}
```

### 1.2 核心实体

| 实体 | 说明 | 关键字段 |
|------|------|----------|
| **Customer** | 客户 | name, contact, phone, address, deletedAt（软删除） |
| **InventoryStock** | 库存批次 | batchNo, grade, weight, pieceCount, status, location |
| **DistributionOrder** | 配货单 | orderNo, customerId, status（状态机） |
| **DistributionOrderItem** | 配货单明细 | orderId, stockId, weight, pieceCount |
| **AiRecognitionHistory** | AI 识别历史 | imageUrl, result, status, itemCount |

---

## 二、核心功能

### 2.1 库存管理

**API**：`GET/POST/PATCH/DELETE /distribution/inventory`

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/distribution/inventory` | 分页查询（keyword/grade/status/productType） |
| GET | `/distribution/inventory/:id` | 获取单条 |
| POST | `/distribution/inventory` | 创建 |
| POST | `/distribution/inventory/batch` | 批量创建 |
| PATCH | `/distribution/inventory/:id` | 更新 |
| DELETE | `/distribution/inventory/:id` | 删除（含引用校验） |
| POST | `/distribution/inventory/batch-delete` | 批量删除 |
| POST | `/distribution/inventory/ai-recognize` | AI 图像识别 |

**关键实现**：
- 删除时校验库存是否被有效配货单引用
- 状态为 `shipped` 或 `reserved` 时禁止修改

### 2.2 客户管理

**API**：`/distribution/customers`

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/distribution/customers` | 获取所有客户 |
| GET | `/distribution/customers/:id` | 获取单个（含最近 10 条订单） |
| POST | `/distribution/customers` | 创建 |
| PUT | `/distribution/customers/:id` | 更新 |
| DELETE | `/distribution/customers/:id` | 软删除 |

### 2.3 配货单管理

**状态机**：
```
draft → confirmed → shipping → shipped
  ↓         ↓          ↓
cancelled  cancelled  cancelled
```

**API**：`/distribution/orders`

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/distribution/orders` | 分页查询 |
| GET | `/distribution/orders/:id` | 获取详情（含明细+库存） |
| POST | `/distribution/orders` | 创建（自动锁定库存） |
| PUT | `/distribution/orders/:id` | 更新 |
| POST | `/distribution/orders/:id/confirm` | 确认 |
| POST | `/distribution/orders/:id/ship` | 发货 |
| POST | `/distribution/orders/:id/deliver` | 完成发运 |
| POST | `/distribution/orders/:id/cancel` | 取消 |
| DELETE | `/distribution/orders/:id` | 删除（仅已完成/已取消） |

**事务处理**：
```typescript
this.prisma.$transaction(async (tx) => {
  const order = await tx.distributionOrder.create({...});
  await tx.inventoryStock.updateMany({
    where: { id: { in: stockIds } },
    data: { status: 'reserved' },
  });
  return order;
});
```

### 2.4 AI 图像识别

**API**：`/distribution/inventory/ai-recognize`

**流程**：
1. Multer 上传文件到 `uploads/inventory/`
2. 读取文件转 Base64
3. 调用千问 VL Plus（`qwen-vl-plus`）
4. JSON 解析容错 + 规范化输出
5. 记录识别历史（成功/失败）

**上传配置**：
- 文件大小：10MB
- 格式：JPEG / PNG / GIF / WebP
- 随机文件名：`${Date.now()}-${random}.${ext}`

### 2.5 统计概览

**API**：`GET /distribution/statistics`

```json
{
  "inventory": { "total": 0, "available": 0, "reserved": 0, "shipped": 0, "totalWeight": "0", "totalPieces": 0 },
  "order": { "total": 0, "draft": 0, "confirmed": 0, "shipping": 0, "shipped": 0 },
  "customer": { "total": 0 }
}
```

---

## 三、前端架构

### 3.1 Axios 封装

**关键**：响应拦截器已解包 `response.data`

```typescript
// request.ts
const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || 'http://localhost:3002',
  timeout: 30000,
})

// 请求拦截器 — 排除 FormData/Blob
request.interceptors.request.use((config) => {
  const hasContentType = config.headers['Content-Type'] || config.headers['content-type']
  if (!hasContentType && !(config.data instanceof FormData) && !(config.data instanceof Blob)) {
    config.headers['Content-Type'] = 'application/json'
  }
  return config
})

// 响应拦截器 — 解包
request.interceptors.response.use(
  (response) => response.data,
  (error) => {
    ElMessage.error(error.response?.data?.message || error.message)
    return Promise.reject(error)
  }
)
```

**调用方式**：
```typescript
const res = await getInventoryList(params)
inventoryList.value = res.data   // ✅ res 已是 response.data
total.value = res.total           // ✅ 直接访问
```

### 3.2 状态管理（Pinia）

| Store | 职责 |
|-------|------|
| `themeStore` | 主题切换（亮/暗） |
| `statisticsStore` | 全局统计数据 |
| `inventoryStore` | 库存列表 + CRUD |
| `customerStore` | 客户列表 + CRUD |
| `orderStore` | 配货单列表 + 状态操作 |

### 3.3 路由

| 路径 | 页面 | 说明 |
|------|------|------|
| `/` | 仪表盘 | 统计卡片 + ECharts |
| `/inventory` | 库存管理 | CRUD + 批量删除 + AI 识别入口 |
| `/orders` | 配货单 | 状态机 + 配货明细 |
| `/customers` | 客户管理 | CRUD |
| `/ai` | AI 识别 | 图像上传 + 批量导入 |
| `/test` | API 测试窗口 | 可视化请求日志 |

### 3.4 主题切换

```typescript
// CSS 类 .dark 切换全局样式
document.documentElement.classList.toggle('dark', isDark.value)
```

### 3.5 ECharts 图表

- 库存状态饼图：可用 / 已预留 / 已发货
- 订单状态柱状图：草稿 / 已确认 / 发货中 / 已发货
- 主题切换时自动重新渲染

---

## 四、后端架构

### 4.1 main.ts 配置

```typescript
// CORS
app.enableCors({ origin: process.env.FRONTEND_URL || 'http://localhost:5174', credentials: true })

// 全局验证
app.useGlobalPipes(new ValidationPipe({ whitelist: true, transform: true, forbidNonWhitelisted: true }))

// 全局异常过滤器
app.useGlobalFilters(new GlobalExceptionFilter())

// 结构化日志
app.useGlobalInterceptors(new LoggingInterceptor())

// 静态文件
app.useStaticAssets(uploadsDir, { prefix: '/uploads/' })

// Swagger
SwaggerModule.setup('api', app, document)
```

### 4.2 环境变量

```env
DATABASE_URL="mysql://root:root123456@localhost:3306/flat_library"
QWEN_API_KEY="sk-7520063d83b24e48989e169eaa263906"
FRONTEND_URL="http://localhost:5174"
PORT=3002
```

### 4.3 API 响应格式

**成功**：`{ data: [...], total: number, page: number, pageSize: number }`（分页）或直接返回对象

**错误**：`{ success: false, message: string, statusCode: number }`

---

## 五、启动命令

### 5.1 后端
```bash
cd backend
npm install
npx prisma generate
npx prisma db push
npm run start:dev
# Swagger: http://localhost:3002/api
```

### 5.2 前端
```bash
cd frontend
npm install
npm run dev
# http://localhost:5174
```

---

## 六、问题与修复记录

### 6.1 启动调试修复（后端）

| 问题 | 解决方案 |
|------|----------|
| 缺少 datasource 配置 | 添加 `datasource db { provider = "mysql" url = env("DATABASE_URL") }` |
| 缺少 generator 配置 | 添加 `generator client { provider = "prisma-client-js" }` |
| OnInit 接口不存在 | 改为 `OnModuleInit` |
| useStaticAssets 类型错误 | 使用 `NestExpressApplication` 类型 |
| noImplicitAny 严格检查 | tsconfig 关闭严格检查 |
| 关联查询缺少 include | `include: { orderItems: { include: { order: true } } }` |
| 数据库认证失败 | 统一使用 root/root123456 |

### 6.2 前端启动问题

| 问题 | 说明 |
|------|------|
| Sass 警告 `legacy-js-api` | 提示性警告，不影响功能，可忽略 |
| Dashboard stats 不响应 | `ref()` 改为 `computed()` |
| ECharts 内存泄漏 | 添加 `onUnmounted` 清理 |

### 6.3 AI 图像识别问题

**问题 1：后端 `file` 为 `undefined`**
- **原因**：`aiRecognize` 方法未验证 `file` 参数
- **修复**：添加 `if (!file) throw Error()`

**问题 2：AI 返回结果无法解析为 JSON**
- **原因**：千问 API 对无效图片返回非 JSON 格式的错误信息
- **修复**：增强错误处理，提取实际响应内容

**问题 3：图片尺寸限制**
- **错误**：`height:1 or width:1 must be larger than 10`
- **说明**：千问 VL 模型要求最小 10x10 像素，需使用真实票据图片

**问题 4：请求拦截器覆盖 FormData 的 Content-Type**
- **现象**：前端上传失败，后端收不到文件；curl 直接测试后端成功
- **根因**：`request.ts` 拦截器在无 Content-Type 时一律设为 `application/json`，上传 FormData 时覆盖了浏览器的 boundary 设置
- **修复**：拦截器在设置默认 Content-Type 前检查 `config.data` 是否为 `FormData`/`Blob`
```typescript
// ✅ 修复后
const hasContentType = config.headers['Content-Type'] || config.headers['content-type']
if (!hasContentType && !(config.data instanceof FormData) && !(config.data instanceof Blob)) {
  config.headers['Content-Type'] = 'application/json'
}
```

### 6.4 测试问题

**PATCH 库存更新测试 400**：测试只发 `{ grade: 'B' }`，DTO 要求必填 `weight`、`pieceCount` → 发送完整字段

**批量删除状态码不匹配**：Controller 用 `@HttpCode(201)` → 调整测试期望值 `expect(201)`

### 6.5 识别精准度优化（2026-04-16）

**背景**：
- 品级 = 牌号，为 4 位数字，如 9996、9950、9999
- 批号格式固定为 `XX-X-XXX`，如 26-7-090、26-12-005

**优化内容**：

**1. Prompt 精准化**
- 明确业务场景：镍板产品票据识别
- 强调牌号为 4 位数字，要求准确识别不篡改
- 明确批号格式为 `XX-X-XXX`，保持原始格式不转换
- 排除小计行/合计行/汇总行
- 模糊字段设为 null，不猜测

**2. 新增 `normalizeResult()` 后处理**（`qwen-ai.service.ts`）
- 批号去除空格
- 牌号去除前后空格
- 日期统一为 `YYYY-MM-DD`
- 数字字段确保为 number 类型

**变更文件**：`backend/src/common/services/qwen-ai.service.ts`

---

## 七、测试概况

### 7.1 测试配置

| 端 | 框架 | 文件 | 数量 |
|----|------|------|------|
| 后端 | Jest + Supertest | `test/unit/*.spec.ts`, `test/integration/*.spec.ts` | 77 |
| 前端 | Vitest | `src/stores/__tests__/*.test.ts` | 30 |

### 7.2 测试结果

**后端**：77 通过（52 单元 + 25 集成）
**前端**：30 通过（4 文件）

**运行命令**：
```bash
# 后端
cd backend && npm test

# 前端
cd frontend && npm test -- --run
```

---

## 八、优化计划

| 任务 | 优先级 | 状态 |
|------|--------|------|
| JWT 认证系统 | P0 | 待开发 |
| API 速率限制 | P0 | 待开发 |
| 单元测试框架完善 | P1 | 待开发 |
| 模块拆分 | P2 | 待开发 |
| Prisma Migrate 迁移 | P2 | 待开发 |
| Docker 部署 | P2 | 待开发 |
| ESLint + Prettier | P3 | 待开发 |
| NanoID 订单号 | P3 | 待开发 |

---

## 九、经验总结

### 9.1 Multer 文件上传

| 错误 | 根因 | 方案 |
|------|------|------|
| 后端 `file` 为 `undefined` | 未验证文件 | 添加 `if (!file) throw Error()` |
| 前端上传失败 | Axios 拦截器覆盖 Content-Type | 排除 FormData/Blob |

### 9.2 AI 识别

| 错误 | 原因 | 处理 |
|------|------|------|
| `Cannot read properties of undefined (reading 'path')` | 文件参数为空 | 添加验证 |
| `图片尺寸不满足限制` | 图片太小 | 使用至少 10x10 像素 |
| `AI 返回结果无法解析` | AI 返回错误信息 | 增强错误处理，显示实际响应 |

### 9.3 调试技巧

1. **后端日志**：在关键方法添加 `console.log`
2. **前端日志**：`console.log('[AI] 上传文件:', file.name, file.size)`
3. **curl 测试**：`curl -X POST http://localhost:3002/distribution/inventory/ai-recognize -F "file=@test.jpg"`
4. **端口占用**：`netstat -ano | findstr 3002` + `taskkill //F //PID xxx`

### 9.4 性能优化

- 批量查询替代 N+1：`findMany({ where: { id: { in: [...] } } })` + Map 映射
- 聚合查询替代多次 count：`groupBy({ by: ['status'], _count, _sum })`
- ECharts 组件卸载时 `dispose()` 防止内存泄漏

---

_最后更新：2026-04-16 16:57_
