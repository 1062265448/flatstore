# 平面库配货模块 — 开发日志

**最后更新：2026-05-22**

---

## 项目概览

- **开发日期**：2026-04-16 ~ 至今
- **前端**：Vue 3 (Composition API) + TypeScript + Vite 5 + Element Plus + Pinia + ECharts + Three.js
- **后端**：NestJS 10 + Prisma 6.19.3 + MySQL 8.0 + 智谱 GLM-4.6V-Flash
- **移动端**：Vue 3 + Capacitor 7 (Android)
- **项目路径**：`D:\Flatstore\`
- **GitHub**：https://github.com/1062265448/New-repository

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0.0 | 2026-04-16 | 初始化平面库配货系统 |
| v1.2.0 | 2026-04-17 | 前端UI重构 - 极简玻璃态设计系统 |
| v1.3.0 | 2026-04-17 | 前端UI更新 - 字段优化、Bug修复 |
| v1.4.0 | 2026-04-17 | 3D仓库建模 - 平面库真实建筑结构 |
| v1.5.0 | 2026-04-20 | 生产就绪 - JWT认证、速率限制、骨架屏、Docker部署 |
| v1.5.1 | 2026-04-20 | Bug修复 - JwtService注入、图表空白、个人中心 |
| v1.5.2 | 2026-04-20 | UI优化 - 配货单库存信息完善、AI识别历史表格 |
| v1.5.3 | 2026-04-20 | UI优化 - AI识别批量导入增加下拉选项 |
| v1.5.4 | 2026-04-20 | Bug修复 - AI识别规格字段缺失 |
| v1.5.5 | 2026-04-20 | Bug修复 - 配货单productType/specification字段 |
| v1.5.6 | 2026-04-20 | 优化 - 简化配货单状态机，修复批量删除逻辑 |
| v1.6.0 | 2026-04-21 | 功能合并 - v1.5.7安全特性去除导出版本 |
| v1.7.0 | 2026-05-06 | 登录页UI重构、Demo展厅清理、3D仓库路由修复、three.js依赖补全 |
| v1.7.1 | 2026-05-06 | 修复登录页主题切换、移除注册功能、Dashboard统计优化、依赖补全、TypeScript类型修复 |
| **v1.7.2** | **2026-05-09** | **Mobile App v1.0.0 正式发布（Capacitor 7 Android）** |
| **v1.8.0** | **2026-05-11** | **全项目质量审计 + 14 项 Web 修复 + 7 项移动端修复 + 扫码出库模块方案设计** |
| **v1.8.1** | **2026-05-11** | **移动端 App 顶部安全区域适配 — 全面适配异形屏/灵动岛状态栏** |
| **v1.9.0** | **2026-05-11** | **双端库存筛选全面升级 + AI 识别结果优化 + 识别历史时间筛选修复** |
| **v1.10.0** | **2026-05-11** | **Web 前端全面设计升级 — 设计 Tokens 重构、全局样式统一、11 个页面/组件精细化打磨** |
| **v1.11.0** | **2026-05-11** | **移动端 App 全面设计升级 — 20 个文件设计 Tokens 重构、组件统一、页面排版优化** |
| **v1.12.0** | **2026-05-11** | **AI 识别导入新增规格/存放位置下拉菜单（双端）+ 手动录入库存规格改为下拉菜单** |
| **v1.12.1** | **2026-05-11** | **P1 修复 + 导出功能清理 + UI 设计升级** |
| **v1.13.0** | **2026-05-12** | **移动端 App 全面 UX 优化 — AI识别/库存/配货单交互重构** |
| **v1.20.0** | **2026-05-19** | **全项目健康审计 + 配货流程6项修复（TS类型/状态机/事务安全/移动端功能补全）** |
| **v1.21.0** | **2026-05-19** | **AI识别单位修正(kg) + 数据校验(crossValidate) + 库存包号/合计行** |
| **v1.22.0** | **2026-05-19** | **配货流程简化(草稿→已发货) + 库存已发出状态 + 双端单位统一 + 配货明细分组合计** |
| **v1.22.1** | **2026-05-20** | **AI识别批号校验正则升级 — 支持末尾字母 J/s/t** |
| **v1.22.2** | **2026-05-21** | **Login.vue 缺失 useRoute 修复 — 解决 route is not defined 运行时报错** |
| **v1.23.0** | **2026-05-22** | **AI模型切换GLM-4.6V-Flash + 识别解析增强 + 配货分类统计 + 全项目UI移位修复** |

---

## 一、项目结构

```
Flatstore/
├── frontend/                      # Vue 3 前端
│   ├── src/
│   │   ├── api/                  # API 接口
│   │   │   ├── request.ts        # Axios 封装（响应拦截器已解包）
│   │   │   └── distribution.ts   # 业务接口
│   │   ├── stores/               # Pinia 状态管理
│   │   ├── views/               # 页面组件
│   │   └── router/index.ts      # 路由配置
│   └── .env                      # VITE_API_BASE_URL=http://localhost:3002
│
├── backend/                       # NestJS 后端
│   ├── src/
│   │   ├── main.ts               # 入口（CORS/Swagger/静态文件）
│   │   ├── app.module.ts         # 根模块（Throttler/Auth）
│   │   ├── distribution/          # 配货模块
│   │   │   ├── distribution.controller.ts
│   │   │   ├── distribution.service.ts
│   │   │   └── dto/
│   │   ├── auth/                  # 认证模块（JWT）
│   │   │   ├── auth.controller.ts
│   │   │   ├── auth.service.ts
│   │   │   ├── auth.module.ts
│   │   │   ├── jwt.strategy.ts
│   │   │   └── guards/jwt-auth.guard.ts
│   │   └── common/services/       # AI 服务
│   ├── prisma/schema.prisma
│   └── .env
│
├── docker-compose.yml             # MySQL 8.0 配置
├── CLAUDE.md                      # 开发规范
└── 开发日志.md                    # 完整开发记录
```

---

## 二、技术栈

| 组件 | 技术 | 版本 |
|------|------|------|
| 后端框架 | NestJS | 10.x |
| ORM | Prisma | **6.19.3**（必须锁定） |
| 数据库 | MySQL | 8.0 |
| AI 识别 | 阿里云千问 | qwen-vl-plus |
| 前端框架 | Vue 3 | 3.x |
| 构建工具 | Vite | 5.x |
| UI 组件 | Element Plus | 最新 |
| 状态管理 | Pinia | 最新 |
| 图表 | ECharts | 最新 |
| 3D 渲染 | Three.js | 最新 |

---

## 三、数据库设计

### 3.1 实体关系

```
Customer (客户)
    ↓
DistributionOrder (配货单) ←→ DistributionOrderItem (配货单明细)
                                      ↓
                              InventoryStock (库存批次)

AiRecognitionHistory (AI 识别历史)
User (用户)
```

### 3.2 Prisma Schema

```prisma
model Customer {
  id        Int       @id @default(autoincrement())
  name      String    @db.VarChar(100)
  contact   String?   @db.VarChar(50)
  phone     String?   @db.VarChar(20)
  address   String?   @db.VarChar(200)
  remark    String?   @db.Text
  deletedAt DateTime?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model InventoryStock {
  id            Int       @id @default(autoincrement())
  batchNo       String    @db.VarChar(50)
  grade         String    @db.VarChar(20)        // 9997/9996/9950/9920
  specification String?   @db.VarChar(100)
  productType   String?   @db.VarChar(50)        // 电解镍/电积镍/不锈钢专用镍/电镀专用镍
  weight        Float
  pieceCount    Int
  status        String    @default("available")   // available/reserved/shipped
  location      String?   @db.VarChar(100)       // 三厂区/二厂区
  sourceType    String    @default("manual")
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}

model DistributionOrder {
  id           Int       @id @default(autoincrement())
  orderNo      String    @unique
  customerId   Int
  customerName String?
  productType  String?
  specification String?
  productSpec  String?
  targetGrade  String?
  totalWeight  Float
  totalPieces  Int
  status       String    @default("draft")       // draft/shipping/shipped/cancelled
  driverName   String?
  vehicleNo    String?
  shippedAt    DateTime?
  remark       String?
  deletedAt    DateTime?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
}

model DistributionOrderItem {
  id         Int      @id @default(autoincrement())
  orderId    Int
  stockId    Int
  weight     Float
  pieceCount Int
  createdAt  DateTime @default(now())
}

model AiRecognitionHistory {
  id           Int       @id @default(autoincrement())
  imageUrl     String?
  result       String?   @db.Text
  itemCount    Int       @default(0)
  status       String    @default("success")
  errorMessage String?   @db.Text
  batchNo      String?
  grade        String?
  date         DateTime?
  createdAt    DateTime  @default(now())
}

model User {
  id        Int      @id @default(autoincrement())
  username  String   @unique
  password  String   // bcrypt 加密
  role      String   @default("user")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
```

---

## 四、核心功能

### 4.1 库存管理

- CRUD 操作
- 批量导入
- 状态管理（available / reserved / shipped）
- 品级：9997、9996、9950、9920
- 产品类型：电解镍、电积镍、不锈钢专用镍、电镀专用镍
- 位置：三厂区、二厂区

### 4.2 配货单管理

**状态机（v1.5.6 简化版）：**
```
draft → shipping → shipped
  ↓         ↓
cancelled  (可删除)
```

**操作权限矩阵：**

| 操作 | draft | shipping | shipped | cancelled |
|------|-------|----------|---------|-----------|
| 编辑 | ✅ | ❌ | ❌ | ❌ |
| 发货 | ✅ | ❌ | ❌ | ❌ |
| 完成发运 | ❌ | ✅ | ❌ | ❌ |
| 取消 | ✅ | ✅ | ❌ | ❌ |
| 删除 | ❌ | ❌ | ✅ | ✅ |

关键特性：
- 创建时自动锁定库存（reserved）
- 事务处理防止超卖
- 取消时自动释放库存

### 4.3 客户管理

- CRUD 操作（软删除）
- 客户订单关联查询

### 4.4 AI 图像识别

- 调用千问 VL Plus 识别票据
- 自动保存识别历史
- 支持批量导入库存
- 用户可选择规格后批量导入

### 4.5 3D 仓库可视化

- 平面库真实建筑结构（120m × 66m）
- 仓储点网格显示
- 品级颜色区分
- 视角切换控制

### 4.6 认证与安全

- JWT 认证（7天有效期）
- 路由守卫（JwtAuthGuard）
- 全局限流（300次/分钟，登录10次/分钟）
- 统计缓存（TTL 30秒）

---

## 五、API 接口

### 5.1 认证

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/auth/login` | 用户登录 |
| POST | `/auth/register` | 用户注册 |
| GET | `/auth/profile` | 获取用户信息（需认证） |

### 5.2 库存

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/distribution/inventory` | 分页查询（支持 keyword/grade/status/productType） |
| GET | `/distribution/inventory/search` | 库存远程搜索（用于下拉选择） |
| GET | `/distribution/inventory/:id` | 获取单条 |
| POST | `/distribution/inventory` | 创建 |
| POST | `/distribution/inventory/batch` | 批量创建 |
| PATCH | `/distribution/inventory/:id` | 更新 |
| DELETE | `/distribution/inventory/:id` | 删除（需无有效订单引用） |
| POST | `/distribution/inventory/batch-delete` | 批量删除 |
| POST | `/distribution/inventory/ai-recognize` | AI 图像识别 |

### 5.3 配货单

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/distribution/orders` | 分页查询（支持 status/customerId/includeItems） |
| GET | `/distribution/orders/:id` | 获取详情 |
| POST | `/distribution/orders` | 创建（自动锁定库存） |
| PATCH | `/distribution/orders/:id` | 更新（仅 draft） |
| POST | `/distribution/orders/:id/ship` | 发货（填写司机、车牌，仅 draft） |
| POST | `/distribution/orders/:id/deliver` | 完成发运（仅 shipping） |
| POST | `/distribution/orders/:id/cancel` | 取消（释放库存） |
| DELETE | `/distribution/orders/:id` | 删除（仅 shipped/cancelled） |
| POST | `/distribution/orders/batch-delete` | 批量删除 |

### 5.4 客户

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/distribution/customers` | 获取所有 |
| GET | `/distribution/customers/:id` | 获取详情（含最近订单） |
| POST | `/distribution/customers` | 创建 |
| PUT | `/distribution/customers/:id` | 更新 |
| DELETE | `/distribution/customers/:id` | 软删除 |

### 5.5 统计

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/distribution/statistics` | 汇总统计 |
| GET | `/health` | 健康检查 |

### 5.6 AI 识别

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/distribution/inventory/ai-recognize` | 图像识别（multipart/form-data） |
| GET | `/distribution/recognition-history` | 识别历史（分页） |
| DELETE | `/distribution/recognition-history/:id` | 删除历史 |
| POST | `/distribution/recognition-history/batch-delete` | 批量删除历史 |

---

## 六、启动命令

### 6.1 启动 MySQL

```bash
cd /d/Flatstore
docker-compose up -d
```

### 6.2 启动后端

```bash
cd backend
npm install
npx prisma generate
npx prisma db push
npm run start:dev
# API: http://localhost:3002
# Swagger: http://localhost:3002/api
```

### 6.3 启动前端

```bash
cd frontend
npm install
npm run dev
# http://localhost:5174
```

---

## 七、关键问题与修复

### 7.1 Prisma 版本锁定 🔴

```
package.json: "prisma": "6.19.3"
```
Prisma 7 不兼容当前 MySQL 适配器。

**经验**：升级 Prisma 前务必检查适配器兼容性。

---

### 7.2 Axios 响应解包

响应拦截器已解包 `response.data`，直接访问 `res.data`。

```typescript
const res = await getInventoryList(params)
inventoryList.value = res.data      // ✅ 正确
```

---

### 7.3 文件上传

- 字段名是 `file`
- 不手动设置 Content-Type
- 后端限制 10MB

---

### 7.4 AI 识别结果保存

识别结果自动保存到 `ai_recognition_history` 表，可通过前端查看和批量导入。

---

### 7.5 数据库字符集

连接字符串添加 `?charset=utf8mb4`：
```
DATABASE_URL="mysql://root:root123456@localhost:3306/flat_library?charset=utf8mb4"
```

---

## 八、UI 设计系统（v1.2.0）

### 8.1 设计原则

创建了极简玻璃态设计系统，采用 Apple 风格配色。

### 8.2 核心变量

```scss
// frontend/src/styles/variables.scss
--color-primary: #0071e3;           // Apple Blue
--glass-bg: rgba(255, 255, 255, 0.72);
--glass-shadow: 0 8px 32px rgba(0, 0, 0, 0.08);
--radius-pill: 980px;               // 胶囊按钮
```

### 8.3 页面特性

| 页面 | 主要特性 |
|------|---------|
| Dashboard | 统计卡片渐变动画、ECharts 主题切换、骨架屏 |
| Inventory | 胶囊按钮、搜索栏、自定义表格 |
| Orders | 库存可视化选择（卡片式）、状态标签、优化编辑体验 |
| Customers | 卡片悬浮效果、弹窗动画 |
| AI | 拖拽上传、历史卡片列表 |
| Warehouse | 3D 仓库可视化 |
| Login | 简洁登录页 |
| Profile | 个人中心 |

---

## 九、Bug 修复经验 🔴

### 9.1 Toast 重复弹窗 🔴

**问题**：客户删除成功后弹出两次 toast

**原因**：
- Store 层调用了 `ElMessage.success()`
- 组件层也调用了 `showToast()`

**修复**：
```typescript
// ❌ 错误：在 Store 中处理 UI 反馈
const deleteCustomer = async (id: number) => {
  await api.deleteCustomer(id)
  ElMessage.success('删除成功')  // 不要这样做
}

// ✅ 正确：Store 只负责数据操作
const deleteCustomer = async (id: number) => {
  await api.deleteCustomer(id)
  await fetchCustomers()
}
```

**经验**：统一 toast 提示来源，Store 层只负责数据操作，组件层负责 UI 反馈。

---

### 9.2 Vue 响应式变量缺失 🔴

**问题**：
```
[Vue warn]: Property "isDragOver" was accessed during render but is not defined on instance
```

**原因**：模板中使用了 `isDragOver`，但 `<script setup>` 中忘记定义

**修复**：
```typescript
const isDragOver = ref(false)  // 添加缺失的响应式变量
```

**经验**：
- 使用 TypeScript strict 模式可以提前发现此类问题
- 模板中使用的所有变量必须在 `<script setup>` 中声明

---

### 9.3 后端 DTO 验证问题 🔴

**问题**：
```bash
curl -X PATCH /distribution/inventory/40 \
  -d '{"location":"新位置"}'
# 返回 400: weight must be a number
```

**原因**：`UpdateInventoryDto` 中 `weight` 和 `pieceCount` 缺少 `@IsOptional()` 装饰器

**修复** (`backend/src/distribution/dto/inventory.dto.ts`)：
```typescript
@ApiPropertyOptional()
@IsOptional()      // ← 添加这个装饰器
@IsNumber()
@Min(0)
weight?: number;
```

**经验**：
- 更新 DTO 的可选字段必须添加 `@IsOptional()` 装饰器
- 创建 DTO 和更新 DTO 应分开定义，验证规则不同

---

### 9.4 JwtService 注入错误 🔴

**问题**：
```
Nest could not find JwtService element (this provider does not exist in the current context)
```

**原因**：
在 `main.ts` 中尝试手动实例化 `AuthService` 并直接获取 `JwtService`。

**修复**：
1. 移除 `main.ts` 中的手动实例化代码
2. 在 `AuthService` 中实现 `OnModuleInit` 接口
3. 在 `onModuleInit` 中调用 `createDefaultAdmin()`

**经验**：NestJS 依赖注入应在模块初始化时处理。

---

### 9.5 图表显示空白 🔴

**问题**：首页 Dashboard 的 ECharts 图表显示空白。

**原因**：骨架屏加载完成后，数据虽然获取了，但图表在 DOM 未完全渲染时初始化。

**修复**：
```typescript
onMounted(async () => {
  loading.value = true
  await statisticsStore.fetchStatistics()
  loading.value = false
  await nextTick()  // 等待 DOM 渲染完成
  initCharts()
})
```

**经验**：响应式数据变化后操作 DOM（如图表初始化），必须使用 `nextTick()`。

---

### 9.6 个人中心无法打开 🔴

**问题**：点击导航栏用户下拉菜单中的"个人中心"无响应。

**原因**：缺少 `Profile.vue` 页面组件和路由配置。

**修复**：创建 `Profile.vue` 并配置路由。

---

### 9.7 nanoid 模块未安装 🔴

**问题**：
```
Cannot find module 'nanoid' or its corresponding type declarations.
```

**修复**：
```bash
cd backend
npm install nanoid
```

**经验**：
- 新增依赖后必须执行 `npm install`
- 部署前确认所有依赖已安装

---

### 9.8 Capacitor APK Network Error（CORS + 环境检测）🔴

**问题**：APK 安装到手机后登录页显示 Network Error，后端、ADB 代理、网络配置均正常。

**原因**（双因素叠加）：
1. **Capacitor 环境检测时机错误** — `!!(window as any).Capacitor` 在模块加载时求值，此时 Capacitor bridge 尚未注入，`isCapacitor=false` 导致 `BASE_URL` 回退为 `/`
2. **CORS 白名单遗漏** — Capacitor Android WebView 的 origin 是 `https://localhost`（不是 `capacitor://localhost`），不在后端 CORS 白名单中

**关键陷阱**：Axios 将 CORS 拒绝报告为 "Network Error"，而非 "CORS Error"，导致排查方向错误地偏向网络层。

**修复**：
```typescript
// ❌ 错误：模块加载时检测 window 对象
const isCapacitor = !!(window as any).Capacitor

// ✅ 正确：使用 Capacitor 官方 API
import { Capacitor } from '@capacitor/core'
const BASE_URL = Capacitor.isNativePlatform()
  ? 'http://localhost:3002'
  : (import.meta.env.VITE_API_BASE_URL || '/')
```

```typescript
// ❌ 错误：CORS 白名单只有 Web 前端 origin
const allowedOrigins = ['http://localhost:5174']

// ✅ 正确：添加 Capacitor 原生 App origin
const capacitorOrigins = [
  'https://localhost',       // Capacitor Android WebView
  'http://localhost',        // Capacitor 部分版本
  'capacitor://localhost',   // Capacitor iOS
]
```

**经验**：
- **永远不要用 `window.Capacitor` 检测原生环境**，使用 `Capacitor.isNativePlatform()` from `@capacitor/core`
- Capacitor Android WebView 的 origin 是 `https://localhost`，iOS 是 `capacitor://localhost`
- **Axios 的 "Network Error" 可能是 CORS 拒绝**，排查时优先检查 CORS 配置
- Capacitor + HTTP 明文请求需要 `network_security_config.xml` 中 `<base-config cleartextTrafficPermitted="true">`
- 排查移动端网络问题时，在登录页显示调试信息（Platform、isNative、BASE_URL、Protocol）是最高效的定位手段

### 9.9 移动端 BottomSheet 下滑误关闭 🔴

**问题**：AI 识别结果 BottomSheet 可通过下滑手势关闭，用户下滑查看结果时误触关闭，识别结果丢失

**修复**：BottomSheet 组件新增 `dismissible` prop，设为 `false` 时禁用下滑关闭，改为底部按钮（取消/重新识别）控制关闭

**经验**：含重要数据的 Sheet 应禁用手势关闭，通过明确的按钮操作控制

### 9.10 Capacitor 环境图片 URL 不拼接 🔴

**问题**：后端 `imageUrl` 存相对路径 `/uploads/inventory/xxx.jpg`，`<img :src>` 不会自动拼接 baseURL

**修复**：
```typescript
const resolveImageUrl = (url: string) => {
  if (!url) return ''
  if (url.startsWith('http')) return url
  const base = Capacitor.isNativePlatform() ? 'http://localhost:3002' : ''
  return base + url
}
```

**经验**：移动端 `<img>` 标签不会走 axios baseURL，需手动拼接完整 URL

---

## 十、代码规范

### 10.1 Store 层职责
```typescript
// ✅ 正确
const createOrder = async (dto: CreateOrderDto) => {
  const res = await api.createOrder(dto)
  await fetchOrders()
  return res
}
```

### 10.2 组件层职责
```typescript
// 通过 inject 获取 toast 函数
const showToast = inject('showToast') as (message: string, type?: string) => void
```

### 10.3 API 层职责
```typescript
// request.ts - 统一错误处理
request.interceptors.response.use(
  (response) => response.data,
  (error) => {
    ElMessage.error(error.response?.data?.message || error.message)
    return Promise.reject(error)
  }
)
```

---

## 十一、端到端测试结果

### 测试时间：2026-04-20（v1.5.6）

| 测试项 | 结果 |
|--------|------|
| 草稿订单直接发货 | ✅ |
| 批量删除校验（包含不可删除订单） | ✅ |
| 编辑配货单不加载库存 | ✅ |
| 编辑时库存选择区隐藏 | ✅ |
| 取消后库存释放 | ✅ |
| 已发货库存保护 | ✅ |

---

## 十二、3D 仓库可视化（v1.4.0）

### 12.1 建筑基础参数

| 参数 | 图纸值 | 3D 场景值 |
|------|--------|-----------|
| 建筑总长 X | 120,000 mm | 120 units |
| 建筑总宽 Z | 66,000 mm | 66 units |
| 柱距 X | 7,500 mm | 7.5 units |
| 柱距 Z | 6,000 mm | 6 units |
| 仓储点 | 1200×1200×1200 mm | 1.2×1.2×1.2 |

### 12.2 功能特性

1. **建筑结构** - 工业地面、仓储点网格、中央通道
2. **品级颜色** - 9997(绿)/9996(蓝)/9950(橙)/9920(紫)
3. **区域划分** - 6 个仓储区域
4. **视角切换** - 等轴测/俯视/正视
5. **控制面板** - 动画播放/暂停、相机重置

---

## 十三、生产就绪（v1.5.0）

### 13.1 JWT 认证

- Token 自动附加到请求头
- 401 响应自动跳转登录页
- 默认账户：`admin / admin123`

### 13.2 Docker 部署

```bash
# 开发环境
docker-compose up -d

# 生产环境
docker-compose -f docker-compose.prod.yml up -d
```

---

## 十四、后续优化

| 任务 | 优先级 | 状态 |
|------|--------|------|
| JWT 认证 | P0 | ✅ 已完成 |
| JWT_SECRET 安全加固 | P0 | ✅ 已完成 (v1.8.0) |
| API 速率限制 | P0 | ✅ 已完成 |
| CORS 严格校验 | P0 | ✅ 已完成 (v1.8.0) |
| AI 识别单位转换 | P0 | ✅ 已完成 (v1.8.0) |
| 骨架屏加载 | P1 | ✅ 已完成 |
| 统计缓存优化 | P1 | ✅ 已完成 (v1.8.0 实例化) |
| 库存远程搜索 | P1 | ✅ 已完成 |
| Token 有效期验证 | P1 | ✅ 已完成 (v1.8.0) |
| 软删除索引 | P1 | ✅ 已完成 (v1.8.0) |
| 状态枚举约束 | P1 | ❌ 待处理 |
| 导出库存 Excel | P1 | ❌ 待解决（Decimal序列化） |
| Store 类型安全 | P2 | ✅ 已完成 (v1.8.0) |
| Store 错误反馈 | P2 | ✅ 已完成 (v1.8.0) |
| batchDeleteInventory 精确性 | P2 | ✅ 已完成 (v1.8.0) |
| AI 重试次数优化 | P2 | ✅ 已完成 (v1.8.0) |
| 清理调试代码 | P2 | ✅ 已完成 (v1.8.0) |
| 虚拟列表 | P2 | 待做 |
| 单元测试完善 | P2 | 待做 |
| 3D 数据对接 | P1 | ✅ 已完成 |
| 代码分割优化 | P2 | 待做 |
| Docker 部署 | P2 | ✅ 已完成 |
| Helmet 安全头 | P3 | 待做 |
| Mobile 离线支持 | P3 | 待做 |
| API 版本控制 | P3 | 待做 |

---

## 十五、AI 识别规格修复（v1.5.4）

### 15.1 问题描述

用户在 AI 识别后，选择规格「50*50」进行批量导入，但查看识别历史详情时显示的是「整板」和「镍包」。

### 15.2 根本原因

1. **AI 接口缺少 specification 字段** - AI 随机生成规格值
2. **批量导入时表单重置不完整** - 遗漏了 productType 和 specification
3. **识别历史未记录用户选择的规格**

### 15.3 修复方案

1. 后端 AI 接口添加 specification 字段
2. 返回识别历史 ID
3. 批量导入时传递识别历史 ID
4. 导入后更新历史记录中的规格值

### 15.4 修复后的工作流程

```
1. 用户上传图片 → AI 识别 → 返回 { results, historyId }
2. 用户选择规格「50*50」→ 导入
3. 导入时自动将识别历史中的 specification 更新为「50*50」
4. 再次查看识别历史详情，显示正确的规格「50*50」
```

---

## 十六、配货单状态机优化（v1.5.6）

### 16.1 问题背景

1. **确认步骤多余** - `draft → confirmed` 只是空状态转换
2. **批量删除未校验** - 可能删除不可删除的订单而不报错
3. **编辑时冗余加载** - 编辑配货单时加载所有可用库存

### 16.2 优化方案

#### 2.1 简化状态机

**优化前：**
```
draft → confirmed → shipping → shipped
  ↓         ↓          ↓
cancelled  cancelled  cancelled
```

**优化后：**
```
draft → shipping → shipped
  ↓         ↓
cancelled  (可删除)
```

#### 2.2 批量删除逻辑修复

```typescript
// 优化前：静默跳过不可删除的订单
const deletableOrders = orders.filter(
  (o) => o.status === 'shipped' || o.status === 'cancelled',
);
// deletableOrders.length 可能小于 ids.length，但无报错

// 优化后：明确校验并报错
if (deletableOrders.length !== orders.length) {
  const undeletable = orders
    .filter((o) => o.status !== 'shipped' && o.status !== 'cancelled')
    .map((o) => o.orderNo);
  throw new BadRequestException(
    `以下订单不可删除：${undeletable.join(', ')}`,
  );
}
```

#### 2.3 编辑体验优化

| 场景 | 优化前 | 优化后 |
|------|--------|--------|
| 库存加载 | 加载所有可用库存（API 调用） | 使用订单已有库存信息 |
| 库存选择区 | 显示（但不可用） | 隐藏 |
| 明细删除按钮 | 显示 | 隐藏 |

### 16.3 修改的文件

**后端：**
```
backend/src/distribution/
├── distribution.service.ts    # 移除 confirmOrder，修复批量删除
└── distribution.controller.ts  # 移除 confirm 接口
```

**前端：**
```
frontend/src/
├── stores/order.ts            # 移除 confirmOrder
├── api/distribution.ts        # 移除 confirmOrder API
└── views/Orders.vue           # 移除确认按钮，优化编辑体验
```

---

## 十七、字段概念说明

### 17.1 库存与配货单字段对照

| 概念 | 库存字段 | 说明 | 配货单对应 |
|------|----------|------|-----------|
| **品级** | `grade` | 镍含量等级 | `targetGrade` |
| **产品类型** | `productType` | 生产工艺/用途 | `productType` |
| **产品规格** | `specification` | 物理尺寸/形态 | `specification` |

### 17.2 固定选项

**品级（从库存提取）**：
- 9997（最高）
- 9996
- 9950
- 9920

**产品类型（固定选项）**：
- 电解镍
- 电积镍
- 不锈钢专用镍
- 电镀专用镍

**产品规格（固定选项）**：
- 整板
- 镍条
- 100*100
- 50*50
- 25*25
- 20*20
- 镍包

---

## 十八、端到端测试命令参考

### 18.1 JWT 认证测试

```bash
# 1. 登录获取 Token
TOKEN=$(curl -s -X POST http://localhost:3002/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' \
  | grep -o '"access_token":"[^"]*"' | cut -d'"' -f4)

# 2. 携带 Token 访问
curl http://localhost:3002/distribution/statistics \
  -H "Authorization: Bearer $TOKEN"
```

### 18.2 配货单状态机测试

```bash
# 创建库存
curl -X POST http://localhost:3002/distribution/inventory \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"batchNo":"TEST-001","grade":"9997","weight":100,"pieceCount":10}'

# 创建订单 -> 发货 -> 完成
curl -X POST http://localhost:3002/distribution/orders/:id/ship \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"driverName":"司机","vehicleNo":"京A12345"}'
curl -X POST http://localhost:3002/distribution/orders/:id/deliver -H "Authorization: Bearer $TOKEN"
```

---

_最后更新：2026-04-20_

---

## 二十三、功能合并记录（2026-04-21）

### 23.1 合并说明

基于 v1.5.7 功能，合并到 v1.5.6 基础代码，**去除导出功能**。

### 23.2 合并的后端功能

| 功能 | 说明 | 文件 |
|------|------|------|
| JWT 认证 | 用户注册/登录/JWT 策略 | `auth/*.ts` |
| 全局限流 | 300次/分钟，登录10次/分钟 | `app.module.ts`, `throttler.guard.ts` |
| 统计缓存 | TTL 30秒，操作后清除 | `distribution.service.ts` |
| 客户存在性校验 | 创建订单前校验客户 | `distribution.service.ts` |
| 库存远程搜索 | el-select 下拉选择 | `distribution.controller.ts`, `distribution.service.ts` |
| 订单列表优化 | includeItems 参数，默认不加载明细 | `distribution.controller.ts`, `distribution.service.ts` |
| transformOptions | enableImplicitConversion | `main.ts` |
| User 模型 | Prisma schema 添加 | `schema.prisma` |

### 23.3 合并的前端功能

| 功能 | 说明 | 文件 |
|------|------|------|
| JWT Token 自动附加 | 请求拦截器添加 | `api/request.ts` |
| 401 自动跳转登录 | 响应拦截器处理 | `api/request.ts` |
| 认证 API | login/register/getProfile | `api/auth.ts` |
| 订单类型修复 | 移除 confirmed | `types/index.ts` |
| 库存搜索 API | searchInventory | `api/distribution.ts` |
| Orders.vue 修复 | h4 标签闭合 | `views/Orders.vue` |

### 23.4 未合并的功能

| 功能 | 原因 |
|------|------|
| 导出库存 Excel | Decimal 序列化问题待解决 |

### 23.5 新增文件

```
backend/src/auth/
├── dto/auth.dto.ts
├── auth.controller.ts
├── auth.service.ts
├── auth.module.ts
├── jwt.strategy.ts
└── guards/jwt-auth.guard.ts

backend/src/common/guards/throttler.guard.ts
backend/.env.example
frontend/src/api/auth.ts
```

### 23.6 测试结果

| 测试项 | 结果 |
|--------|------|
| 后端 TypeScript 编译 | ✅ 通过 |
| 前端 Vite 构建 | ✅ 通过 |

### 23.7 启动命令

```bash
# 1. 更新 Prisma schema
cd backend
npx prisma generate
npx prisma db push

# 2. 启动后端
npm run start:dev

# 3. 启动前端
cd frontend
npm run dev
```

### 23.8 默认账户

- 用户名: `admin`
- 密码: `admin123`

---

## 二十四、端到端测试结果（2026-04-21）

### 24.1 测试概述

| 轮次 | 测试项 | 通过 | 失败 | 成功率 |
|------|--------|------|------|--------|
| 第1轮 | 16 | 16 | 0 | 100% |
| 第2轮 | 16 | 16 | 0 | 100% |
| 第3轮 | 16 | 16 | 0 | 100% |
| 第4轮 | 16 | 16 | 0 | 100% |
| **总计** | **64** | **64** | **0** | **100%** |

### 24.2 测试模块覆盖

| 模块 | 测试项 | 状态 |
|------|--------|------|
| 🔐 认证 | 登录、获取用户信息、注册 | ✅ |
| 📊 统计 | 全局统计概览 | ✅ |
| 📦 库存 | 创建、分页查询、搜索、更新 | ✅ |
| 👥 客户 | 创建、查询、更新 | ✅ |
| 📋 配货单 | 创建、分页查询、发货、完成发运 | ✅ |
| 🤖 AI识别 | 查询识别历史 | ✅ |
| 🏥 健康检查 | 服务健康状态 | ✅ |

### 24.3 测试修复

**E2E 测试脚本问题修复**：
- **问题**：响应拦截器直接返回 `response`，导致无法获取 `access_token`
- **修复**：响应拦截器应返回 `response.data`
- **文件**：`backend/test-e2e.js`

```javascript
// 修复前
request.interceptors.response.use((response) => {
  return response;  // ❌ 返回整个响应对象
});

// 修复后
request.interceptors.response.use((response) => {
  return response.data;  // ✅ 解包 data
});
```

---

## 二十五、v1.7.0 重构（2026-05-06）

### 25.1 Demo展厅清理

- 删除 5 个 Demo 展厅文件（DemoSelector、Demo1~4）
- 清理 router 中的 demo 路由和面包屑配置
- 导航栏「3D展厅」替换为「仓库」

### 25.2 3D仓库入口修复

- **问题**：首页无 3D 仓库入口，点击导航栏「仓库」按钮无响应
- **根因 1**：缺少 `/warehouse` 路由配置
- **根因 2**：`three.js` 运行时包缺失（仅安装了 `@types/three`）
- **根因 3**：`warehouse-builder.ts` 中 `WarehouseZone` 类型未导入
- **修复**：
  - 添加 `/warehouse` 路由
  - `npm install three` 补全依赖
  - 补全 `WarehouseZone` import
  - 导航栏添加「仓库」链接

### 25.3 登录页UI重构

- **主题系统**：独立于 App.vue 的全局主题，使用 `.theme-light` / `.theme-dark` 类名
- **暗色主题**：深蓝紫渐变背景 + 动态光球 + 网格纹理
- **亮色主题**：纯白基底 + 淡紫渐变点缀
- **输入框**：去除白底色，使用透明背景 + 边框线条
- **品牌区**：渐变文字 + 扫光动画 + Feature 列表
- **右上角切换按钮**：太阳/月亮图标切换亮暗主题
- **根因修复**：`:class="theme"` 产生 `class="light"` 但 CSS 是 `.theme-light`，已修复为 `:class="'theme-' + theme"`

### 25.4 修改文件清单

```
frontend/src/views/Login.vue          # 登录页UI完全重构
frontend/src/views/Warehouse.vue      # 未改动（v1.4.0原版）
frontend/src/three/warehouse-layout.ts # 未改动（v1.4.0原版）
frontend/src/three/warehouse-builder.ts # 修复 WarehouseZone import
frontend/src/router/index.ts           # 添加 /warehouse 路由，清理 demo 路由
frontend/src/App.vue                   # 导航栏添加「仓库」链接
frontend/package.json                  # 新增 three 依赖
```

---

## 二十六、v1.7.2 Mobile App 发布（2026-05-09）

### 26.1 项目概述

Flatstore Mobile 是平面库配货系统的手机端应用，基于 **Vue 3 + TypeScript + Vite + Capacitor 7** 构建，提供原生 Android App 体验。与 Web 前端和后端共享同一数据库，实现数据完全同步。

- **包名**: `com.flatstore.mobile`
- **构建产物**: `dist/index.js` 160KB（gzip: 62KB）

### 26.2 页面清单

| 页面 | 功能 |
|------|------|
| 登录页 | JWT 认证、登录/注册切换 |
| 首页 | 实时统计数据、库存分布可视化、最近配货单、AI 快捷入口 |
| 库存列表 | 搜索、品级筛选、卡片列表、分页加载、库存详情 |
| 配货单列表 | 状态筛选、新建配货单（BottomSheet + 库存选择）、详情 |
| AI 识别 | 原生相机/相册、识别历史、结果展示、批量导入 |
| 个人中心 | 用户信息、快捷入口、退出登录 |

### 26.3 测试结果

| 测试项 | 结果 |
|--------|------|
| TypeScript 类型检查 | ✅ 零错误 |
| Vite 生产构建 | ✅ 1.08s |
| 后端 API 连通性 | ✅ 正常 |
| CORS 配置 | ✅ 正常 |
| Gradle 构建 | ✅ BUILD SUCCESSFUL |
| APK 安装（ADB） | ✅ 正常 |
| 端到端测试 | ✅ 全部通过 |

---

## 二十七、v1.8.0 全项目质量审计（2026-05-11）

### 27.1 审计概览

对 Flatstore 项目进行全面代码审查，覆盖后端（NestJS）、Web 前端（Vue 3）、Mobile App、数据库架构。Web 端发现 **25 项问题**，移动端发现 **7 项问题**，经 QA 验证后驳回 3 项，确认 **25 项有效**，当天完成 **21 项修复**（Web 端 14 项 + 移动端 7 项）。

| 优先级 | 发现 | 驳回 | 已修复 | 待处理 |
|--------|------|------|--------|--------|
| 🔴 P0 | 5 | 1 | **4** | 0 |
| 🟡 P1 | 12 | 2 | **7** | 3 |
| 🟠 P2 | 12 | 0 | **10** | 2 |
| 🟢 P3 | 4 | 0 | 0 | 4 |
| **合计** | **33** | **3** | **21** | **9** |

### 27.2 驳回记录（3 项）

| 原# | 描述 | 驳回原因 |
|-----|------|----------|
| 原 P0#2 | deleteOrder 未释放已取消订单的库存 | `cancelOrder` 已释放库存；`deleteOrder` 仅允许删除 shipped/cancelled 订单，shipped 订单货物已物理出库不应恢复。设计如此，非 bug |
| 原 P1#5 | Dashboard 图表初始化未使用 nextTick | `onMounted` 中已正确 `await fetchStatistics()` 后调用 `initCharts()`，DOM 在 mounted 时已就绪 |
| 原 P1#11 | ValidationPipe forbidNonWhitelisted 过于严格 | 正确的安全实践，前后端 DTO 应保持对齐 |

### 27.3 已修复清单（21 项）

#### 🔴 P0 — 安全与数据正确性

**#1 JWT_SECRET 回退不安全**
- 文件: `backend/src/auth/jwt.strategy.ts`
- 修复: 移除硬编码 fallback `|| 'flatstore-secret-key-change-in-production'`，未配置环境变量时启动即失败

**#2 AI 识别净重单位（吨 → kg）**
- 文件: `frontend/src/views/AI.vue`
- 修复: `handleImportSubmit` 中 `weight: ((r.netWeight \|\| 0) as number) * 1000`，AI 返回净重单位为吨，库存表单位为 kg

#### 🟡 P1 — 架构与性能

**#3 statsCache 模块级全局变量竞态风险**
- 文件: `backend/src/distribution/distribution.service.ts`
- 修复: 改为类实例属性 `this.statsCache`，新增 `invalidateStatsCache()` 和 `setStatsCache()` 方法，所有引用统一替换

**#4 CORS 过于宽松**
- 文件: `backend/src/main.ts`
- 修复: `callback(null, true)` 无条件放行改为严格校验，拒绝未在 `allowedOrigins` 中的来源

**#5 前端 weight 类型不一致**
- 文件: `frontend/src/types/index.ts`
- 修复: `InventoryStock.weight` 添加注释说明 Prisma Decimal 序列化行为

**#6 软删除字段无索引**
- 文件: `backend/prisma/schema.prisma`
- 修复: Customer 表新增 `@@index([deletedAt])`

#### 🟠 P2 — 代码质量

**#7 Store `as any` 泛滥**
- 文件: `frontend/src/stores/inventory.ts`, `stores/order.ts`
- 修复: 替换为明确类型断言 `as { data: InventoryStock[]; total: number }`

**#8 Store fetch 无 error 反馈**
- 文件: 同上
- 修复: 添加 catch 块重置数据，保留拦截器提示

**#9 路由守卫不验证 Token 有效期**
- 文件: `frontend/src/router/index.ts`
- 修复: 解析 JWT payload.exp 检查过期时间，过期自动清除 token 并跳转登录页

**#10 batchDeleteInventory 逻辑不精确**
- 文件: `backend/src/distribution/distribution.service.ts`
- 修复: 逐个检查每个 id 状态，分别返回不存在和不可删除的具体 id 列表

**#11 QwenAIService 仅重试 1 次**
- 文件: `backend/src/common/services/qwen-ai.service.ts`
- 修复: `MAX_RETRIES` 改为 2，指数退避（1s→2s→4s），429 限流重试延迟也增加

**#12 console.log 调试代码残留**
- 文件: `backend/src/distribution/distribution.service.ts`
- 修复: 移除 `aiRecognize` 中的两处 `console.log` 调试输出

**#13 deleteOrder 死代码**
- 文件: `backend/src/distribution/distribution.service.ts`
- 修复: 移除未使用的 `stockIds` 变量和冗余事务包装，简化为直接软删除

**#14 deleteOrder 死代码（续）**
- 文件: `backend/src/distribution/distribution.service.ts`
- 修复: `batchDeleteOrders` 同样移除未使用的 `shippedOrders` 变量和冗余事务

#### 📱 移动端修复（7 项）

**#15 P0-3 路由守卫 Token 过期验证**
- 文件: `mobile/src/router/index.ts`
- 修复: 解析 JWT payload.exp 检查过期时间，过期自动清除 token 并跳转登录页

**#16 P0-4 AI 识别净重单位（吨 → kg）**
- 文件: `mobile/src/views/AiView.vue`
- 修复: `handleImport` 中 `weight: (r.netWeight || 0) * 1000`，与 Web 端对齐

**#17 P1-7 Store `as any` 泛滥**
- 文件: `mobile/src/stores/inventory.ts`, `mobile/src/stores/order.ts`
- 修复: 替换为明确类型断言，与 Web 端对齐

**#18 P1-8 Store fetch 无 error 反馈**
- 文件: 同上
- 修复: 添加 catch 块重置数据，保留拦截器提示，与 Web 端对齐

**#19 P1-9 断网无提示**
- 文件: `mobile/src/api/request.ts`
- 修复: 检测 `!navigator.onLine` 注入断网错误信息

**#20 P1-10 注册功能未移除**
- 文件: `mobile/src/stores/auth.ts`
- 修复: 删除 register 方法，与 Web 端对齐

**#21 P2-15 order store loading 异常**
- 文件: `mobile/src/stores/order.ts`
- 修复: loading 改为 `computed(loadingList || loadingDetail)`

### 27.4 修改文件清单

| 文件 | 修改内容 |
|------|----------|
| `backend/src/auth/jwt.strategy.ts` | JWT_SECRET 移除 fallback |
| `backend/src/distribution/distribution.service.ts` | **完全重写** — statsCache 实例化、删除调试代码、精确批量删除、移除死代码 |
| `backend/src/main.ts` | CORS 收紧校验 |
| `backend/src/common/services/qwen-ai.service.ts` | 重试次数 1→2，指数退避 |
| `backend/prisma/schema.prisma` | Customer deletedAt 索引 |
| `frontend/src/views/AI.vue` | AI 识别净重 ×1000 转换 |
| `frontend/src/types/index.ts` | weight 类型注释 |
| `frontend/src/router/index.ts` | Token 有效期验证 |
| `frontend/src/stores/inventory.ts` | 类型断言 + 错误处理 |
| `frontend/src/stores/order.ts` | 类型断言 + 错误处理 |
| `mobile/src/router/index.ts` | Token 过期验证（解析 JWT payload.exp） |
| `mobile/src/views/AiView.vue` | AI 识别净重 ×1000 转换 |
| `mobile/src/stores/inventory.ts` | 类型断言 + 错误处理 |
| `mobile/src/stores/order.ts` | 类型断言 + 错误处理 + loading computed |
| `mobile/src/api/request.ts` | 断网检测 + 错误提示 |
| `mobile/src/stores/auth.ts` | 删除 register 方法 |

### 27.5 输出文档

| 文档 | 路径 |
|------|------|
| 问题清单 | `D:\Flatstore\问题清单.md` |
| 扫码出库模块技术方案 | `D:\Flatstore\扫码出库模块-技术方案.md` |

### 27.6 待处理事项

| 优先级 | 问题 | 说明 |
|--------|------|------|
| 🟡 P1 | 状态枚举约束 | 需改 Prisma Enum，影响所有 CRUD，建议单独 PR |
| 🟠 P2 | 导出接口缺失 | `export-inventory.dto.ts` 存在但无路由，需确认是否开发 |
| 🟠 P2 | 无数据库迁移管理 | 需切换 `prisma migrate` 工作流 |
| 🟠 P2 | 仅 Android 平台 | iOS 支持属功能扩展 |
| 🟢 P3 | Helmet 安全头 | 生产环境配置 CSP/HSTS |
| 🟢 P3 | Mobile 离线支持 | IndexedDB 缓存 |
| 🟢 P3 | statistics store retry | 网络失败自动重试 |
| 🟢 P3 | API 版本控制 | `/api/v1/` 前缀 |

---

## 二十八、扫码出库模块规划（2026-05-11）

### 28.1 需求背景

现场人员使用扫码枪扫描镍板条形码（25 位统一行业编码），需要：
1. 自动解析条形码，提取批号、包号、品级、车间、净重等信息
2. 在屏幕上实时显示出库清单
3. **人工核对**后再决定是否出库（不自动修改数据库）

### 28.2 条形码规则（统一行业编码）

```
N1N2N3 N4N5 N6N7 N8N9N10N11N12N13 N14N15N16N17N18N19N20 N21N22N23N24N25
│      │    │    │                   │                      │
│      │    │    │                   │                      └─ 捆净重×10 (5位)
│      │    │    │                   └─ 七位生产序号代码
│      │    │    └─ 生产日期 YYMMDD (6位)
│      │    └─ 品级代码 (2位)
│      └─ 产品类别代码 (2位)
└─ 企业代码 (3位)
```

### 28.3 技术方案

详见 `D:\Flatstore\扫码出库模块-技术方案.md`

---

## 二十九、Mobile APK Network Error 修复（2026-05-11）

### 29.1 问题描述

APK 安装到手机后，登录页显示 **Network Error**，无法登录。后端服务、ADB 反向代理、网络安全配置均正常。

### 29.2 根因（双因素叠加）

| # | 根因 | 文件 | 说明 |
|---|------|------|------|
| 1 | Capacitor 环境检测时机错误 | `mobile/src/api/request.ts` | `!!(window as any).Capacitor` 在模块加载时求值，bridge 未就绪 → `isCapacitor=false` → `BASE_URL` 回退为 `/` |
| 2 | CORS 白名单缺少 Capacitor origin | `backend/src/main.ts` | Capacitor Android WebView 的 origin 是 `https://localhost`，不在 CORS 白名单中 |

**关键认知**：Axios 将 CORS 拒绝报告为 "Network Error"（而非 CORS Error），导致排查方向偏向网络层。

### 29.3 修复方案

| Fix | 文件 | 修改 |
|-----|------|------|
| Capacitor 检测 | `mobile/src/api/request.ts` | `!!(window as any).Capacitor` → `Capacitor.isNativePlatform()`（from `@capacitor/core`） |
| CORS 白名单 | `backend/src/main.ts` | 添加 `https://localhost`、`http://localhost`、`capacitor://localhost` |
| 网络安全配置 | `mobile/android/.../network_security_config.xml` | `domain-config` → `base-config cleartextTrafficPermitted="true"` |
| 登录页清理 | `mobile/src/views/LoginView.vue` | 移除废弃的注册 UI |

### 29.4 修改文件清单

| 文件 | 修改内容 |
|------|----------|
| `mobile/src/api/request.ts` | Capacitor 环境检测改用官方 API |
| `mobile/src/views/LoginView.vue` | 移除注册功能 UI |
| `mobile/android/app/src/main/res/xml/network_security_config.xml` | 全局允许明文 HTTP |
| `backend/src/main.ts` | CORS 白名单添加 Capacitor origin |

### 29.5 测试结果

| 测试项 | 结果 |
|--------|------|
| TypeScript 编译 | ✅ 零错误 |
| Vite 生产构建 | ✅ |
| Gradle 构建 | ✅ BUILD SUCCESSFUL |
| CORS 模拟（Origin: https://localhost） | ✅ 201 |
| **手机端登录** | ✅ **已确认正常**（2026-05-11 11:40） |

---

---

## 三十、v1.8.1 移动端顶部安全区域适配（2026-05-11）

### 30.1 问题描述

手机顶部状态栏（时间、通知图标）和灵动岛/刘海与 App 内容重叠，"平面库配货"标题被遮挡。

### 30.2 根因

各页面顶部间距未考虑 `safe-area-inset-top`，异形屏状态栏区域高度（44-59px）直接覆盖页面内容。

### 30.3 修复方案

| 修改 | 文件 | 说明 |
|------|------|------|
| CSS 变量 | `mobile/src/styles/variables.css` | 新增 `--safe-top`、`--page-header-top: calc(var(--safe-top) + 16px)` |
| HomeView | `mobile/src/views/HomeView.vue` | `.home-view` padding-top 使用 `var(--page-header-top)` |
| InventoryView | `mobile/src/views/InventoryView.vue` | 同上 |
| OrdersView | `mobile/src/views/OrdersView.vue` | 同上；`.page-header` padding `4px` → `12px` 与首页统一 |
| AiView | `mobile/src/views/AiView.vue` | 同上；`.page-header` padding `4px` → `12px` |
| ProfileView | `mobile/src/views/ProfileView.vue` | 新增统一 `page-header` 结构（label + 标题），padding 对齐其他页面 |
| InventoryDetail | `mobile/src/views/InventoryDetail.vue` | `.detail-body` padding-top 增加 `var(--safe-top)` 避让 sticky header |
| OrderDetail | `mobile/src/views/OrderDetail.vue` | 同上 |
| LoginView | `mobile/src/views/LoginView.vue` | padding-top 增加 `var(--safe-top)` |

### 30.4 统一样式规范

修改后所有 tab 页面（首页/库存/配货/AI/我的）顶部结构一致：

```
|-- safe-area-inset-top (设备状态栏高度)     |
|-- 16px 呼吸间距                           |
|-- page-header (12px 20px 16px) 标签+标题  |
|-- 页面内容                                |
```

### 30.5 修改文件清单

| 文件 | 修改内容 |
|------|----------|
| `mobile/src/styles/variables.css` | 新增 `--safe-top`、`--page-header-top` 变量 |
| `mobile/src/views/HomeView.vue` | padding-top 适配安全区域 |
| `mobile/src/views/InventoryView.vue` | 同上 |
| `mobile/src/views/OrdersView.vue` | 同上 + `.page-header` padding 统一 |
| `mobile/src/views/AiView.vue` | 同上 + `.page-header` padding 统一 |
| `mobile/src/views/ProfileView.vue` | 新增 page-header + padding-top 适配 |
| `mobile/src/views/InventoryDetail.vue` | detail-body 顶部避让 sticky header |
| `mobile/src/views/OrderDetail.vue` | 同上 |
| `mobile/src/views/LoginView.vue` | padding-top 适配安全区域 |

### 30.6 测试结果

| 测试项 | 结果 |
|--------|------|
| TypeScript 编译 | ✅ 零错误 |
| Vite 生产构建 | ✅ 996ms |
| cap sync android | ✅ 0.125s |
| Gradle 构建 | ✅ BUILD SUCCESSFUL in 3s |
| APK 大小 | ~8.0 MB |

---

_最后更新：2026-05-11 v1.9.0 双端库存筛选升级 + AI 识别优化_

---

## 三十一、v1.9.0 双端库存筛选升级 + AI 识别优化（2026-05-11）

### 31.1 Web 端库存筛选升级

| 维度 | 变更前 | 变更后 |
|------|--------|--------|
| 关键词 | 批号/品级/规格 | 批号/规格/位置 |
| 品级 | 文本输入框 | 下拉选择（Ni9997/9996/9950/9920） |
| 产品类型 | 无 | **新增**下拉选择（电解镍/电积镍/...） |
| 规格 | 无 | **新增**下拉选择（整板/镍条/100*100/50*50/25*25） |
| 状态 | 下拉选择 | 保留 |
| 创建日期 | 无 | **新增**单日日期选择器（占位显示"全部日期"） |

### 31.2 手机端库存筛选升级

| 维度 | 变更前 | 变更后 |
|------|--------|--------|
| 品级 | FilterPills | 保留 |
| 产品类型 | 无 | **新增** FilterPills（电解镍/电积镍/...） |
| 规格 | 无 | **新增** FilterPills（整板/镍条/...） |
| 状态 | 底部 FilterPills | **改为顶部统计卡片可点击筛选**（总批次/可用/预留） |
| 创建日期 | 无 | **新增**单日日期选择器 |

### 31.3 AI 识别页面优化

- **去除 emoji 图标**：拍照📷/相册🖼 → SVG 线性图标
- **识别结果重构**：对齐 Web 端表格布局，每条结果独立卡片，顶部序号+批号+品级，详情区逐行展示，净重/块数同行并列
- **识别历史时间筛选修复**：`selectedTime` watch 缺失导致点击无响应 → 添加 watch 并传入后端 `timeRange` 参数

### 31.4 后端变更

- `getInventoryList` 新增 `specification` 精确匹配 + `dateFrom` 单日范围筛选
- `getRecognitionHistory` 新增 `timeRange` 参数（today/week）

### 31.5 修改文件清单

| 文件 | 修改内容 |
|------|----------|
| `backend/src/distribution/distribution.controller.ts` | specification + dateFrom + timeRange 参数 |
| `backend/src/distribution/distribution.service.ts` | 对应筛选逻辑实现 |
| `frontend/src/types/index.ts` | InventoryQuery 新增 specification/dateFrom |
| `frontend/src/views/Inventory.vue` | 5 维度筛选 + 单日日期选择器 |
| `mobile/src/types/index.ts` | InventoryQuery 新增 specification/dateFrom，RecognitionHistoryQuery 新增 timeRange |
| `mobile/src/views/InventoryView.vue` | 4 维度 FilterPills + 统计卡片筛选 + 日期选择 |
| `mobile/src/components/StatCard.vue` | 新增 highlight + click 事件支持 |
| `mobile/src/views/AiView.vue` | emoji → SVG 图标，识别结果卡片重构，时间筛选 watch 修复 |

---

## 32. v1.10.0 — Web 前端全面设计升级（2026-05-11）

### 32.1 设计 Tokens 升级（variables.scss）

- **字体**：从 Apple 系统字体换为 \DM Sans\ + \JetBrains Mono\ 组合，更具辨识度
- **颜色体系**：全面切换至 Tailwind 色系 — 主色 \#3B82F6\（Blue 500），文字用 Slate 色系（\#0F172A\ / \#64748B\ / \#94A3B8\），背景用暖灰 \#F8FAFC\
- **间距**：严格 4px 步进（4/8/12/16/24/32/40/48），新增 \--spacing-3xl\ / \--spacing-4xl\
- **圆角**：层级化 sm(6px) / md(10px) / lg(14px) / xl(20px) / pill(999px)
- **阴影**：多层柔和阴影替代单层硬阴影，新增 \--glass-shadow-lg\
- **动效**：统一 \cubic-bezier(0.16, 1, 0.3, 1)\ easing，150ms/200ms/350ms 三档

### 32.2 全局样式重构（global.scss）

- 新增 typography 基础规则（h1-h6 统一字重/间距）
- **data-table**：全局统一表格样式（sticky thead、hover 行高亮）
- **统一输入框**：focus 状态用 \ox-shadow: 0 0 0 3px var(--color-primary-100)\
- **glass-card**：去除 hover 抖动效果，更克制
- **滚动条**：从 8px 缩减为 6px，更精致
- **统一 Loading/Empty/Error 状态**：新增全局规范
- 页面切换动画统一为 \ar(--transition-page)\
- 删除重复的 keyframes 定义

### 32.3 各页面优化

| 页面 | 改动要点 |
|------|---------|
| **App.vue** | 导航 pill 形化，活跃态指示器更精致，toast/下拉菜单统一 |
| **Dashboard** | 统计卡片渐变色换为 Tailwind 现代色系，数字 28px/700 字重，padding 更透气 |
| **AI.vue** | 上传区域更有呼吸感，图标更立体，空状态改用 SVG |
| **Inventory.vue** | 表格更紧凑，spinner 更精致，font-mono 统一为变量，modal 动画统一 |
| **Orders.vue** | 表格/加载/空状态统一，移除 emoji，表单 focus shadow 统一 |
| **Customers.vue** | 空状态 SVG 替代 emoji，删除未使用 keyframes，行 hover 简化 |
| **Login.vue** | 保持不变（已有高级主题系统） |
| **Warehouse.vue** | 统计图标渐变色切换为 Tailwind 色系，数字字重升级 |
| **Test.vue** | 日志容器色切换为 Slate 900，边框统一，字体/圆角统一 |

### 32.4 修改文件清单

| 文件 | 修改内容 |
|------|----------|
| \rontend/src/styles/variables.scss\ | 设计 Tokens 全面升级 |
| \rontend/src/styles/global.scss\ | 全局样式重构 |
| \rontend/src/App.vue\ | 导航/toast/下拉菜单优化 |
| \rontend/src/views/Dashboard.vue\ | 统计卡片/数字排版升级 |
| \rontend/src/views/AI.vue\ | 上传区域/空状态优化 |
| \rontend/src/views/Inventory.vue\ | 表格/加载/modal 动画统一 |
| \rontend/src/views/Orders.vue\ | 表格/加载/空状态统一 |
| \rontend/src/views/Customers.vue\ | 空状态/行 hover/keyframes 优化 |
| \rontend/src/views/Warehouse.vue\ | 统计图标/数字升级 |
| \rontend/src/views/Test.vue\ | 日志容器/字体/圆角统一 |

---

## 33. v1.11.0 — 移动端 App 全面设计升级（2026-05-11）

### 33.1 设计 Tokens 升级（variables.css）

- **字体**：标题用 DM Sans，正文用 DM Sans，数据用 JetBrains Mono
- **颜色**：切换至 Tailwind 色系，中性色带色调（warm gray）
- **间距**：统一到 4px 基础系统（4/8/12/16/20/24/32px）
- **圆角**：sm(6px) / md(10px) / lg(14px) / xl(20px)
- **阴影**：多层柔和阴影
- **动效**：cubic-bezier(0.16, 1, 0.3, 1) easing，微交互 150ms

### 33.2 全局样式与组件优化

| 组件 | 改动要点 |
|------|---------|
| **global.css** | 统一卡片/列表项/按钮设计语言，优化空/加载状态 |
| **TabBar** | 底部导航更精致，活跃态指示器清晰 |
| **FilterPills** | 筛选标签更有质感 |
| **BottomSheet** | 弹出面板过渡更流畅 |
| **StatCard** | 统计数字层次更清晰 |
| **OrderCard / InventoryCard** | 信息层次更分明 |
| **AiHistoryItem** | 历史记录卡片更精致 |

### 33.3 页面优化

| 页面 | 改动要点 |
|------|---------|
| **HomeView** | 欢迎区更有氛围，统计卡片更精致 |
| **InventoryView** | 筛选栏间距更合理，列表更清晰 |
| **InventoryDetail** | 详情页排版更优，编辑表单更规范 |
| **OrdersView** | 状态标签一致性 |
| **OrderDetail** | 详情信息层次分明 |
| **AiView** | 识别结果卡片更清晰，导入表单更规范 |
| **ProfileView** | 个人中心简洁精致 |
| **LoginView** | 登录表单精致感 |

### 33.4 修改文件清单

| 文件 | 修改内容 |
|------|----------|
| \mobile/src/styles/variables.css\ | 设计 Tokens 全面升级 |
| \mobile/src/styles/global.css\ | 全局样式重构 |
| \mobile/src/App.vue\ | 应用框架优化 |
| \mobile/src/components/TabBar.vue\ | 底部导航优化 |
| \mobile/src/components/*.vue\ | 8 个组件统一设计 |
| \mobile/src/views/*.vue\ | 8 个页面优化 |

---

## 34. v1.12.0 — AI 识别导入 + 手动录入下拉菜单（2026-05-11）

### 34.1 Web 端 AI 识别批量导入

- **新增规格下拉菜单**：整板、镍条、100\*100、50\*50、25\*25
- **新增存放位置下拉菜单**：二厂区、三厂区
- 导入预览表格同步新增规格和存放位置列
- 导入逻辑包含 \specification\ 字段

### 34.2 Web 端手动录入库存

- **规格**：从文本输入框改为下拉菜单，选项同上

### 34.3 移动端 AI 识别导入

- **新增导入设置 Sheet**：点击"导入全部到库存"后弹出设置面板
- **规格下拉菜单**：整板、镍条、100\*100、50\*50、25\*25
- **存放位置下拉菜单**：二厂区、三厂区
- 导入预览显示规格、位置、记录数
- 数据同步包含 specification 和 location

### 34.4 移动端库存编辑

- **规格**：从文本输入改为下拉菜单
- **存放位置**：从文本输入改为下拉菜单（二厂区、三厂区）

### 34.5 修改文件清单

| 文件 | 修改内容 |
|------|----------|
| \rontend/src/views/AI.vue\ | 导入弹窗新增规格/存放位置下拉 |
| \rontend/src/views/Inventory.vue\ | 手动录入规格改为下拉菜单 |
| \mobile/src/views/AiView.vue\ | 新增导入设置 Sheet + 下拉菜单 |
| \mobile/src/views/InventoryDetail.vue\ | 编辑表单规格/位置改为下拉 |
---

## 35. v1.14.0 — 移动端 Sticky Header + BottomSheet 滚动优化 (2026-05-12)

### 35.1 问题描述

1. 页面往下滑时，顶部"平面库配货"标题和页面标题随滚动消失，用户无法确认当前页面
2. 四个 Tab 页面（首页/库存/配货/我的）顶部留白间距不一致
3. 标题与手机状态栏（时间等）重叠
4. BottomSheet 滚动时拖拽条消失，下方标题文字溢出

### 35.2 修复方案

#### Sticky Page Header

- 所有 Tab 页面的 `.page-header` 添加 `position: sticky; top: 0; z-index: 10; background: var(--bg)`
- 将外层容器的 `padding-top: var(--page-header-top)` 移除，改为 `page-header` 自身的 `padding-top: var(--page-header-top)`
- 这样顶部安全区空白 + 标题在滚动时都固定不动

#### 顶部留白增大

- `--page-header-top` 从 `calc(var(--safe-top) + 16px)` 增大至 `calc(var(--safe-top) + 44px)`
- Android 上 `env(safe-area-inset-top)` 通常为 0，需要足够的固定 padding 避开状态栏

#### BottomSheet 结构重构

- 将"整体滚动 + sticky bar"改为"固定头部 + 独立滚动体"结构
- `.sheet` 改为 `display: flex; flex-direction: column`
- `.sheet-bar` 设为 `flex-shrink: 0`，固定不滚动
- 新增 `.sheet-body` 包裹 slot 内容，`flex: 1; overflow-y: auto`，仅此区域滚动
- 拖拽条和标题永远固定在顶部，内容在 body 内滚动不会溢出

### 35.3 修改文件清单

| 文件 | 修改内容 |
|------|----------|
| `mobile/src/styles/variables.css` | `--page-header-top` 增大至 `calc(var(--safe-top) + 44px)` |
| `mobile/src/views/HomeView.vue` | page-header sticky + padding-top 移入 header |
| `mobile/src/views/InventoryView.vue` | 同上 |
| `mobile/src/views/OrdersView.vue` | 同上 |
| `mobile/src/views/AiView.vue` | 同上 |
| `mobile/src/views/ProfileView.vue` | 同上 |
| `mobile/src/components/BottomSheet.vue` | 重构为固定头部 + 独立滚动体 |

### 35.4 测试结果

| 测试项 | 结果 |
|--------|------|
| Vite 生产构建 | ✓ 1.01s |
| Gradle 构建 | ✓ BUILD SUCCESSFUL in 2s |
| ADB 安装 | ✓ Success |
| Sticky header 滚动固定 | ✓ 四个 Tab 页面一致 |
| 状态栏不重叠 | ✓ 44px 顶部留白 |
| BottomSheet 拖拽条固定 | ✓ 滚动内容不溢出 |

## 36. v1.15.0 - 配货业务流程优化 (2026-05-12)

### 36.1 配货单创建防重
- 重复 stockId 校验：同一库存不可被同一订单重复选择
- productType/specification 自动聚合：创建订单时从库存提取产品类型和规格

### 36.2 发货强制校验
- ShipOrderDto driverName/vehicleNo 改为必填
- 后端 validator 自动拒绝空值，前端加了必填标记 + 前端校验

### 36.3 草稿订单可编辑明细
- updateOrder 支持 items 更新：草稿订单可增删明细项
- 旧库存释放→新库存锁定（事务保证原子性）

### 36.4 客户删除保护
- deleteCustomer 校验关联订单：有进行中配货单时禁止删除

### 36.5 完成配送幂等保护
- deliverOrder 加前置状态校验，确认所有库存均为 reserved
- 统一在事务内更新

### 36.6 库存关联反查
- getInventoryById 返回 linkedOrders：知道库存被哪个订单锁定
- 前端 InventoryDetail 显示关联配货单列表，可点击跳转

### 36.7 前端安全加固
- 取消订单加 confirm 确认，不可撤销提醒
- AI 识别临时文件成功后也清理

### 36.8 修改文件清单

| 文件 | 修改内容 |
|------|----------|
| `backend/prisma/schema.prisma` | DistributionOrder 新增 product_type, specification |
| `backend/src/distribution/distribution.service.ts` | 重复校验、发货必填、更新明细、客户保护、库存反查、文件清理 |
| `backend/src/distribution/dto/order.dto.ts` | ShipOrderDto 必填、UpdateOrderDto 支持 items |
| `mobile/src/views/OrderDetail.vue` | 发货必填校验、取消确认弹窗 |
| `mobile/src/views/InventoryDetail.vue` | 关联配货单展示 + 跳转 |

### 36.9 E2E 测试结果

- TEST 1: 重复 stockId 被拒绝 ✓
- TEST 2: 正常创建订单（productType 自动聚合）✓
- TEST 3: 空司机/车牌发货被拒绝 ✓
- TEST 4: 正常发货 ✓
- TEST 5: 确认送达 ✓
- TEST 6: 库存详情返回关联订单 ✓
- TEST 7: 有订单的客户删除被拒绝 ✓
- TEST 8: 草稿订单更新明细项 ✓

---

## 37. v1.16.0 — Web 前端 UX 审查修复 (2026-05-12)

Web 前端全面 UX 审查，修复 12 个问题（P0×4, P1×8）。

### 37.1 P0 严重问题修复

| # | 问题 | 修复 |
|---|------|------|
| P0-1 | 库存无详情页，无法查看关联订单 | Inventory.vue 添加详情弹窗 + linkedOrders 展示 |
| P0-2 | 发货弹窗 driverName/vehicleNo 无前端校验 | 添加必填校验 + `*` 标记 |
| P0-3 | 取消订单确认文案不够详细 | "不可撤销，库存将被释放" 提示 |
| P0-4 | UpdateOrderDto 类型缺 items 字段 | types/index.ts 补充 items?: OrderItemDto[] |

### 37.2 P1 体验优化修复

| # | 问题 | 修复 |
|---|------|------|
| P1-5 | 表格窄屏水平溢出 | 所有表格加 min-width + overflow-x |
| P1-8 | AI 识别结果仍显示检验员 | 移除 inspector 列 |
| P1-10 | 登录页主题与全局不联动 | 统一 localStorage key 为 'theme' |
| P1-12 | AI 删除确认用原生 confirm | 替换为 ElMessageBox.confirm |
| P1-13 | Dashboard 统计卡片不跳转 | 点击跳转对应页面 + cursor:pointer |
| P1-18 | 空状态无操作引导 | 加 empty-hint 引导文案 |
| P1-21 | 创建订单加载 1000 条库存 | 改为 50 条 + 远程搜索 debounce |
| P1-22 | 库存位置输入交互混乱 | 简化为 select 预设 + input 自定义 |

### 37.3 类型补充

```typescript
// types/index.ts
interface InventoryStock {
  linkedOrders?: LinkedOrder[]  // 新增
}
interface LinkedOrder {
  id: number; orderNo: string; status: string
  customerName?: string; totalWeight?: string | number; createdAt: string
}
interface UpdateOrderDto {
  items?: OrderItemDto[]  // 新增
}
```

### 37.4 修改文件清单

| 文件 | 修改内容 |
|------|----------|
| `frontend/src/types/index.ts` | InventoryStock +linkedOrders, LinkedOrder, UpdateOrderDto +items |
| `frontend/src/views/Inventory.vue` | 详情弹窗+关联订单+空状态引导+位置输入修复+表格min-width |
| `frontend/src/views/Orders.vue` | 发货必填校验+取消确认+空状态引导+远程搜索+表格min-width |
| `frontend/src/views/AI.vue` | 移除检验员列+ElMessageBox替换confirm |
| `frontend/src/views/Dashboard.vue` | 统计卡片点击跳转+cursor:pointer |
| `frontend/src/views/Login.vue` | 主题key统一为'theme' |
| `frontend/src/views/Customers.vue` | 表格min-width |



---

## 38. v1.17.0 — 双端搜索体验优化 + 空状态引导升级 (2026-05-13)

### 38.1 搜索体验优化

#### 后端 — 配货单关键词搜索

| 文件 | 变更 |
|------|------|
| `backend/distribution.controller.ts` | `getOrderList` 添加 `@Query('keyword')` 参数 |
| `backend/distribution.service.ts` | `getOrderList` 支持按 `orderNo` + `customerName` 模糊搜索 |

#### Web 前端 — 库存页

| 文件 | 变更 |
|------|------|
| `frontend/src/views/Inventory.vue` | 搜索框改为 300ms debounce 自动触发（不再仅回车） |
| 同上 | 新增搜索建议下拉框：focus 时显示最近搜索，输入时显示 API 匹配结果 |
| 同上 | 搜索关键词自动保存到 localStorage 历史（最多 10 条，LRU） |

#### Web 前端 — 配货单页

| 文件 | 变更 |
|------|------|
| `frontend/src/views/Orders.vue` | 工具栏新增关键词搜索输入框，300ms debounce 自动过滤 |
| `frontend/src/types/index.ts` | `OrderQuery` 添加 `keyword` 字段 |

#### 移动端

| 文件 | 变更 |
|------|------|
| `mobile/src/components/SearchBar.vue` | 输入 300ms debounce 自动搜索 + focus 时显示最近搜索下拉框 |
| `mobile/src/views/OrdersView.vue` | 新增 SearchBar 组件，支持关键词搜索配货单 |

#### 新建工具函数

| 文件 | 说明 |
|------|------|
| `frontend/src/utils/searchHistory.ts` | localStorage 搜索历史封装（get/add/clear） |
| `mobile/src/utils/searchHistory.ts` | 同上（移动端独立副本） |

### 38.2 空状态引导优化

| 页面 | 变更前 | 变更后 |
|------|--------|--------|
| Web 库存页 | 图标+文字+提示 | + `[+ 新增库存]` `[AI识别]` 操作按钮 |
| Web 配货单页 | 图标+文字+提示 | + `[+ 新增配货单]` 操作按钮 |
| Web 配货弹窗-库存选择 | 纯文字"暂无可用库存" | + 图标 + `[去添加库存]` 按钮 |
| 移动端库存列表 | 纯文字"暂无库存数据" | + SVG仓库图标 + "添加筛选条件或前往 AI 识别添加" |
| 移动端配货单页 | 纯文字"暂无配货单" | + SVG图标 + "点击右下角「+」创建第一笔订单" |
| 移动端首页 | 纯文字"暂无配货单" | + SVG图标 + "前往配货页面创建订单" |

### 38.3 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/src/distribution/distribution.controller.ts` | 编辑 |
| `backend/src/distribution/distribution.service.ts` | 编辑 |
| `frontend/src/utils/searchHistory.ts` | **新建** |
| `frontend/src/types/index.ts` | 编辑 |
| `frontend/src/views/Inventory.vue` | 编辑 |
| `frontend/src/views/Orders.vue` | 编辑 |
| `mobile/src/utils/searchHistory.ts` | **新建** |
| `mobile/src/components/SearchBar.vue` | 编辑 |
| `mobile/src/views/OrdersView.vue` | 编辑 |
| `mobile/src/components/InventoryTable.vue` | 编辑 |
| `mobile/src/views/HomeView.vue` | 编辑 |
| `开发日志.md` | 编辑 |


---

## v1.18.0 — 2026-05-13

### 修复：deleteOrder / batchDeleteOrders 不释放库存（P0）

| 问题 | 修复方式 |
|------|----------|
| `deleteOrder` 只设 `deletedAt`，不恢复库存 | 改为 `$transaction`：软删 + `inventoryStock.updateMany` 释放 `reserved`/`shipped` 状态的库存回 `available` |
| `batchDeleteOrders` 同上 + 无事务包裹 | 同上，改为事务 + 去重 stockId 批量释放 |

### 修复：Multer 文件上传目录不存在时崩溃（P0）

`distribution.controller.ts` 的 `diskStorage.destination` 回调中，两个分支做了同一件事：返回不存在的目录路径。修复为 `existsSync` 时自动 `mkdirSync(recursive: true)`。

### 修复：注册接口缺限流和输入校验（P1）

- `AuthController.register` 添加 `@Throttle({ default: { limit: 5, ttl: 60000 } })`
- `RegisterDto` / `LoginDto` 补充 `@MaxLength(50)`（username）和 `@MaxLength(100)`（password），防止超长字符串

### UX 优化

- **全站快捷键**: `/` 键聚焦搜索输入框（Inventory / Orders 页），再次按 ESC 取消聚焦
- **订单详情**: 单号旁添加 📋 复制按钮，点击将单号写入剪贴板 + Toast 提示
- **AI 识别导入**: 批量入库成功后 1.5 秒自动跳转至库存管理页，方便查看结果
- **Orders 页**: 接入 `onUnmounted` 清理键盘事件监听

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/src/distribution/distribution.service.ts` | 编辑（deleteOrder / batchDeleteOrders 事务重写） |
| `backend/src/distribution/distribution.controller.ts` | 编辑（Multer 目录自动创建） |
| `backend/src/auth/auth.controller.ts` | 编辑（register 限流） |
| `backend/src/auth/dto/auth.dto.ts` | 编辑（MaxLength 约束） |
| `frontend/src/views/Inventory.vue` | 编辑（键盘快捷键 /） |
| `frontend/src/views/Orders.vue` | 编辑（键盘快捷键 / + 复制单号按钮 + onUnmounted） |
| `frontend/src/views/AI.vue` | 编辑（导入后自动跳转 + useRouter） |
| `开发日志.md` | 编辑 |

### 测试重构（2026-05-13）

**问题**: 之前的单元测试仍然引用已被移除的 `confirmOrder` 方法，`shipOrder` 测试校验的是旧状态流（confirmed→shipping），与实际代码（draft→shipping）不符。`deleteOrder`/`batchDeleteOrders` 完全无测试覆盖。

**修改**:
- 删除 `confirmOrder` 测试块（方法已移除，创建后可直发货）
- `shipOrder` 测试改为校验 `draft → shipping` + driverName/vehicleNo 必填校验
- `cancelOrder` 测试新增 `shipping → cancelled` 路径
- **新增**: `deleteOrder` 测试（事务+库存释放验证）
- **新增**: `batchDeleteOrders` 测试（批量删除+混入拒绝）
- **新增**: `updateOrder(items)` 测试（旧库存释放+新库存锁定事务）
- **新增**: 边界条件测试（空列表、分页skip/take、limit上限100、page最小1）
- `setup.ts` 添加 `jest.mock('nanoid')` 解决 ESM 兼容问题

**结果**: 测试用例从 58 → 61 个，100% 通过 ✅

| 测试覆盖 | 之前 | 之后 |
|---------|------|------|
| 订单状态机 - createOrder | 1 | 3 |
| 订单状态机 - deleteOrder | 0 | 4 |
| 订单状态机 - batchDeleteOrders | 1 | 2 |
| 订单状态机 - updateOrder(items) | 0 | 1 |
| 客户 - updateCustomer | 0 | 2 |
| 客户 - getCustomerById | 0 | 2 |
| 边界条件 | 2 | 4 |
| AI 识别 | 2 | 2 |
| 总计 | 58 | 61 |

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/test/unit/distribution.service.spec.ts` | **重写**（470行新增，394行删除） |
| `backend/test/setup.ts` | 编辑（添加 nanoid mock） |
| `开发日志.md` | 编辑 |

---

## v1.19.0 — 生产部署 + 15 项上线后优化 (2026-05-13)

### 🚀 生产环境部署

| 项目 | 详情 |
|------|------|
| 服务器 | 腾讯云轻量应用服务器，Ubuntu 24.04，3.6GB RAM，IP: 62.234.92.126 |
| 技术栈 | Nginx (:80) + NestJS PM2 (:3002) + MySQL 8.0 |
| 部署文档 | `生产环境部署指南.md` (10章完整文档)、`APK构建指南.md` |

### 🐛 修复

| 问题 | 根因 | 修复 |
|------|------|------|
| 413 上传大文件 | Nginx 默认 1MB limit | `client_max_body_size 20m` |
| Qwen API Key 错误 | 配置了错误的 Key | 更新为正确 Key（见 .env） |
| 识别历史缩略图不显示 | 后端 `aiRecognize()` 识别后立即 `fs.unlink` 删除文件 | 移除 unlink，文件永久保留 |
| `existsSync/mkdirSync` 未导入 | distribution.controller.ts 缺少 fs 导入 | 补上 `import { existsSync, mkdirSync } from 'fs'` |

### ✨ 新增

| 功能 | 说明 |
|------|------|
| 配货单号新规则 | `PMK` + `YYYYMMDD` + `01` + 当日序号（如 `PMK202605130103`） |
| 识别历史自动清理 | 启动 + 每 24h 清理 7 天前的记录和图片文件 |
| 识别结果表格展示 | 历史详情弹窗中 JSON 原文 → 库存风格的表格 |
| 批量导入 Chip 选择器 | 规格/存放位置下拉框 → Chip 卡片选择器（点击切换） |

### 🎨 UI/UX 优化

| 优化项 | 说明 |
|------|------|
| 库存首页默认「可用」 | `status` 默认 `available`，已发货/已预留不显示 |
| 筛选项热更新 | 品级/类型/规格/日期/状态切换即刷新，无需点搜索 |
| 配货单工具栏吸顶 | `position: sticky; top: 60px` + 毛玻璃背景 |
| 弹窗标题吸顶 | 新建/编辑配货单弹窗 header/footer 固定，body 滚动 |
| 下拉箭头间距 | 隐藏原生 select 箭头，自定义 SVG 箭头 `right 10px center` |
| 库存筛选联动配货 | 新建配货单时，产品类型/规格自动过滤可用库存 |

### 🔧 数据修正

| 修正 | 说明 |
|------|------|
| `电积镍板` → `电积镍` | DB 存量 4 条迁移 + AI 提示词修正 + 前端兜底转换 |

### ⚙️ 配置调整

| 配置 | 旧值 | 新值 |
|------|------|------|
| API 限流（default） | 300/分钟 | 3000/分钟 |
| 登录限流 | 10/分钟 | 100/分钟 |
| 上传限流 | 20/分钟 | 200/分钟 |
| AI 识别限流 | 10/分钟 | 100/分钟 |
| Nginx body size | 1MB | 20MB |

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/src/distribution/distribution.service.ts` | 编辑（移除 unlink + 单号生成 + 历史清理） |
| `backend/src/distribution/distribution.controller.ts` | 编辑（导入 fs + 放宽限流） |
| `backend/src/app.module.ts` | 编辑（放宽全局限流） |
| `backend/src/common/services/qwen-ai.service.ts` | 编辑（AI提示词：电积镍板→电积镍） |
| `frontend/src/views/Inventory.vue` | 编辑（默认可用 + 热筛选 + 箭头间距） |
| `frontend/src/views/Orders.vue` | 编辑（吸顶工具栏+弹窗+filters+库存联动筛选+箭头） |
| `frontend/src/views/AI.vue` | 编辑（缩略图 + 表格 + Chip 选择器 + 箭头） |
| `/etc/nginx/sites-available/flatstore` | 编辑（413 fix） |
| `生产环境部署指南.md` | 新增 |
| `APK构建指南.md` | 新增 |

---

## v1.20.0 — 全项目健康审计 + 配货流程修复 (2026-05-19)

### 项目健康审计发现

| # | 类别 | 问题 | 严重度 |
|---|------|------|--------|
| 1 | TypeScript | 前端 `DistributionOrder.status` 缺少 `'shipping'`，导致状态比较 TS 报错 | P0 |
| 2 | TypeScript | `AI.vue` 存在未使用的 `useRouter` / `router` / `apiBaseUrl` / `formatJson`（TS6133） | P1 |
| 3 | TypeScript | `Inventory.vue` 未使用的 `LinkedOrder` import + `blurTimer` 变量（TS6133） | P1 |
| 4 | 业务逻辑 | PC 端删除按钮无状态限制，可删除任意状态订单（后端仅允许 shipped/cancelled） | P0 |
| 5 | 业务逻辑 | Mobile `OrderDetail` 缺少 `shipping` 状态展示 + "完成发运"/"删除"按钮 | P0 |
| 6 | 业务逻辑 | Mobile `OrdersView` 缺少"发货中"筛选项 | P1 |
| 7 | 数据安全 | 后端 `shipOrder` 更新操作未包裹事务，并发时存在数据不一致风险 | P1 |
| 8 | 测试 | 前端单元测试 `getOrderList` 断言缺少 AbortSignal 第二参数 | P1 |
| 9 | 测试 | 后端单元测试 `shipOrder` 未适配 `$transaction` mock | P1 |
| 10 | 数据安全 | 开发日志包含明文 QWEN_API_KEY | P0 |

### 修复内容

#### 1. 前端 TypeScript 类型修复

| 文件 | 修改 |
|------|------|
| `frontend/src/types/index.ts` | `DistributionOrder.status` 加入 `'shipping'` |
| `mobile/src/types/index.ts` | 同上；`ShipOrderDto` 的 `driverName`/`vehicleNo` 从 optional 改 required |
| `frontend/src/views/AI.vue` | 移除未使用的 `useRouter` / `router` / `apiBaseUrl` / `formatJson` |
| `frontend/src/views/Inventory.vue` | 移除未使用 `LinkedOrder` import；`blurTimer` 在 `selectSuggestion` 中增加 `clearTimeout` 引用 |

#### 2. 配货业务流程修复

| 文件 | 修改 |
|------|------|
| `frontend/src/views/Orders.vue` | 删除按钮加 `v-if="row.status === 'shipped' \|\| row.status === 'cancelled'"`；shipping 状态增加取消按钮 |
| `mobile/src/views/OrderDetail.vue` | 新增 `shipping` 状态展示（badge+文案）；时间线从 2 步扩展为 3 步（创建→发货→完成）；新增"完成发运"按钮（shipping→shipped）；新增"删除"按钮（shipped/cancelled） |
| `mobile/src/views/OrdersView.vue` | 新增 `{ label: '发货中', value: 'shipping' }` 筛选项 |

#### 3. 后端事务安全

| 文件 | 修改 |
|------|------|
| `backend/src/distribution/distribution.service.ts` | `shipOrder` 的 `update` 包裹 `$transaction`，确保原子性 |

#### 4. 测试适配

| 文件 | 修改 |
|------|------|
| `frontend/src/stores/__tests__/order.test.ts` | `getOrderList` 断言添加 `expect.any(AbortSignal)` 第二参数 |
| `frontend/src/stores/__tests__/inventory.test.ts` | 同上 |
| `backend/test/unit/distribution.service.spec.ts` | `shipOrder` 测试 mock `$transaction` 传入 tx 对象 |
| `backend/test/integration/distribution.controller.spec.ts` | 移除已删除的 `confirm` 测试；ship/cancel 测试状态从 `confirmed` 改为 `draft` |

#### 5. 开发日志安全修复

| 修改 | 说明 |
|------|------|
| 移除明文 API Key | QWEN_API_KEY 替换为"见 .env" |
| 编码修复 | UTF-8 双重编码乱码通过 git 历史恢复（v1.13.0 为基准重建） |

### 验证结果

| 项目 | 结果 |
|------|------|
| Backend 单元测试 | 61/61 通过 ✅ |
| Frontend 单元测试 | 29/29 通过 ✅ |
| Frontend vue-tsc | 0 errors ✅ |
| Mobile vue-tsc | 0 errors ✅ |

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/src/distribution/distribution.service.ts` | 编辑 |
| `backend/test/unit/distribution.service.spec.ts` | 编辑 |
| `backend/test/integration/distribution.controller.spec.ts` | 编辑 |
| `frontend/src/types/index.ts` | 编辑 |
| `frontend/src/views/AI.vue` | 编辑 |
| `frontend/src/views/Inventory.vue` | 编辑 |
| `frontend/src/views/Orders.vue` | 编辑 |
| `frontend/src/stores/__tests__/order.test.ts` | 编辑 |
| `frontend/src/stores/__tests__/inventory.test.ts` | 编辑 |
| `mobile/src/types/index.ts` | 编辑 |
| `mobile/src/views/OrderDetail.vue` | 编辑 |
| `mobile/src/views/OrdersView.vue` | 编辑 |
| `开发日志.md` | 编辑（安全修复 + 新增 v1.20.0 记录） |

---

## v1.21.0 — AI识别单位修正 + 数据校验 + 库存展示优化 (2026-05-19)

### 背景

发现库存中出现 14500000 kg 的异常数据（AI 误将千克读成吨后前端又 ×1000 导致），同时 AI 识别结果单位为吨但实际票据以千克为单位，双端识别结果偶有差异。

### 1. AI 识别单位统一为千克

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| AI Prompt | 净重单位：吨 | 净重单位：**千克** |
| 后端 normalizeResult | 直接使用 AI 返回值 | 超过 10t 的值自动 /1000 纠正（千克误读为吨） |
| Web 前端 AI 表头 | `净重(吨)` | `净重(kg)` |
| Web 前端 AI 显示 | `.toFixed(3)` | `.toFixed(1)` |
| 移动端 AI 显示 | `XX.XXXt` | `XX.Xkg` |
| Web 导入转换 | `netWeight * 1000` → kg | `netWeight` 直接使用（已是 kg） |
| 移动端导入转换 | `netWeight * 1000` → kg | `netWeight` 直接使用 |

### 2. AI 识别数据校验（crossValidate）

新增 `QwenAIService.crossValidate()` 方法，在后端识别后自动执行：

| 校验项 | 逻辑 |
|--------|------|
| 重量越界 | netWeight > 10t → 自动 /1000 修正 + 警告 |
| 重量无效 | netWeight ≤ 0 → 提示人工确认 |
| 片数异常 | pieceCount < 0 → 纠正为 0 |
| 品级格式 | 不在 9999/9997/9996/9950/9920/99 → 警告 |
| 批号格式 | 不匹配 XX-X-XXX → 警告 |
| 品级一致性 | 同一票据出现多个品级 → 警告 |

返回 `warnings` 数组，双端识别后展示警告信息。

### 3. 后端 DTO 上限校验

| 字段 | 修改 |
|------|------|
| `CreateInventoryDto.weight` | 新增 `@Max(10000)` |
| `CreateInventoryDto.pieceCount` | 新增 `@Max(1000)` |

### 4. Web 库存表格优化

| 修改 | 说明 |
|------|------|
| 新增包号列 | 批号后显示 `packageNo` |
| 新增合计行 | tfoot 显示当前页重量合计 + 片数合计 |

### 5. AI 识别结果合计

| 端 | 修改 |
|----|------|
| Web | 识别结果表添加 tfoot 合计行（重量 kg + 片数），头部计数增加重量/片数汇总 |
| Web | 历史详情表添加 tfoot 合计行 |
| 移动端 | 已有合计行，仅更新单位 |

### 6. 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/src/common/services/qwen-ai.service.ts` | 编辑（单位改千克 + normalizeResult 越界纠正 + 新增 crossValidate） |
| `backend/src/distribution/distribution.service.ts` | 编辑（aiRecognize 调用 crossValidate + 返回 warnings） |
| `backend/src/distribution/dto/inventory.dto.ts` | 编辑（weight @Max(10000) + pieceCount @Max(1000)） |
| `frontend/src/views/Inventory.vue` | 编辑（新增包号列 + 合计行） |
| `frontend/src/views/AI.vue` | 编辑（单位改 kg + 合计行 + warnings 展示 + 导入去掉 ×1000） |
| `mobile/src/views/AIView.vue` | 编辑（单位改 kg + 导入去掉 ×1000 + warnings 提示） |

## v1.22.0 — 单位统一 + 代码优化 + CI (2026-05-19)

### 背景

gitee 上提出全部使用 kg 单位，取消吨的使用；代码 review 发现若干可优化点。

### 1. 全面改用 kg 单位

- **crossValidate 重量校验**：去掉 kg→吨 的自动 /1000 转换，改为 > 10000kg 仅警告不修改
- **所有表头标签**：`重量(吨)` → `重量(kg)`，`净重(吨)` → `净重(kg)`
- **所有合计行单位**：`X 吨` → `X kg`
- 覆盖 Web 端（Inventory.vue / AI.vue / Dashboard.vue / Warehouse.vue / Orders.vue / Test.vue）
- 覆盖移动端（AiView.vue / OrdersView.vue / InventoryDetail.vue）

### 2. 代码优化

| 项目 | 修改 |
|------|------|
| Login 未授权重定向 | 401 时保留当前页面路径到 redirect 参数，登录后自动跳回 |
| API 层类型安全 | `as any` 替换为 `as AxiosRequestConfig`，DistributionOrder 补上缺失的 productType / specification 字段 |
| QwenAIServiceMock 补齐 | 新增 `crossValidate` mock 方法，确保现有测试不因新增方法而失败 |

### 3. 测试补充

- 新增 `backend/test/unit/qwen-ai.service.spec.ts`：跨 Validate 单元测试
  - 正常 kg 重量通过（500~5000kg）
  - 超重警告（> 10000kg）
  - 零/负值警告
  - 负片数自动纠正
  - 品级格式校验
  - 批号格式校验

### 4. CI/CD

- 新增 `.github/workflows/ci.yml`：GitHub Actions 自动构建
  - lint + type-check
  - 后端单元测试 + 集成测试（MySQL service）
  - 前端单元测试
  - 构建产物上传

### 5. 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/src/common/services/qwen-ai.service.ts` | 编辑（crossValidate 去掉 /1000 转换，阈值改为 10000kg） |
| `backend/src/common/__mocks__/mock-services.ts` | 编辑（补齐 crossValidate mock） |
| `backend/test/unit/qwen-ai.service.spec.ts` | **新增**（crossValidate 单元测试） |
| `.github/workflows/ci.yml` | **新增**（CI 自动化） |
| `frontend/src/api/request.ts` | 编辑（401 重定向带 redirect 参数） |
| `frontend/src/api/distribution.ts` | 编辑（替换 as any → as AxiosRequestConfig） |
| `frontend/src/types/index.ts` | 编辑（DistributionOrder 补字段） |
| `frontend/src/views/Login.vue` | 编辑（登录成功后跳转 redirect 参数） |
| `frontend/src/views/Inventory.vue` | 编辑（吨→kg） |
| `frontend/src/views/AI.vue` | 编辑（吨→kg） |
| `frontend/src/views/Orders.vue` | 编辑（吨→kg + 去除 as any） |
| `frontend/src/views/Dashboard.vue` | 编辑（吨→kg） |
| `frontend/src/views/Warehouse.vue` | 编辑（吨→kg） |
| `frontend/src/views/Test.vue` | 编辑（吨→kg） |
| `mobile/src/types/index.ts` | 编辑（DistributionOrder 补字段） |
| `mobile/src/views/AiView.vue` | 编辑（吨→kg） |
| `mobile/src/views/OrdersView.vue` | 编辑（吨→kg） |
| `mobile/src/views/InventoryDetail.vue` | 编辑（吨→kg） |

## v1.22.0 — 配货流程简化 + 库存已发出状态 + 双端单位统一 + 配货明细分组合计 (2026-05-19)

### 1. 配货流程简化（草稿→已发货）

取消三步流程（草稿→发货中→已发货），简化为两步（草稿→已发货）。

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| OrderStatus 枚举 | draft / shipping / shipped / cancelled | draft / shipped / cancelled |
| 发货接口 | POST /ship (需 driverName+vehicleNo) | POST /ship (无参数，直接发货) |
| 完成发运接口 | POST /deliver | 已移除 |
| 数据库字段 | driverName + vehicleNo | 已删除 |
| 发货弹窗 | 需填写司机/车牌 | 移除弹窗，确认即发货 |
| 时间线 | 3步（创建→发货→发运） | 2步（创建→已发货） |

### 2. 库存新增"已发出"状态

| 项目 | 修改 |
|------|------|
| Prisma StockStatus 枚举 | 新增 `issued` |
| 后端统计 | 新增 issued 计数 |
| Web 库存筛选 | 新增"已发出"选项 + tag-info 样式 |
| Web Dashboard | 饼图新增"已发出"扇区 |
| App 库存页 | 新增"已发出" StatCard + 状态分布条 |
| App 首页 | 新增"已发出"统计 + 分布条 |
| 双端类型定义 | Statistics.inventory + InventoryStock.status 补充 issued |

### 3. 双端单位统一（6处 t→kg）

| 文件 | 修改 |
|------|------|
| mobile/components/InventoryTable.vue | `}}t` → `}}kg` |
| mobile/components/InventoryCard.vue | 补充 `kg` 后缀 |
| mobile/components/OrderCard.vue | `+'t'` → `+'kg'`，精度统一 .toFixed(3) |
| mobile/views/OrderDetail.vue | 2处 `+'t'` → `+'kg'` |
| mobile/views/OrdersView.vue | `}}t` → `}}kg`，精度统一 .toFixed(3) |
| mobile/views/InventoryView.vue | `}}t` → `}}kg` |

### 4. 配货明细优化

| 项目 | 修改 |
|------|------|
| Ni% 显示 | 库存选择、配货明细编辑中移除镍含量标签 |
| 批号分组合计 | Web 详情弹窗 + App 详情页按批号分组，同一批号多条显示小计行 |
| 总合计 | 双端明细表末尾添加合计行 |
| 每页条数 | Web 配货单列表 limit 20→24 |

### 5. App端识别结果总计

| 项目 | 修改 |
|------|------|
| 历史详情表头 | 添加 `合计 XX.X kg / X块` |
| computed | 新增 detailTotalWeight / detailTotalPieces |

### 6. 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| backend/prisma/schema.prisma | 编辑（StockStatus+issued, OrderStatus去shipping, 删driverName/vehicleNo） |
| backend/src/distribution/distribution.service.ts | 编辑（shipOrder简化, deliverOrder移除, issued统计, shipping移除） |
| backend/src/distribution/distribution.controller.ts | 编辑（ship无body, deliver移除） |
| backend/src/distribution/dto/order.dto.ts | 编辑（ShipOrderDto清空） |
| backend/test/unit/distribution.service.spec.ts | 编辑（测试同步） |
| backend/test/unit/qwen-ai.service.spec.ts | 编辑（AiRecognizeResult类型修复） |
| backend/test/integration/distribution.controller.spec.ts | 编辑（测试同步） |
| frontend/src/types/index.ts | 编辑（status去shipping, 删driverName/vehicleNo/ShipOrderDto, +issued） |
| frontend/src/api/distribution.ts | 编辑（ship无参数, deliver移除, 去ShipOrderDto） |
| frontend/src/stores/order.ts | 编辑（ship无dto, deliver移除） |
| frontend/src/views/Orders.vue | 编辑（去司机列/弹窗, 去shipping, 每页24条, 去Ni%, 批号分组+合计） |
| frontend/src/views/Dashboard.vue | 编辑（饼图+issued, 柱状图去shipping） |
| frontend/src/views/Inventory.vue | 编辑（筛选+issued, 标签样式） |
| frontend/src/stores/__tests__/order.test.ts | 编辑（测试同步） |
| frontend/src/views/Test.vue | 编辑（shipOrder参数同步） |
| mobile/src/types/index.ts | 编辑（status去shipping, 删driverName/vehicleNo/ShipOrderDto, +issued） |
| mobile/src/api/distribution.ts | 编辑（ship无参数, deliver移除, 去ShipOrderDto） |
| mobile/src/stores/order.ts | 编辑（ship无dto, deliver移除） |
| mobile/src/components/InventoryTable.vue | 编辑（t→kg, +issued） |
| mobile/src/components/InventoryCard.vue | 编辑（+kg后缀, +issued） |
| mobile/src/components/OrderCard.vue | 编辑（t→kg, 去司机信息） |
| mobile/src/views/OrderDetail.vue | 编辑（去司机/车牌, 2步时间线, 批号分组+合计, 直接发货） |
| mobile/src/views/OrdersView.vue | 编辑（t→kg, 去shipping筛选） |
| mobile/src/views/InventoryView.vue | 编辑（t→kg, +issued StatCard） |
| mobile/src/views/InventoryDetail.vue | 编辑（+issued） |
| mobile/src/views/HomeView.vue | 编辑（+issued统计/分布条） |
| mobile/src/views/AiView.vue | 编辑（历史详情+合计行） |

### 7. 端到端测试（4轮）

#### 第1轮 — 发现问题

| 项目 | 结果 |
|------|------|
| 后端 tsc | 通过 |
| 后端 unit (65) | 通过 |
| 后端 integration (23) | **全部 401 Unauthorized** |
| 前端 tsc | 通过 |
| 前端 build | 通过 |
| 移动端 tsc | 通过 |
| 移动端 build | **失败 — AIView.vue 大小写不一致** |

**错误1：集成测试全部 401 Unauthorized**
- 原因：DistributionController 使用 `@UseGuards(JwtAuthGuard)` 全局守卫，但集成测试未绕过认证
- 修复：在 `Test.createTestingModule` 中 `.overrideGuard(JwtAuthGuard).useValue({ canActivate: () => true })`

**错误2：移动端 build 失败 TS1261**
- 原因：router 中 `@/views/AiView.vue` 与磁盘上实际文件名 `AIView.vue` 大小写不一致
- 修复：router import 改为 `@/views/AIView.vue`

**错误3：dist 目录残留 mock 文件**
- 原因：`backend/dist/common/__mocks__/mock-services.*` 残留，导致 jest duplicate manual mock 警告
- 修复：删除 dist 目录下残留的 mock 文件

#### 第2轮 — 修复后重测

| 项目 | 结果 |
|------|------|
| 后端 tsc | 通过 |
| 后端 unit (65) | 通过 |
| 后端 integration (23) | **2 failed** |

**错误4：批量创建库存 400 Bad Request**
- 原因：测试直接 `.send(items)` 发送数组，但 BatchCreateInventoryDto 期望 `{ items: [...] }`
- 修复：改为 `.send({ items })`

**错误5：发货接口 400 Bad Request**
- 原因：测试创建订单时库存 status 为默认 `available`，但 shipOrder 校验库存必须为 `reserved`
- 修复：测试改为创建 `status: 'reserved'` 的库存

#### 第3轮 — 回归验证

| 项目 | 结果 |
|------|------|
| 后端 tsc + unit (65) + integration (23) | 全部通过 |
| 前端 tsc + build | 全部通过 |
| 移动端 tsc + build | 全部通过 |

#### 第4轮 — 最终确认

| 项目 | 结果 |
|------|------|
| 后端 tsc + unit (65) + integration (23) | 全部通过 |
| 前端 tsc + build | 全部通过 |
| 移动端 tsc + build | 全部通过 |

**零错误，4轮端到端测试全部通过。**

---

### v1.22.1 — AI识别批号校验正则升级（2026-05-20）

**背景**：实际生产中镍板批号末尾可带一位字母后缀（J/s/t），如 `26-1-109J`、`26-3-045t`，原正则 `/^\d{2}-\d{1,2}-\d{3}$/` 会将这些合法批号误报为格式异常。

**修改内容**：

1. **crossValidate 正则**（`qwen-ai.service.ts:188`）
   - 旧：`/^\d{2}-\d{1,2}-\d{3}$/`
   - 新：`/^\d{2}-\d{1,2}-\d{3}[Jst]?$/`
   - 警告文案同步更新为"预期 XX-X-XXX 或 XX-X-XXX字母[J/s/t]"

2. **AI 提示词**（`qwen-ai.service.ts:26`）
   - 批号格式说明增加末尾字母规则和示例（`26-1-109J`、`26-3-045t`）

3. **AI 提示词返回格式**（`qwen-ai.service.ts:50`）
   - batchNo 示例增加 `"26-1-109J"`

**影响范围**：仅 `backend/src/common/services/qwen-ai.service.ts` 一文件三处

---

### v1.22.2 — Login.vue 缺失 useRoute 导致运行时报错（2026-05-21）

**问题**：登录页 `Login.vue` 使用 `route.query.redirect` 但未导入 `useRoute`，也未声明 `const route = useRoute()`，导致运行时报错 `route is not defined`。

**修复**：

1. `import { useRouter } from 'vue-router'` → `import { useRouter, useRoute } from 'vue-router'`
2. `const router = useRouter()` 后补 `const route = useRoute()`

**影响范围**：仅 `frontend/src/views/Login.vue` 一文件两处

---

### v1.23.0 — AI模型切换GLM-4.6V-Flash + 识别解析增强 + 配货分类统计 + 全项目UI移位修复（2026-05-22）

**1. AI视觉识别模型切换**

- 原模型：阿里云千问 qwen-vl-plus
- 新模型：智谱 GLM-4.6V-Flash
- API 端点：`https://open.bigmodel.cn/api/paas/v4/chat/completions`
- 环境变量：`QWEN_API_KEY` → `ZHIPU_API_KEY`
- `max_tokens` 上限测试：glm-4v-flash 上限 1024，glm-4.6v-flash 支持 4096

**2. 识别结果解析修复**

- **问题1**：GLM-4.6V-Flash 返回 JSON 用 `` ```json `` 代码块包裹，正则 `/[\s\S]*/` 匹配不到
  - 修复：`parseResponse` 开头增加去 markdown 包裹逻辑
- **问题2**：数据量较大时 AI 返回被 `max_tokens` 截断，JSON 不完整导致解析失败
  - 修复：新增 `tryFixTruncatedJSON` 方法，自动补全括号闭合；同时增加 `max_tokens: 4096` 避免截断
- **问题3**：`max_tokens` 超过模型上限（1024 for glm-4v-flash）导致 API 返回 1210 参数错误，被后端误当作"图片格式不支持"
  - 修复：模型改为 glm-4.6v-flash 后设置 `max_tokens: 4096`

**3. Prompt 精简**

- 原 prompt ~700 字符 → 精简至 ~350 字符
- 保留核心识别要求，减少 token 开销

**4. 批号末尾字母纠正**

- 批号格式 `XX-X-XXX字母`：J 保持大写，S/T 自动转小写
- 正则：`/([0-9])([ST])$/` → 转小写

**5. 品级选项规范化**

- 删除 9999、99 两个非标准品级
- 后端校验正则：`/^(9997|9996|9950|9920)$/`
- 前端品级下拉：从 `datalist` 改为 `select`，固定选项 9997/9996/9950/9920

**6. 识别历史导入库存**

- 历史详情弹窗 footer 新增"导入库存"按钮
- 新增 `handleImportFromHistory` 函数：解析历史 result → 填入 `recognizeResults` → 打开导入弹窗
- `handleImportSubmit` 补 `sourceImage` 字段关联源图片
- `v-if="parsedResults.length"` 守护，失败/空记录不显示按钮

**7. 库存页合计行 colspan 修复**

- 表头 11 列，合计行原 `colspan` 4+1+1+4=10，少一列导致右侧底色缺失
- 修复：改为 5+1+1+5=11

**8. 配货单分类统计**

- 配货弹窗底部按 产品类型 × 品级 × 规格 分类显示每类重量和片数
- 新增 `categorySummary` computed，底部保留合计行

**9. 仪表盘图表优化**

- 库存扇形图：删除"已发货"项，"已发出"用蓝色 `#5dacf5`
- 配货柱状图：标签"已发货"→"已发出"，颜色与扇形图同步
- 全部图表颜色降低饱和度

**10. 全项目 UI 移位/跳动修复**

**根因**：关键帧动画和 hover 效果使用 `translateY`/`translateX` 导致元素在入场和交互时发生位移，`transition: all` 导致非预期属性参与过渡动画

**关键帧动画修复**（移除所有 translateY/translateX，只保留 opacity）：

| 文件 | 动画 | 原位移 | 修复 |
|------|------|--------|------|
| `global.scss` | fadeIn | translateY(10px) | 纯淡入 |
| `global.scss` | page-enter/leave | translateY(±8px) | 纯淡入淡出 |
| `App.vue` | slideDown | translateY(-10px) | 纯淡入 |
| `AI.vue` | slideUp/slideDown | translateY(20px/-10px) | 纯淡入淡出 |
| `AI.vue` | fadeIn | translateX(-10px) | 纯淡入 |
| `Customers.vue` | slideUp | translateY(20px) | 纯淡入 |
| `Warehouse.vue` | slideUp | translateY(20px) | 纯淡入 |
| `Dashboard.vue` | slideUp | translateY(20px) | 纯淡入（已在 v1.23.0 前修复） |

**Hover 效果修复**（移除位移，改用 box-shadow/background 提供反馈）：

| 文件 | 元素 | 原效果 | 修复 |
|------|------|--------|------|
| `Customers.vue` | .action-btn | translateY(-1px) | 仅改背景色 |
| `Login.vue` | .login-btn | translateY(-1px) | 仅加 box-shadow |
| `AI.vue` | .history-item | translateX(4px) | 仅改背景色 |
| `AI.vue` | .preview-btn | scale(1.1) | 仅加 box-shadow |
| `Inventory.vue` | .linked-order-item | translateX(4px) | 仅改背景色 |
| `Orders.vue` | .stock-option | translateX(4px) | 仅改背景色 |
| `App.vue` | .nav-action-btn | scale(1.1) | 仅改背景色 |
| `Dashboard.vue` | .weight-item | translateY(-2px) | 移除 |
| `Dashboard.vue` | .order-badge | translateY(-4px) | 仅加 box-shadow |

**transition: all 修复**（改为显式属性列表）：

| 文件 | 元素 | 修复后 |
|------|------|--------|
| `AI.vue` | .upload-area | background, border-color |
| `AI.vue` | .preview-btn | background, box-shadow |
| `AI.vue` | .history-item | background, border-color |
| `Inventory.vue` | .linked-order-item | background, border-color |
| `Orders.vue` | .stock-option | background, border-color |
| `Customers.vue` | .action-btn | background, color |
| `Login.vue` | .login-btn | background, box-shadow |
| `App.vue` | .nav-action-btn | background, color |
| `Dashboard.vue` | .stat-glow | opacity, width, height |
| `Dashboard.vue` | .order-badge | background, box-shadow |

**影响范围**：8 个前端文件
