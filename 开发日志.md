# 平面库配货模块 — 开发日志

**最后更新：2026-04-17**

---

## 项目概览

- **开发日期**：2026-04-16 ~ 2026-04-17
- **前端**：Vue 3 (Composition API) + TypeScript + Vite 5 + Element Plus + Pinia + ECharts + Three.js
- **后端**：NestJS 10 + Prisma 6.19.3 + MySQL 8.0 + 阿里云千问 qwen-vl-plus
- **项目路径**：`D:\Flatstore\`
- **GitHub**：https://github.com/1062265448/New-repository

---

## 版本历史

| 版本 | 日期 | Commit | 说明 |
|------|------|--------|------|
| v1.0.0 | 2026-04-16 | `cdcc944` | 初始化平面库配货系统 |
| v1.2.0 | 2026-04-17 | `0daf9fa` | 前端UI重构 - 极简玻璃态设计系统 |
| v1.3.0 | 2026-04-17 | `12cfbff` | 前端UI更新 - 字段优化、Bug修复 |

---

## 一、项目结构

```
Flatstore/
├── frontend/                      # Vue 3 前端
│   ├── src/
│   │   ├── api/
│   │   │   ├── request.ts         # Axios 封装（响应拦截器已解包）
│   │   │   └── distribution.ts     # 业务接口
│   │   ├── stores/                 # Pinia Store
│   │   ├── views/                 # 页面组件
│   │   └── router/index.ts        # 路由配置
│   └── .env
│
├── backend/                       # NestJS 后端
│   ├── src/
│   │   ├── main.ts
│   │   ├── distribution/           # 配货模块
│   │   └── common/services/       # AI 服务
│   ├── prisma/schema.prisma
│   └── .env
│
├── docker-compose.yml             # MySQL 8.0
├── CLAUDE.md                      # 开发规范
└── 开发日志.md
```

---

## 二、技术栈

| 组件 | 技术 | 版本 |
|------|------|------|
| 后端框架 | NestJS | 10.x |
| ORM | Prisma | **6.19.3**（必须锁定） |
| 数据库 | MySQL | 8.0 |
| AI 识别 | 阿里云千问 | qwen-vl-plus |
| 前端框架 | Vue 3 | 3.x |
| 构建工具 | Vite | 5.x |
| UI 组件 | Element Plus | 最新 |
| 状态管理 | Pinia | 最新 |
| 图表 | ECharts | 最新 |
| 3D 渲染 | Three.js | 最新 |

---

## 三、数据库设计

### 3.1 实体关系

```
Customer (客户)
    ↓
DistributionOrder (配货单) ←→ DistributionOrderItem (配货单明细)
                                      ↓
                              InventoryStock (库存批次)

AiRecognitionHistory (AI 识别历史)
```

### 3.2 Prisma Schema

```prisma
model Customer {
  id        Int       @id @default(autoincrement())
  name      String    @db.VarChar(100)
  contact   String?   @db.VarChar(50)
  phone     String?   @db.VarChar(20)
  address   String?   @db.VarChar(200)
  remark    String?   @db.Text
  deletedAt DateTime?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model InventoryStock {
  id            Int       @id @default(autoincrement())
  batchNo       String    @db.VarChar(50)
  grade         String    @db.VarChar(20)        // 9997/9996/9950/9920
  specification String?   @db.VarChar(100)
  productType   String?   @db.VarChar(50)        // 电解镍/电积镍/不锈钢专用镍/电镀专用镍
  weight        Float
  pieceCount    Int
  status        String    @default("available")   // available/reserved/shipped
  location      String?   @db.VarChar(100)       // 三厂区/二厂区
  sourceType    String    @default("manual")
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}

model DistributionOrder {
  id           Int       @id @default(autoincrement())
  orderNo      String    @unique
  customerId   Int
  customerName String?
  productSpec  String?
  targetGrade  String?
  totalWeight  Float
  totalPieces  Int
  status       String    @default("draft")
  driverName   String?
  vehicleNo    String?
  shippedAt    DateTime?
  remark       String?
  deletedAt    DateTime?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
}

model DistributionOrderItem {
  id         Int      @id @default(autoincrement())
  orderId    Int
  stockId    Int
  weight     Float
  pieceCount Int
  createdAt  DateTime @default(now())
}

model AiRecognitionHistory {
  id           Int       @id @default(autoincrement())
  imageUrl     String?
  result       String?   @db.Text
  itemCount    Int       @default(0)
  status       String    @default("success")
  errorMessage String?   @db.Text
  batchNo      String?
  grade        String?
  date         DateTime?
  createdAt    DateTime  @default(now())
}
```

---

## 四、核心功能

### 4.1 库存管理

- CRUD 操作
- 批量导入
- 状态管理（available / reserved / shipped）
- 品级：9997、9996、9950、9920
- 产品类型：电解镍、电积镍、不锈钢专用镍、电镀专用镍
- 位置：三厂区、二厂区

### 4.2 配货单管理

状态机：
```
draft → confirmed → shipping → shipped
  ↓         ↓          ↓
cancelled  cancelled  cancelled
```

关键特性：
- 创建时自动锁定库存（reserved）
- 事务处理防止超卖
- 取消时自动释放库存

### 4.3 AI 图像识别

- 调用千问 VL Plus 识别票据
- 自动保存识别历史
- 支持批量导入库存

### 4.4 3D 仓库可视化

| Demo | 风格 | 适用场景 |
|------|------|----------|
| 工业写实 | 金属材质、实时阴影 | 生产监控 |
| 简约现代 | 低多边形、明亮配色 | Dashboard 集成 |
| 科幻未来 | 霓虹灯光、粒子效果 | 展厅大屏 |
| RTS 游戏 | 俯视等轴测、单位移动 | 指挥中心 |

---

## 五、API 接口

### 5.1 库存

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/distribution/inventory` | 分页查询（支持 keyword/grade/status/productType） |
| GET | `/distribution/inventory/:id` | 获取单条 |
| POST | `/distribution/inventory` | 创建 |
| POST | `/distribution/inventory/batch` | 批量创建 |
| PATCH | `/distribution/inventory/:id` | 更新 |
| DELETE | `/distribution/inventory/:id` | 删除（需无有效订单引用） |

### 5.2 配货单

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/distribution/orders` | 分页查询（支持 status/customerId） |
| GET | `/distribution/orders/:id` | 获取详情 |
| POST | `/distribution/orders` | 创建 |
| PATCH | `/distribution/orders/:id` | 更新 |
| POST | `/distribution/orders/:id/confirm` | 确认 |
| POST | `/distribution/orders/:id/ship` | 发货（填写司机、车牌） |
| POST | `/distribution/orders/:id/deliver` | 完成发运 |
| POST | `/distribution/orders/:id/cancel` | 取消（释放库存） |
| DELETE | `/distribution/orders/:id` | 删除（仅已完成/已取消） |
| POST | `/distribution/orders/batch-delete` | 批量删除 |

### 5.3 客户

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/distribution/customers` | 获取所有 |
| GET | `/distribution/customers/:id` | 获取详情（含最近订单） |
| POST | `/distribution/customers` | 创建 |
| PUT | `/distribution/customers/:id` | 更新 |
| DELETE | `/distribution/customers/:id` | 软删除 |

### 5.4 统计

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/distribution/statistics` | 汇总统计 |

### 5.5 AI 识别

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/distribution/inventory/ai-recognize` | 图像识别（multipart/form-data） |
| GET | `/distribution/recognition-history` | 识别历史（分页） |
| DELETE | `/distribution/recognition-history/:id` | 删除历史 |

---

## 六、启动命令

### 6.1 启动 MySQL

```bash
cd /d/Flatstore
docker-compose up -d
```

### 6.2 启动后端

```bash
cd backend
npm install
npx prisma generate
npx prisma db push
npm run start:dev
# API: http://localhost:3002
# Swagger: http://localhost:3002/api
```

### 6.3 启动前端

```bash
cd frontend
npm install
npm run dev
# http://localhost:5174
```

---

## 七、关键问题与修复

### 7.1 Prisma 版本锁定 🔴

```
package.json: "prisma": "6.19.3"
```
Prisma 7 不兼容当前 MySQL 适配器。

**经验**：升级 Prisma 前务必检查适配器兼容性。

---

### 7.2 Axios 响应解包

响应拦截器已解包 `response.data`，直接访问 `res.data`。

```typescript
const res = await getInventoryList(params)
inventoryList.value = res.data      // ✅ 正确
```

---

### 7.3 文件上传

- 字段名是 `file`
- 不手动设置 Content-Type
- 后端限制 10MB

---

### 7.4 AI 识别结果保存

识别结果自动保存到 `ai_recognition_history` 表，可通过前端查看和批量导入。

---

### 7.5 数据库字符集

连接字符串添加 `?charset=utf8mb4`：
```
DATABASE_URL="mysql://root:root123456@localhost:3306/flat_library?charset=utf8mb4"
```

---

## 八、UI 重构（2026-04-17）

### 8.1 设计系统

创建了极简玻璃态设计系统：

```scss
// 核心变量 (frontend/src/styles/variables.scss)
--color-primary: #0071e3;           // Apple Blue
--glass-bg: rgba(255, 255, 255, 0.72);
--glass-shadow: 0 8px 32px rgba(0, 0, 0, 0.08);
--radius-pill: 980px;               // 胶囊按钮
```

### 8.2 重构的页面

| 页面 | 主要特性 |
|------|---------|
| App.vue | 顶部导航栏、面包屑、Toast 系统 |
| Dashboard.vue | 统计卡片渐变动画、ECharts 主题切换 |
| Inventory.vue | 胶囊按钮、搜索栏、自定义表格 |
| Orders.vue | 库存可视化选择（卡片式）、状态标签 |
| Customers.vue | 卡片悬浮效果、弹窗动画 |
| AI.vue | 拖拽上传、历史卡片列表 |

---

## 九、Bug 修复经验 🔴

### 9.1 Toast 重复弹窗 🔴

**问题**：客户删除成功后弹出两次 toast

**原因**：
- Store 层调用了 `ElMessage.success()`
- 组件层也调用了 `showToast()`
- 双重提示

**修复**：
```typescript
// ❌ 错误：在 Store 中处理 UI 反馈
const deleteCustomer = async (id: number) => {
  await api.deleteCustomer(id)
  ElMessage.success('删除成功')  // 不要这样做
}

// ✅ 正确：Store 只负责数据操作
const deleteCustomer = async (id: number) => {
  await api.deleteCustomer(id)
  await fetchCustomers()
}
```

**经验**：统一 toast 提示来源，Store 层只负责数据操作，组件层负责 UI 反馈。

---

### 9.2 Vue 响应式变量缺失 🔴

**问题**：
```
[Vue warn]: Property "isDragOver" was accessed during render but is not defined on instance
```

**原因**：模板中使用了 `isDragOver`，但 `<script setup>` 中忘记定义

**修复**：
```typescript
const isDragOver = ref(false)  // 添加缺失的响应式变量
```

**经验**：
- 使用 TypeScript strict 模式可以提前发现此类问题
- 模板中使用的所有变量必须在 `<script setup>` 中声明

---

### 9.3 后端 DTO 验证问题 🔴

**问题**：
```bash
curl -X PATCH /distribution/inventory/40 \
  -d '{"location":"新位置"}'
# 返回 400: weight must be a number
```

**原因**：`UpdateInventoryDto` 中 `weight` 和 `pieceCount` 缺少 `@IsOptional()` 装饰器

**修复** (`backend/src/distribution/dto/inventory.dto.ts`)：
```typescript
@ApiPropertyOptional()
@IsOptional()      // ← 添加这个装饰器
@IsNumber()
@Min(0)
weight?: number;
```

**经验**：
- 更新 DTO 的可选字段必须添加 `@IsOptional()` 装饰器
- 创建 DTO 和更新 DTO 应分开定义，验证规则不同
- 未设置 `@IsOptional()` 的字段默认必填

---

### 9.4 分页参数边界问题

**问题**：分页参数可能出现负数或极大值，导致异常

**修复**：
```typescript
const page = Math.max(1, Number(params.page) || 1);
const limit = Math.min(100, Math.max(1, Number(params.limit) || 20));
```

**经验**：所有分页参数都应做边界校验，防止 SQL 异常或性能问题。

---

### 9.5 Prisma Where 类型不安全

**问题**：`where: any` 绕过类型检查

**修复**：
```typescript
const where: Prisma.InventoryStockWhereInput = {};
const where: Prisma.DistributionOrderWhereInput = { deletedAt: null };
const where: Prisma.AiRecognitionHistoryWhereInput = {};
```

**经验**：使用 Prisma 生成的类型，保持类型安全。

---

### 9.6 AI 失败时临时文件未清理

**问题**：AI 识别失败后，上传的临时文件残留在服务器

**修复**：
```typescript
try {
  aiResults = await this.qwenAI.recognizeImage(base64);
} catch (error: any) {
  // 清理临时文件
  try {
    await fs.promises.unlink(file.path);
  } catch (unlinkError) {
    console.error('[AI] 临时文件清理失败:', unlinkError);
  }
  throw error;
}
```

**经验**：文件上传接口必须处理失败时的清理逻辑，防止磁盘空间泄漏。

---

### 9.7 __dirname ESM 兼容问题

**问题**：Vite 构建时报错 `__dirname is not defined`

**原因**：ESM 模块中没有 `__dirname`

**修复**：
```typescript
import { dirname } from 'path';
import { fileURLToPath } from 'url';
const __dirname = dirname(fileURLToPath(import.meta.url));
```

**经验**：Vite 项目使用 ESM，涉及时必须用此方式处理。

---

### 9.8 vue-tsc 版本不兼容

**问题**：`npm run build` 失败，vue-tsc 版本与 TypeScript 不兼容

**修复**：直接使用 `npx vite build` 替代

**经验**：
- vue-tsc 和 TypeScript 版本需匹配
- 生产构建可直接用 Vite，内置类型检查足够

---

### 9.9 CSS/SCSS 解析错误

**问题**：构建失败，`expected "{"` 解析错误

**原因**：编辑后遗留了未匹配的 CSS 代码块

**经验**：
- 修改组件样式时注意代码块完整性
- 使用 IDE 格式化工具检查语法

---

### 9.10 订单号格式问题

**问题**：`ORD-${Date.now()}-${Math.random()...}` 格式冗长

**修复**：使用 nanoid 生成短码
```typescript
import { customAlphabet } from 'nanoid';
const nanoid = customAlphabet('1234567890ABCDEFGHIJKLMNOPQRSTUVWXYZ', 10);
const generateOrderNo = () => `ORD-${nanoid()}`;
```

**经验**：订单号应简短且唯一，避免暴露时间戳。

---

### 9.11 nanoid 模块未安装 🔴

**问题**：
```
src/distribution/distribution.service.ts:10:32 - error TS2307:
Cannot find module 'nanoid' or its corresponding type declarations.
```

**原因**：代码中引用了 nanoid，但未安装依赖包

**修复**：
```bash
cd backend
npm install nanoid
```

**经验**：
- 新增依赖后必须执行 `npm install`
- 依赖应添加到 `package.json` 的 `dependencies` 中
- 部署前确认所有依赖已安装

---

## 十、代码规范

### 10.1 Store 层职责
```typescript
// ✅ 正确
const createOrder = async (dto: CreateOrderDto) => {
  const res = await api.createOrder(dto)
  await fetchOrders()
  return res
}
```

### 10.2 组件层职责
```typescript
// 通过 inject 获取 toast 函数
const showToast = inject('showToast') as (message: string, type?: string) => void
```

### 10.3 API 层职责
```typescript
// request.ts - 统一错误处理
request.interceptors.response.use(
  (response) => response.data,
  (error) => {
    ElMessage.error(error.response?.data?.message || error.message)
    return Promise.reject(error)
  }
)
```

### 10.4 HTTP 方法规范
```typescript
// 创建资源 → POST
// 更新资源（全部） → PUT
// 更新资源（部分） → PATCH
// 删除资源 → DELETE
// 查询 → GET
```

---

## 十一、端到端测试结果

### 测试时间：2026-04-17

### 第 1 轮：基础 CRUD 测试

| 测试项 | 结果 |
|--------|------|
| 创建客户 | ✅ |
| 查询客户列表 | ✅ |
| 创建库存 | ✅ |
| 批量创建库存 | ✅ |
| 创建配货单 | ✅ |
| 库存自动锁定 | ✅ |
| 更新客户 | ✅ |
| 分页查询 | ✅ |
| 统计接口 | ✅ |

### 第 2 轮：配货单状态流转

| 测试项 | 结果 |
|--------|------|
| 草稿 → 确认 | ✅ |
| 确认 → 发货 | ✅ |
| 发货 → 完成 | ✅ |
| 状态机保护（已发货不可再确认） | ✅ |
| 取消订单 | ✅ |
| 取消后库存释放 | ✅ |

### 第 3 轮：边界条件测试

| 测试项 | 结果 |
|--------|------|
| 分页负数自动纠正 | ✅ |
| 分页过大自动限制 | ✅ |
| 查询不存在记录返回 404 | ✅ |
| 库存不可用时创建订单失败 | ✅ |
| 已发货库存不可修改 | ✅ |
| 预留库存不可修改 | ✅ |
| 被引用库存不可删除 | ✅ |
| 空搜索条件正常 | ✅ |
| 筛选条件正常 | ✅ |

### 第 4 轮：前端交互测试

| 测试项 | 结果 |
|--------|------|
| 首页加载 200 | ✅ |
| 库存页加载 200 | ✅ |
| 配货单页加载 200 | ✅ |
| 客户页加载 200 | ✅ |
| AI 页加载 200 | ✅ |
| CORS 配置正常 | ✅ |
| 前端静态资源加载 | ✅ |

### 第 5 轮：综合压力测试

| 测试项 | 结果 |
|--------|------|
| 快速创建 5 个客户 | ✅ |
| 批量创建 5 条库存 | ✅ |
| 创建多个配货单 | ✅ |
| 批量确认/发货/完成 | ✅ |
| 并发请求（10 并发） | ✅ |
| 数据一致性验证 | ✅ |

### 最终数据状态

- 库存：63 条（可用 58，预留 4，已发货 1）
- 配货单：7 条（草稿 3，已发货 4）
- 客户：8 条

---

## 十二、文件清单

### 前端样式

```
frontend/src/styles/
├── variables.scss        # 设计系统变量
└── global.scss           # 全局样式、动画
```

### 前端视图

```
frontend/src/views/
├── Dashboard.vue          # 仪表盘（统计卡片、ECharts）
├── Inventory.vue          # 库存管理（CRUD、批量）
├── Orders.vue             # 配货单（库存可视化选择）
├── Customers.vue          # 客户管理
├── AI.vue                 # AI 识别（拖拽上传）
├── Test.vue               # API 测试
├── DemoSelector.vue       # Demo 选择
├── Demo1_Industrial.vue    # 工业写实
├── Demo2_Modern.vue        # 简约现代
├── Demo3_Cyberpunk.vue     # 科幻未来
└── Demo4_RTS.vue          # RTS 游戏
```

### 前端 Store

```
frontend/src/stores/
├── theme.ts              # 主题管理（亮/暗切换）
├── statistics.ts         # 统计数据
├── inventory.ts          # 库存 API 封装
├── order.ts              # 配货单 API 封装
└── customer.ts           # 客户 API 封装
```

### 前端 API

```
frontend/src/api/
├── request.ts           # Axios 封装（响应解包）
└── distribution.ts     # 业务接口
```

### 后端核心文件

```
backend/src/
├── main.ts
├── distribution/
│   ├── distribution.controller.ts
│   ├── distribution.service.ts
│   └── dto/
│       ├── inventory.dto.ts
│       ├── order.dto.ts
│       └── customer.dto.ts
└── common/services/qwen-ai.service.ts
```

---

## 十三、后续优化

| 任务 | 优先级 | 状态 |
|------|--------|------|
| 骨架屏加载 | P1 | 待做 |
| 虚拟列表 | P1 | 待做 |
| 代码分割优化 | P2 | 待做 |
| JWT 认证 | P0 | 待做 |
| API 速率限制 | P0 | 待做 |
| 单元测试完善 | P1 | 待做 |
| 3D 数据对接 | P1 | 待做 |
| Docker 部署 | P2 | 待做 |

---

## 十四、错误经验总结

### 14.1 前端常见错误

| 错误 | 原因 | 解决方案 |
|------|------|----------|
| Property not defined | 模板使用了未声明的变量 | 在 `<script setup>` 中声明 |
| Toast 重复 | Store 和组件都调用了提示 | Store 只做数据操作 |
| 构建失败 | vue-tsc 版本不兼容 | 使用 `npx vite build` |
| CSS 解析错误 | 样式代码块不完整 | 使用 IDE 格式化检查 |

### 14.2 后端常见错误

| 错误 | 原因 | 解决方案 |
|------|------|----------|
| DTO 验证失败 | 缺少 `@IsOptional()` | 为更新字段添加装饰器 |
| 分页异常 | 参数无边界校验 | `Math.max/min` 限制 |
| Prisma 类型错误 | `where: any` | 使用类型化 Where 条件 |
| 文件残留 | 失败时未清理 | `fs.promises.unlink` |
| Cannot find module 'xxx' | 依赖未安装 | `npm install xxx` |

### 14.3 通用经验

1. **类型安全**：始终使用 TypeScript 类型，避免 `any`
2. **防御性编程**：对所有外部输入做校验
3. **事务处理**：涉及多表操作时使用事务
4. **资源清理**：文件上传必须处理失败清理
5. **日志记录**：关键操作添加日志便于排查

---

_最后更新：2026-04-17_
