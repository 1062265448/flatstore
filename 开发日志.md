# 平面库配货模块 — 开发日志

**最后更新：2026-04-21**

---

## 项目概览

- **开发日期**：2026-04-16 ~ 2026-04-21
- **前端**：Vue 3 (Composition API) + TypeScript + Vite 5 + Element Plus + Pinia + ECharts + Three.js
- **后端**：NestJS 10 + Prisma 6.19.3 + MySQL 8.0 + 阿里云千问 qwen-vl-plus
- **项目路径**：`D:\Flatstore\`
- **GitHub**：https://github.com/1062265448/New-repository

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0.0 | 2026-04-16 | 初始化平面库配货系统 |
| v1.2.0 | 2026-04-17 | 前端UI重构 - 极简玻璃态设计系统 |
| v1.3.0 | 2026-04-17 | 前端UI更新 - 字段优化、Bug修复 |
| v1.4.0 | 2026-04-17 | 3D仓库建模 - 平面库真实建筑结构 |
| v1.5.0 | 2026-04-20 | 生产就绪 - JWT认证、速率限制、骨架屏、Docker部署 |
| v1.5.1 | 2026-04-20 | Bug修复 - JwtService注入、图表空白、个人中心 |
| v1.5.2 | 2026-04-20 | UI优化 - 配货单库存信息完善、AI识别历史表格 |
| v1.5.3 | 2026-04-20 | UI优化 - AI识别批量导入增加下拉选项 |
| v1.5.4 | 2026-04-20 | Bug修复 - AI识别规格字段缺失 |
| v1.5.5 | 2026-04-20 | Bug修复 - 配货单productType/specification字段 |
| v1.5.6 | 2026-04-20 | 优化 - 简化配货单状态机，修复批量删除逻辑 |
| v1.6.0 | 2026-04-21 | 功能合并 - v1.5.7安全特性去除导出版本 |

---

## 一、项目结构

```
Flatstore/
├── frontend/                      # Vue 3 前端
│   ├── src/
│   │   ├── api/                  # API 接口
│   │   │   ├── request.ts        # Axios 封装（响应拦截器已解包）
│   │   │   └── distribution.ts   # 业务接口
│   │   ├── stores/               # Pinia 状态管理
│   │   ├── views/               # 页面组件
│   │   └── router/index.ts      # 路由配置
│   └── .env                      # VITE_API_BASE_URL=http://localhost:3002
│
├── backend/                       # NestJS 后端
│   ├── src/
│   │   ├── main.ts               # 入口（CORS/Swagger/静态文件）
│   │   ├── app.module.ts         # 根模块（Throttler/Auth）
│   │   ├── distribution/          # 配货模块
│   │   │   ├── distribution.controller.ts
│   │   │   ├── distribution.service.ts
│   │   │   └── dto/
│   │   ├── auth/                  # 认证模块（JWT）
│   │   │   ├── auth.controller.ts
│   │   │   ├── auth.service.ts
│   │   │   ├── auth.module.ts
│   │   │   ├── jwt.strategy.ts
│   │   │   └── guards/jwt-auth.guard.ts
│   │   └── common/services/       # AI 服务
│   ├── prisma/schema.prisma
│   └── .env
│
├── docker-compose.yml             # MySQL 8.0 配置
├── CLAUDE.md                      # 开发规范
└── 开发日志.md                    # 完整开发记录
```

---

## 二、技术栈

| 组件 | 技术 | 版本 |
|------|------|------|
| 后端框架 | NestJS | 10.x |
| ORM | Prisma | **6.19.3**（必须锁定） |
| 数据库 | MySQL | 8.0 |
| AI 识别 | 阿里云千问 | qwen-vl-plus |
| 前端框架 | Vue 3 | 3.x |
| 构建工具 | Vite | 5.x |
| UI 组件 | Element Plus | 最新 |
| 状态管理 | Pinia | 最新 |
| 图表 | ECharts | 最新 |
| 3D 渲染 | Three.js | 最新 |

---

## 三、数据库设计

### 3.1 实体关系

```
Customer (客户)
    ↓
DistributionOrder (配货单) ←→ DistributionOrderItem (配货单明细)
                                      ↓
                              InventoryStock (库存批次)

AiRecognitionHistory (AI 识别历史)
User (用户)
```

### 3.2 Prisma Schema

```prisma
model Customer {
  id        Int       @id @default(autoincrement())
  name      String    @db.VarChar(100)
  contact   String?   @db.VarChar(50)
  phone     String?   @db.VarChar(20)
  address   String?   @db.VarChar(200)
  remark    String?   @db.Text
  deletedAt DateTime?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model InventoryStock {
  id            Int       @id @default(autoincrement())
  batchNo       String    @db.VarChar(50)
  grade         String    @db.VarChar(20)        // 9997/9996/9950/9920
  specification String?   @db.VarChar(100)
  productType   String?   @db.VarChar(50)        // 电解镍/电积镍/不锈钢专用镍/电镀专用镍
  weight        Float
  pieceCount    Int
  status        String    @default("available")   // available/reserved/shipped
  location      String?   @db.VarChar(100)       // 三厂区/二厂区
  sourceType    String    @default("manual")
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}

model DistributionOrder {
  id           Int       @id @default(autoincrement())
  orderNo      String    @unique
  customerId   Int
  customerName String?
  productType  String?
  specification String?
  productSpec  String?
  targetGrade  String?
  totalWeight  Float
  totalPieces  Int
  status       String    @default("draft")       // draft/shipping/shipped/cancelled
  driverName   String?
  vehicleNo    String?
  shippedAt    DateTime?
  remark       String?
  deletedAt    DateTime?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
}

model DistributionOrderItem {
  id         Int      @id @default(autoincrement())
  orderId    Int
  stockId    Int
  weight     Float
  pieceCount Int
  createdAt  DateTime @default(now())
}

model AiRecognitionHistory {
  id           Int       @id @default(autoincrement())
  imageUrl     String?
  result       String?   @db.Text
  itemCount    Int       @default(0)
  status       String    @default("success")
  errorMessage String?   @db.Text
  batchNo      String?
  grade        String?
  date         DateTime?
  createdAt    DateTime  @default(now())
}

model User {
  id        Int      @id @default(autoincrement())
  username  String   @unique
  password  String   // bcrypt 加密
  role      String   @default("user")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
```

---

## 四、核心功能

### 4.1 库存管理

- CRUD 操作
- 批量导入
- 状态管理（available / reserved / shipped）
- 品级：9997、9996、9950、9920
- 产品类型：电解镍、电积镍、不锈钢专用镍、电镀专用镍
- 位置：三厂区、二厂区

### 4.2 配货单管理

**状态机（v1.5.6 简化版）：**
```
draft → shipping → shipped
  ↓         ↓
cancelled  (可删除)
```

**操作权限矩阵：**

| 操作 | draft | shipping | shipped | cancelled |
|------|-------|----------|---------|-----------|
| 编辑 | ✅ | ❌ | ❌ | ❌ |
| 发货 | ✅ | ❌ | ❌ | ❌ |
| 完成发运 | ❌ | ✅ | ❌ | ❌ |
| 取消 | ✅ | ✅ | ❌ | ❌ |
| 删除 | ❌ | ❌ | ✅ | ✅ |

关键特性：
- 创建时自动锁定库存（reserved）
- 事务处理防止超卖
- 取消时自动释放库存

### 4.3 客户管理

- CRUD 操作（软删除）
- 客户订单关联查询

### 4.4 AI 图像识别

- 调用千问 VL Plus 识别票据
- 自动保存识别历史
- 支持批量导入库存
- 用户可选择规格后批量导入

### 4.5 3D 仓库可视化

- 平面库真实建筑结构（120m × 66m）
- 仓储点网格显示
- 品级颜色区分
- 视角切换控制

### 4.6 认证与安全

- JWT 认证（7天有效期）
- 路由守卫（JwtAuthGuard）
- 全局限流（300次/分钟，登录10次/分钟）
- 统计缓存（TTL 30秒）

---

## 五、API 接口

### 5.1 认证

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/auth/login` | 用户登录 |
| POST | `/auth/register` | 用户注册 |
| GET | `/auth/profile` | 获取用户信息（需认证） |

### 5.2 库存

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/distribution/inventory` | 分页查询（支持 keyword/grade/status/productType） |
| GET | `/distribution/inventory/search` | 库存远程搜索（用于下拉选择） |
| GET | `/distribution/inventory/:id` | 获取单条 |
| POST | `/distribution/inventory` | 创建 |
| POST | `/distribution/inventory/batch` | 批量创建 |
| PATCH | `/distribution/inventory/:id` | 更新 |
| DELETE | `/distribution/inventory/:id` | 删除（需无有效订单引用） |
| POST | `/distribution/inventory/batch-delete` | 批量删除 |
| POST | `/distribution/inventory/ai-recognize` | AI 图像识别 |

### 5.3 配货单

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/distribution/orders` | 分页查询（支持 status/customerId/includeItems） |
| GET | `/distribution/orders/:id` | 获取详情 |
| POST | `/distribution/orders` | 创建（自动锁定库存） |
| PATCH | `/distribution/orders/:id` | 更新（仅 draft） |
| POST | `/distribution/orders/:id/ship` | 发货（填写司机、车牌，仅 draft） |
| POST | `/distribution/orders/:id/deliver` | 完成发运（仅 shipping） |
| POST | `/distribution/orders/:id/cancel` | 取消（释放库存） |
| DELETE | `/distribution/orders/:id` | 删除（仅 shipped/cancelled） |
| POST | `/distribution/orders/batch-delete` | 批量删除 |

### 5.4 客户

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/distribution/customers` | 获取所有 |
| GET | `/distribution/customers/:id` | 获取详情（含最近订单） |
| POST | `/distribution/customers` | 创建 |
| PUT | `/distribution/customers/:id` | 更新 |
| DELETE | `/distribution/customers/:id` | 软删除 |

### 5.5 统计

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/distribution/statistics` | 汇总统计 |
| GET | `/health` | 健康检查 |

### 5.6 AI 识别

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/distribution/inventory/ai-recognize` | 图像识别（multipart/form-data） |
| GET | `/distribution/recognition-history` | 识别历史（分页） |
| DELETE | `/distribution/recognition-history/:id` | 删除历史 |
| POST | `/distribution/recognition-history/batch-delete` | 批量删除历史 |

---

## 六、启动命令

### 6.1 启动 MySQL

```bash
cd /d/Flatstore
docker-compose up -d
```

### 6.2 启动后端

```bash
cd backend
npm install
npx prisma generate
npx prisma db push
npm run start:dev
# API: http://localhost:3002
# Swagger: http://localhost:3002/api
```

### 6.3 启动前端

```bash
cd frontend
npm install
npm run dev
# http://localhost:5174
```

---

## 七、关键问题与修复

### 7.1 Prisma 版本锁定 🔴

```
package.json: "prisma": "6.19.3"
```
Prisma 7 不兼容当前 MySQL 适配器。

**经验**：升级 Prisma 前务必检查适配器兼容性。

---

### 7.2 Axios 响应解包

响应拦截器已解包 `response.data`，直接访问 `res.data`。

```typescript
const res = await getInventoryList(params)
inventoryList.value = res.data      // ✅ 正确
```

---

### 7.3 文件上传

- 字段名是 `file`
- 不手动设置 Content-Type
- 后端限制 10MB

---

### 7.4 AI 识别结果保存

识别结果自动保存到 `ai_recognition_history` 表，可通过前端查看和批量导入。

---

### 7.5 数据库字符集

连接字符串添加 `?charset=utf8mb4`：
```
DATABASE_URL="mysql://root:root123456@localhost:3306/flat_library?charset=utf8mb4"
```

---

## 八、UI 设计系统（v1.2.0）

### 8.1 设计原则

创建了极简玻璃态设计系统，采用 Apple 风格配色。

### 8.2 核心变量

```scss
// frontend/src/styles/variables.scss
--color-primary: #0071e3;           // Apple Blue
--glass-bg: rgba(255, 255, 255, 0.72);
--glass-shadow: 0 8px 32px rgba(0, 0, 0, 0.08);
--radius-pill: 980px;               // 胶囊按钮
```

### 8.3 页面特性

| 页面 | 主要特性 |
|------|---------|
| Dashboard | 统计卡片渐变动画、ECharts 主题切换、骨架屏 |
| Inventory | 胶囊按钮、搜索栏、自定义表格 |
| Orders | 库存可视化选择（卡片式）、状态标签、优化编辑体验 |
| Customers | 卡片悬浮效果、弹窗动画 |
| AI | 拖拽上传、历史卡片列表 |
| Warehouse | 3D 仓库可视化 |
| Login | 简洁登录页 |
| Profile | 个人中心 |

---

## 九、Bug 修复经验 🔴

### 9.1 Toast 重复弹窗 🔴

**问题**：客户删除成功后弹出两次 toast

**原因**：
- Store 层调用了 `ElMessage.success()`
- 组件层也调用了 `showToast()`

**修复**：
```typescript
// ❌ 错误：在 Store 中处理 UI 反馈
const deleteCustomer = async (id: number) => {
  await api.deleteCustomer(id)
  ElMessage.success('删除成功')  // 不要这样做
}

// ✅ 正确：Store 只负责数据操作
const deleteCustomer = async (id: number) => {
  await api.deleteCustomer(id)
  await fetchCustomers()
}
```

**经验**：统一 toast 提示来源，Store 层只负责数据操作，组件层负责 UI 反馈。

---

### 9.2 Vue 响应式变量缺失 🔴

**问题**：
```
[Vue warn]: Property "isDragOver" was accessed during render but is not defined on instance
```

**原因**：模板中使用了 `isDragOver`，但 `<script setup>` 中忘记定义

**修复**：
```typescript
const isDragOver = ref(false)  // 添加缺失的响应式变量
```

**经验**：
- 使用 TypeScript strict 模式可以提前发现此类问题
- 模板中使用的所有变量必须在 `<script setup>` 中声明

---

### 9.3 后端 DTO 验证问题 🔴

**问题**：
```bash
curl -X PATCH /distribution/inventory/40 \
  -d '{"location":"新位置"}'
# 返回 400: weight must be a number
```

**原因**：`UpdateInventoryDto` 中 `weight` 和 `pieceCount` 缺少 `@IsOptional()` 装饰器

**修复** (`backend/src/distribution/dto/inventory.dto.ts`)：
```typescript
@ApiPropertyOptional()
@IsOptional()      // ← 添加这个装饰器
@IsNumber()
@Min(0)
weight?: number;
```

**经验**：
- 更新 DTO 的可选字段必须添加 `@IsOptional()` 装饰器
- 创建 DTO 和更新 DTO 应分开定义，验证规则不同

---

### 9.4 JwtService 注入错误 🔴

**问题**：
```
Nest could not find JwtService element (this provider does not exist in the current context)
```

**原因**：
在 `main.ts` 中尝试手动实例化 `AuthService` 并直接获取 `JwtService`。

**修复**：
1. 移除 `main.ts` 中的手动实例化代码
2. 在 `AuthService` 中实现 `OnModuleInit` 接口
3. 在 `onModuleInit` 中调用 `createDefaultAdmin()`

**经验**：NestJS 依赖注入应在模块初始化时处理。

---

### 9.5 图表显示空白 🔴

**问题**：首页 Dashboard 的 ECharts 图表显示空白。

**原因**：骨架屏加载完成后，数据虽然获取了，但图表在 DOM 未完全渲染时初始化。

**修复**：
```typescript
onMounted(async () => {
  loading.value = true
  await statisticsStore.fetchStatistics()
  loading.value = false
  await nextTick()  // 等待 DOM 渲染完成
  initCharts()
})
```

**经验**：响应式数据变化后操作 DOM（如图表初始化），必须使用 `nextTick()`。

---

### 9.6 个人中心无法打开 🔴

**问题**：点击导航栏用户下拉菜单中的"个人中心"无响应。

**原因**：缺少 `Profile.vue` 页面组件和路由配置。

**修复**：创建 `Profile.vue` 并配置路由。

---

### 9.7 nanoid 模块未安装 🔴

**问题**：
```
Cannot find module 'nanoid' or its corresponding type declarations.
```

**修复**：
```bash
cd backend
npm install nanoid
```

**经验**：
- 新增依赖后必须执行 `npm install`
- 部署前确认所有依赖已安装

---

## 十、代码规范

### 10.1 Store 层职责
```typescript
// ✅ 正确
const createOrder = async (dto: CreateOrderDto) => {
  const res = await api.createOrder(dto)
  await fetchOrders()
  return res
}
```

### 10.2 组件层职责
```typescript
// 通过 inject 获取 toast 函数
const showToast = inject('showToast') as (message: string, type?: string) => void
```

### 10.3 API 层职责
```typescript
// request.ts - 统一错误处理
request.interceptors.response.use(
  (response) => response.data,
  (error) => {
    ElMessage.error(error.response?.data?.message || error.message)
    return Promise.reject(error)
  }
)
```

---

## 十一、端到端测试结果

### 测试时间：2026-04-20（v1.5.6）

| 测试项 | 结果 |
|--------|------|
| 草稿订单直接发货 | ✅ |
| 批量删除校验（包含不可删除订单） | ✅ |
| 编辑配货单不加载库存 | ✅ |
| 编辑时库存选择区隐藏 | ✅ |
| 取消后库存释放 | ✅ |
| 已发货库存保护 | ✅ |

---

## 十二、3D 仓库可视化（v1.4.0）

### 12.1 建筑基础参数

| 参数 | 图纸值 | 3D 场景值 |
|------|--------|-----------|
| 建筑总长 X | 120,000 mm | 120 units |
| 建筑总宽 Z | 66,000 mm | 66 units |
| 柱距 X | 7,500 mm | 7.5 units |
| 柱距 Z | 6,000 mm | 6 units |
| 仓储点 | 1200×1200×1200 mm | 1.2×1.2×1.2 |

### 12.2 功能特性

1. **建筑结构** - 工业地面、仓储点网格、中央通道
2. **品级颜色** - 9997(绿)/9996(蓝)/9950(橙)/9920(紫)
3. **区域划分** - 6 个仓储区域
4. **视角切换** - 等轴测/俯视/正视
5. **控制面板** - 动画播放/暂停、相机重置

---

## 十三、生产就绪（v1.5.0）

### 13.1 JWT 认证

- Token 自动附加到请求头
- 401 响应自动跳转登录页
- 默认账户：`admin / admin123`

### 13.2 Docker 部署

```bash
# 开发环境
docker-compose up -d

# 生产环境
docker-compose -f docker-compose.prod.yml up -d
```

---

## 十四、后续优化

| 任务 | 优先级 | 状态 |
|------|--------|------|
| JWT 认证 | P0 | ✅ 已完成 |
| API 速率限制 | P0 | ✅ 已完成 |
| 骨架屏加载 | P1 | ✅ 已完成 |
| 统计缓存优化 | P1 | ✅ 已完成 |
| 库存远程搜索 | P1 | ✅ 已完成 |
| 导出库存 Excel | P1 | ❌ 待解决（Decimal序列化） |
| 虚拟列表 | P1 | 待做 |
| 单元测试完善 | P1 | 待做 |
| 3D 数据对接 | P1 | ✅ 已完成 |
| 代码分割优化 | P2 | 待做 |
| Docker 部署 | P2 | ✅ 已完成 |

---

## 十五、AI 识别规格修复（v1.5.4）

### 15.1 问题描述

用户在 AI 识别后，选择规格「50*50」进行批量导入，但查看识别历史详情时显示的是「整板」和「镍包」。

### 15.2 根本原因

1. **AI 接口缺少 specification 字段** - AI 随机生成规格值
2. **批量导入时表单重置不完整** - 遗漏了 productType 和 specification
3. **识别历史未记录用户选择的规格**

### 15.3 修复方案

1. 后端 AI 接口添加 specification 字段
2. 返回识别历史 ID
3. 批量导入时传递识别历史 ID
4. 导入后更新历史记录中的规格值

### 15.4 修复后的工作流程

```
1. 用户上传图片 → AI 识别 → 返回 { results, historyId }
2. 用户选择规格「50*50」→ 导入
3. 导入时自动将识别历史中的 specification 更新为「50*50」
4. 再次查看识别历史详情，显示正确的规格「50*50」
```

---

## 十六、配货单状态机优化（v1.5.6）

### 16.1 问题背景

1. **确认步骤多余** - `draft → confirmed` 只是空状态转换
2. **批量删除未校验** - 可能删除不可删除的订单而不报错
3. **编辑时冗余加载** - 编辑配货单时加载所有可用库存

### 16.2 优化方案

#### 2.1 简化状态机

**优化前：**
```
draft → confirmed → shipping → shipped
  ↓         ↓          ↓
cancelled  cancelled  cancelled
```

**优化后：**
```
draft → shipping → shipped
  ↓         ↓
cancelled  (可删除)
```

#### 2.2 批量删除逻辑修复

```typescript
// 优化前：静默跳过不可删除的订单
const deletableOrders = orders.filter(
  (o) => o.status === 'shipped' || o.status === 'cancelled',
);
// deletableOrders.length 可能小于 ids.length，但无报错

// 优化后：明确校验并报错
if (deletableOrders.length !== orders.length) {
  const undeletable = orders
    .filter((o) => o.status !== 'shipped' && o.status !== 'cancelled')
    .map((o) => o.orderNo);
  throw new BadRequestException(
    `以下订单不可删除：${undeletable.join(', ')}`,
  );
}
```

#### 2.3 编辑体验优化

| 场景 | 优化前 | 优化后 |
|------|--------|--------|
| 库存加载 | 加载所有可用库存（API 调用） | 使用订单已有库存信息 |
| 库存选择区 | 显示（但不可用） | 隐藏 |
| 明细删除按钮 | 显示 | 隐藏 |

### 16.3 修改的文件

**后端：**
```
backend/src/distribution/
├── distribution.service.ts    # 移除 confirmOrder，修复批量删除
└── distribution.controller.ts  # 移除 confirm 接口
```

**前端：**
```
frontend/src/
├── stores/order.ts            # 移除 confirmOrder
├── api/distribution.ts        # 移除 confirmOrder API
└── views/Orders.vue           # 移除确认按钮，优化编辑体验
```

---

## 十七、字段概念说明

### 17.1 库存与配货单字段对照

| 概念 | 库存字段 | 说明 | 配货单对应 |
|------|----------|------|-----------|
| **品级** | `grade` | 镍含量等级 | `targetGrade` |
| **产品类型** | `productType` | 生产工艺/用途 | `productType` |
| **产品规格** | `specification` | 物理尺寸/形态 | `specification` |

### 17.2 固定选项

**品级（从库存提取）**：
- 9997（最高）
- 9996
- 9950
- 9920

**产品类型（固定选项）**：
- 电解镍
- 电积镍
- 不锈钢专用镍
- 电镀专用镍

**产品规格（固定选项）**：
- 整板
- 镍条
- 100*100
- 50*50
- 25*25
- 20*20
- 镍包

---

## 十八、端到端测试命令参考

### 18.1 JWT 认证测试

```bash
# 1. 登录获取 Token
TOKEN=$(curl -s -X POST http://localhost:3002/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' \
  | grep -o '"access_token":"[^"]*"' | cut -d'"' -f4)

# 2. 携带 Token 访问
curl http://localhost:3002/distribution/statistics \
  -H "Authorization: Bearer $TOKEN"
```

### 18.2 配货单状态机测试

```bash
# 创建库存
curl -X POST http://localhost:3002/distribution/inventory \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"batchNo":"TEST-001","grade":"9997","weight":100,"pieceCount":10}'

# 创建订单 -> 发货 -> 完成
curl -X POST http://localhost:3002/distribution/orders/:id/ship \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"driverName":"司机","vehicleNo":"京A12345"}'
curl -X POST http://localhost:3002/distribution/orders/:id/deliver -H "Authorization: Bearer $TOKEN"
```

---

_最后更新：2026-04-20_

---

## 二十三、功能合并记录（2026-04-21）

### 23.1 合并说明

基于 v1.5.7 功能，合并到 v1.5.6 基础代码，**去除导出功能**。

### 23.2 合并的后端功能

| 功能 | 说明 | 文件 |
|------|------|------|
| JWT 认证 | 用户注册/登录/JWT 策略 | `auth/*.ts` |
| 全局限流 | 300次/分钟，登录10次/分钟 | `app.module.ts`, `throttler.guard.ts` |
| 统计缓存 | TTL 30秒，操作后清除 | `distribution.service.ts` |
| 客户存在性校验 | 创建订单前校验客户 | `distribution.service.ts` |
| 库存远程搜索 | el-select 下拉选择 | `distribution.controller.ts`, `distribution.service.ts` |
| 订单列表优化 | includeItems 参数，默认不加载明细 | `distribution.controller.ts`, `distribution.service.ts` |
| transformOptions | enableImplicitConversion | `main.ts` |
| User 模型 | Prisma schema 添加 | `schema.prisma` |

### 23.3 合并的前端功能

| 功能 | 说明 | 文件 |
|------|------|------|
| JWT Token 自动附加 | 请求拦截器添加 | `api/request.ts` |
| 401 自动跳转登录 | 响应拦截器处理 | `api/request.ts` |
| 认证 API | login/register/getProfile | `api/auth.ts` |
| 订单类型修复 | 移除 confirmed | `types/index.ts` |
| 库存搜索 API | searchInventory | `api/distribution.ts` |
| Orders.vue 修复 | h4 标签闭合 | `views/Orders.vue` |

### 23.4 未合并的功能

| 功能 | 原因 |
|------|------|
| 导出库存 Excel | Decimal 序列化问题待解决 |

### 23.5 新增文件

```
backend/src/auth/
├── dto/auth.dto.ts
├── auth.controller.ts
├── auth.service.ts
├── auth.module.ts
├── jwt.strategy.ts
└── guards/jwt-auth.guard.ts

backend/src/common/guards/throttler.guard.ts
backend/.env.example
frontend/src/api/auth.ts
```

### 23.6 测试结果

| 测试项 | 结果 |
|--------|------|
| 后端 TypeScript 编译 | ✅ 通过 |
| 前端 Vite 构建 | ✅ 通过 |

### 23.7 启动命令

```bash
# 1. 更新 Prisma schema
cd backend
npx prisma generate
npx prisma db push

# 2. 启动后端
npm run start:dev

# 3. 启动前端
cd frontend
npm run dev
```

### 23.8 默认账户

- 用户名: `admin`
- 密码: `admin123`

---

## 二十四、端到端测试结果（2026-04-21）

### 24.1 测试概述

| 轮次 | 测试项 | 通过 | 失败 | 成功率 |
|------|--------|------|------|--------|
| 第1轮 | 16 | 16 | 0 | 100% |
| 第2轮 | 16 | 16 | 0 | 100% |
| 第3轮 | 16 | 16 | 0 | 100% |
| 第4轮 | 16 | 16 | 0 | 100% |
| **总计** | **64** | **64** | **0** | **100%** |

### 24.2 测试模块覆盖

| 模块 | 测试项 | 状态 |
|------|--------|------|
| 🔐 认证 | 登录、获取用户信息、注册 | ✅ |
| 📊 统计 | 全局统计概览 | ✅ |
| 📦 库存 | 创建、分页查询、搜索、更新 | ✅ |
| 👥 客户 | 创建、查询、更新 | ✅ |
| 📋 配货单 | 创建、分页查询、发货、完成发运 | ✅ |
| 🤖 AI识别 | 查询识别历史 | ✅ |
| 🏥 健康检查 | 服务健康状态 | ✅ |

### 24.3 测试修复

**E2E 测试脚本问题修复**：
- **问题**：响应拦截器直接返回 `response`，导致无法获取 `access_token`
- **修复**：响应拦截器应返回 `response.data`
- **文件**：`backend/test-e2e.js`

```javascript
// 修复前
request.interceptors.response.use((response) => {
  return response;  // ❌ 返回整个响应对象
});

// 修复后
request.interceptors.response.use((response) => {
  return response.data;  // ✅ 解包 data
});
```

---

_最后更新：2026-04-21 E2E测试100%通过_
