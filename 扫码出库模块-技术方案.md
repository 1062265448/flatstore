# 扫码记录模块 — 技术方案

**编制日期**: 2026-05-11
**修订日期**: 2026-05-11
**目标**: 扫码枪扫描镍板条形码 → 自动解析 → 显示信息 → 记录扫码日志

---

## 一、需求分析

### 1.1 核心流程

```
扫码枪输入 25 位数字 → 解析为镍板信息 → 屏幕实时显示 → 记录扫码日志 → 统计计数
```

### 1.2 设计原则

- **扫码模块独立运行**，不修改 `inventory_stock` 表状态
- 操作员根据扫码清单在**配货模块人工核对并处理出库**
- 模块职责：解析条形码 + 记录扫码日志 + 提供统计汇总

### 1.3 关键约束

| 约束 | 说明 |
|------|------|
| **扫码枪** | HID 设备，模拟键盘输入，以 Enter 结尾 |
| **条形码** | 25 位纯数字（统一行业编码，依据标码文档） |
| **操作节奏** | 现场人员连续扫码，需要快速响应 |
| **扫码枪数量** | 当前 1 把，后续可能增加（`barcode @unique` 约束天然支持幂等） |

---

## 二、条形码解析规则

> 依据：标码文档《统一行业编码》

### 2.1 25 位编码结构

```
N1N2N3 N4N5 N6N7 N8N9N10N11N12N13 N14N15N16N17N18N19N20 N21N22N23N24N25
│      │    │    │                   │                      │
│      │    │    │                   │                      └─ 捆净重×10 (5位)
│      │    │    │                   └─ 七位生产序号代码
│      │    │    └─ 生产日期 YYMMDD (6位)
│      │    └─ 品级代码 (2位)
│      └─ 产品类别代码 (2位)
└─ 企业代码 (3位)
```

### 2.2 七位生产序号代码详解

```
①②③④⑤⑥⑦
│ │││ │││
│ │││ └── 包号编码（3位，需转换）
│ │└──── 批号后三位数字（不含 D、J、s、t）
│ └───── 车间代码（1-7）
└─────── （见下方映射表）
```

### 2.3 车间代码映射

| 代码 | 车间 | 产品类型 |
|------|------|---------|
| 1 | 电解一车间 | 电解镍 |
| 2 | 电解二车间 | 电解镍 |
| 3 | 电解三车间 | 电解镍 |
| 4 | 电积一车间（128槽）| 电解镍 |
| 5 | 电解三车间电积镍 | 电积镍（停产）|
| 6 | 电积一车间 | 电积镍 |
| 7 | 电积二车间 | 电积镍 |

### 2.4 包号编码转换

| 编码范围 | 含义 | 实际包号 | 批号后缀 |
|---------|------|---------|---------|
| 000-200 | 正常包号 | `编码值`（无后缀）| 无 |
| 201-400 | 一期机组 | `编码-200` | +J |
| 401-600 | 二期机组 | `编码-400` | +J |
| 601-800 | 三期机组 | `编码-600` | +J |

> **依据**: 标码文档统一行业编码规则，所有机组范围批号后缀均为 J。
> 注：②③④ 代表批号后三位（不含 D、J、s、t），J 作为后缀单独拼接。

### 2.5 批号拼接规则

```
批号 = "YY" + "-" + 车间代码 + "-" + ②③④ + (包号编码在 200-800 范围 ? "J" : "")
```

### 2.6 解析示例

**条形码**: `109 06 02 260419 1109047 14820`

| 段 | 值 | 解析 |
|----|----|------|
| 企业代码 | 109 | 金川集团 |
| 产品类别 | 06 | 电解镍 |
| 品级代码 | 02 | Ni9996 |
| 生产日期 | 260419 | 2026-04-19 |
| 生产序号 | 1109047 | 车间1(电解一车间)，批号后三位109，包号编码047(0-200范围，正常包号)→包号47，批号无后缀 |
| 捆净重 | 14820 | 1482.0 kg |

**解析结果**:
- 批号: `26-1-109`（包号编码 047 在 0-200 范围，无 J 后缀）
- 包号: `47`
- 品级: `9996`
- 产品类型: `电解镍`
- 净重: `1482.0 kg`
- 车间: `电解一车间电解镍`
- 生产日期: `2026-04-19`

---

## 三、技术架构

### 3.1 新增模块

```
backend/src/
├── scanning/                          # 新增：扫码模块
│   ├── scanning.controller.ts         # API 端点
│   ├── scanning.service.ts            # 解析 + 记录业务逻辑
│   ├── scanning.module.ts             # 模块注册
│   └── dto/
│       ├── parse-barcode.dto.ts       # 解析请求 DTO
│       └── scan-record.dto.ts         # 扫码记录 DTO
│
frontend/src/
├── views/
│   └── ScanOutbound.vue               # 扫码记录页面
├── api/
│   └── scanning.ts                    # 扫码 API
└── stores/
    └── scanning.ts                    # 扫码状态管理
```

### 3.2 新增数据库表

```prisma
// 扫码记录表
model ScanRecord {
  id           Int       @id @default(autoincrement())
  barcode      String    @unique @db.VarChar(25)   // 25位条形码
  batchNo      String    @db.VarChar(50)           // 解析后的批号
  packNo       String    @db.VarChar(50)           // 解析后的包号
  grade        String    @db.VarChar(20)           // 品级
  productType  String    @db.VarChar(50)           // 产品类型
  workshop     String    @db.VarChar(50)           // 车间名称
  netWeight    Decimal    @db.Decimal(10, 3)        // 净重(kg)
  productionDate DateTime?                          // 生产日期
  status       String    @default("parsed") @db.VarChar(20)
  // status: parsed / duplicate / error
  errorMessage String?   @db.Text
  scannedAt    DateTime  @default(now()) @map("scanned_at")

  @@map("scan_record")
  @@index([scannedAt(sort: Desc)])
  @@index([status])
  @@index([batchNo])
}
```

> **status 含义**：
> - `parsed` — 解析成功，已记录
> - `duplicate` — 重复扫码（同一条形码已存在）
> - `error` — 解析失败（条形码格式错误等）
>
> **注意**：不包含 `not_found` 状态。本模块仅做解析记录，不查询 `inventory_stock`。

### 3.3 API 设计

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/scanning/parse` | 解析 25 位条形码，返回解析结果（不入库） |
| POST | `/scanning/record` | 扫码 + 解析 + 记录（一键式） |
| GET | `/scanning/records` | 获取扫码记录（分页） |
| GET | `/scanning/today-summary` | 今日扫码统计汇总 |
| DELETE | `/scanning/records/:id` | 删除单条记录 |

### 3.4 与配货模块的关系

```
扫码记录模块                          配货模块
┌─────────────────┐                ┌─────────────────┐
│ 扫码 → 解析     │                │ 库存管理         │
│ → 记录日志      │  人工核对      │ 配货单管理       │
│ → 统计汇总      │ ──────────→   │ 出库操作         │
└─────────────────┘                └─────────────────┘
```

- 两个模块**完全独立**，不共享数据
- 操作员查看扫码记录清单，在配货模块中手动处理出库
- 后续如需打通，可在扫码记录中增加 `distributionOrderId` 字段

---

## 四、前端页面设计

### 4.1 页面布局

```
┌─────────────────────────────────────────────────────────┐
│  📷 扫码记录                                          [退出] │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────┐                │
│  │  扫码输入框（自动聚焦）              │                │
│  │  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │                │
│  │                                     │                │
│  └─────────────────────────────────────┘                │
│                                                         │
│  ┌──────────────┐  ┌─────────────────────────────────┐ │
│  │  今日统计     │  │  最新扫码结果                    │ │
│  │              │  │                                 │ │
│  │  今日已扫:   │  │  ┌─────────────────────┐       │ │
│  │  128 件       │  │  │  批号: 26-1-109     │       │ │
│  │  今日净重:   │  │  │  包号: 47            │       │ │
│  │  156.780 t   │  │  │  品级: 9996          │       │ │
│  │  成功率: 98% │  │  │  类型: 电解镍        │       │ │
│  │              │  │  │  车间: 电解一车间     │       │ │
│  └──────────────┘  │  │  净重: 1482.0 kg       │       │ │
│                    │  │  ✅ 已记录             │       │ │
│                    │  └─────────────────────┘       │ │
│                    │                                 │ │
│                    │  ┌─────────────────────┐       │ │
│                    │  │  最近扫码记录        │       │ │
│                    │  │  14:32 26-1-109 #47  │       │ │
│                    │  │  14:31 26-7-111J #1  │       │ │
│                    │  │  14:30 26-3-045J #12 │       │ │
│                    │  │  ...                 │       │ │
│                    │  └─────────────────────┘       │ │
│                    └─────────────────────────────────┘ │
│                                                         │
│  [导出今日记录]  [删除记录]  [手动输入]                  │
└─────────────────────────────────────────────────────────┘
```

### 4.2 交互设计

| 操作 | 效果 |
|------|------|
| **扫码枪扫描** | 25 位数字 + Enter → 自动提交 → 1 秒内显示结果 → 自动清空输入框 → 准备下一次扫描 |
| **解析成功** | 绿色提示 + 显示镍板信息 + 右侧列表追加记录 |
| **重复扫码** | 黄色警告 + 显示"该条形码已扫过" |
| **解析失败** | 红色警告 + 显示解析失败原因 |
| **手动输入** | 支持手动输入条形码（扫码枪故障时应急） |

### 4.3 自动聚焦策略

```
页面加载 → 聚焦输入框
扫码提交后 → 自动清空 + 重新聚焦
点击页面空白区域 → 重新聚焦
弹窗关闭后 → 重新聚焦
```

---

## 五、后端业务逻辑

### 5.1 条形码解析流程

```
输入: 25位数字字符串
  ↓
1. 长度校验（必须25位纯数字）
  ↓  不通过 → 返回 error，记录到 scan_record
2. 字段分割
   - N1N2N3: 企业代码（校验是否为 109）
   - N4N5: 产品类别代码
   - N6N7: 品级代码 → 映射为 grade（如 "02" → "9996"）
   - N8-N13: 生产日期 → 20YY-MM-DD
   - N14-N20: 七位生产序号
     - ① 车间代码 → 车间名称 + 产品类型
     - ②③④ 批号后三位 → 拼出批号
     - ⑤⑥⑦ 包号编码 → 转换规则 → 包号 + 批号后缀
   - N21-N25: 捆净重 → ÷10 → 实际净重(kg)
  ↓
3. 去重检查
   - 查询 scan_record 表 barcode 是否已存在
   - 已存在 → 返回 duplicate，不重复插入
  ↓
4. 记录保存
   - 写入 scan_record 表，status = "parsed"
  ↓
5. 返回解析结果
```

### 5.2 品级代码映射

| 品级代码 | 对应 grade | 说明 |
|---------|-----------|------|
| 02 | 9996 | Ni9996 |
| （待确认）| 9997 | 需要实际数据确认 |
| （待确认）| 9950 | 需要实际数据确认 |
| （待确认）| 9920 | 需要实际数据确认 |

> 未知品级代码时，grade 字段记录原始代码值（如 "99"），不阻断扫码流程。

---

## 六、品级代码映射确认

⚠️ **需要现场确认**：条形码中的 N6N7（品级代码）与实际品级（9997/9996/9950/9920）的对应关系。

根据现有规则文档和标码信息，已知：
- 电解镍(Ni 9996) / 电积镍(Ni 9996) → 牌号固定为 Ni9996
- 但实际库存中有 9997/9996/9950/9920 四种品级

**建议**：先按 `02 → 9996` 实现，后续根据实际扫码数据补充完整映射表。

---

## 七、数据库变更

### 7.1 新增表

```prisma
model ScanRecord {
  id             Int       @id @default(autoincrement())
  barcode        String    @unique @db.VarChar(25)
  batchNo        String    @db.VarChar(50)
  packNo         String    @db.VarChar(50)
  grade          String    @db.VarChar(20)
  productType    String    @db.VarChar(50)
  workshop       String    @db.VarChar(50)
  netWeight      Decimal   @db.Decimal(10, 3)
  productionDate DateTime?
  status         String    @default("parsed") @db.VarChar(20)
  errorMessage   String?   @db.Text
  scannedAt      DateTime  @default(now()) @map("scanned_at")

  @@map("scan_record")
  @@index([scannedAt(sort: Desc)])
  @@index([status])
  @@index([batchNo])
}
```

### 7.2 现有表影响

**无**。本模块不修改 `inventory_stock`、`distribution_order` 等现有表。

---

## 八、实施步骤

| 阶段 | 内容 | 预计时间 |
|------|------|---------|
| **1. 数据库** | 新增 `ScanRecord` 模型，`prisma db push` | 10 min |
| **2. 后端核心** | 条形码解析服务、扫码控制器、记录逻辑 | 1-2 h |
| **3. 前端页面** | ScanOutbound.vue（扫码界面 + 实时统计） | 2-3 h |
| **4. 联调测试** | 模拟扫码 + 数据验证 + 异常处理 | 1 h |
| **5. 品级映射** | 根据实际扫码数据确认 N6N7 映射关系 | 待定 |
| **6. 部署上线** | 生产环境测试 | 30 min |

---

## 九、异常处理

| 场景 | 处理方式 |
|------|---------|
| 条形码非 25 位 | 红色提示 + 不提交 |
| 条形码含非数字字符 | 红色提示 + 不提交 |
| 企业代码非 109 | 黄色警告 + 记录 error + 提示"非金川集团产品" |
| 品级代码未知 | 正常记录，grade 字段存原始代码值 |
| 重复扫码 | 黄色提示 + 记录 duplicate + 不重复插入 |
| 数据库异常 | 全局异常过滤器处理，返回错误信息 |

---

## 十、技术决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 扫码输入方式 | 键盘事件监听 | 扫码枪即 HID 键盘，零依赖 |
| 自动聚焦 | 页面加载 + 操作后自动聚焦 | 无需手动点击输入框 |
| 扫码提交 | 监听 Enter 键 | 扫码枪自动带 Enter 后缀 |
| 响应速度 | < 1 秒 | 解析纯算法，无外部调用 |
| 与库存关系 | 完全独立 | 扫码仅记录，出库由配货模块人工处理 |
| 去重机制 | barcode @unique | 天然幂等，支持后续多扫码枪扩展 |
| 记录存储 | 独立表 | 可追溯、可审计、可统计 |

---

_方案编制: AI 助手 · 2026-05-11_
_修订: 依据标码文档修正包号编码规则，明确扫码模块独立运行设计_
