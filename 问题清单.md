# Flatstore 项目问题清单

**审查日期**: 2026-05-11
**QA 验证日期**: 2026-05-11
**审查范围**: 后端 (NestJS) + Web 前端 (Vue 3) + Mobile App + 数据库 + 安全部署
**验证方法**: 逐条代码审查，确认/修正/驳回
**最后更新**: 2026-05-21 — 逐条验证修复状态

---

## 🔴 P0 — 立即处理（2 项）

### 1. JWT_SECRET 回退不安全

| 项目 | 详情 |
|------|------|
| **严重度** | 🔴 P0 |
| **位置** | `backend/src/auth/jwt.strategy.ts` |
| **现象** | `secretOrKey: process.env.JWT_SECRET \|\| 'flatstore-secret-key-change-in-production'` |
| **影响** | 环境变量未设置时，strategy 用硬编码弱密钥签发/验证 token，与 AuthModule 中 JwtModule 的行为不一致（JwtModule 未配置 secret 时会直接抛错） |
| **验证状态** | ✅ 已确认 — `jwt.strategy.ts` 有硬编码 fallback，`auth.module.ts` 会抛错，策略不一致 |
| **修复** | 移除 fallback，改为启动时检查环境变量不存在则拒绝启动：<br>`if (!process.env.JWT_SECRET) throw new Error('JWT_SECRET 未配置')` |
| **修复状态** | ✅ **已修复（v1.5.0）** — `secretOrKey: process.env.JWT_SECRET` 已移除 fallback。备注：启动时 `exp` 检查在 NestJS 框架层（JwtModule 无 secret 时会抛错），安全风险已消除 |

### 2. AI 识别净重单位未转换（数据缩小 1000 倍）

| 项目 | 详情 |
|------|------|
| **严重度** | 🔴 P0 |
| **位置** | `frontend/src/views/AI.vue` / `backend/src/common/services/qwen-ai.service.ts` |
| **现象** | AI Prompt 定义 netWeight 单位为**吨**，但未明确要求 AI 将原始单位转换为吨。前端导入时 `weight: r.netWeight` 无任何单位转换逻辑 |
| **影响** | 所有通过 AI 识别导入的库存重量数据可能缩小 1000 倍（如票面 1234kg → 存储 1.234 吨） |
| **验证状态** | ✅ 已确认 — 全链路（AI prompt → normalizeResult → handleImportSubmit）均无单位转换 |
| **修复** | 前端 AI.vue 批量导入时 `weight: (item.netWeight \|\| 0) * 1000`，或在后端 normalize 阶段 ×1000，或修改 AI prompt 为"将净重从原始单位转换为吨后返回" |
| **修复状态** | ✅ **已修复（v1.21.0~v1.22.0）** — ① AI Prompt 已改为明确要求 kg 输出 ② normalizeResult 有 `< 10kg → ×1000` 自动纠正逻辑 ③ 全链路统一使用 kg 单位 |

---

## 🟡 P1 — 优先处理（6 项）

### 3. statsCache 模块级全局变量竞态风险

| 项目 | 详情 |
|------|------|
| **严重度** | 🟡 P1 |
| **位置** | `backend/src/distribution/distribution.service.ts` 第 16-17 行 |
| **现象** | `let statsCache: { data: any; timestamp: number } \| null = null` 是模块级变量 |
| **影响** | 多实例/并发请求下可能竞态读取脏数据，且无手动失效机制（创建/修改/删除操作后仅靠 30 秒 TTL 过期） |
| **验证状态** | ✅ 已确认 — 模块级 `let` 变量，无主动失效 |
| **修复** | 改为 Service 类实例属性，或使用内存 TTL Map；增删改操作后主动清除缓存 |
| **修复状态** | ✅ **已修复（v1.20.0+）** — 已改为 `private statsCache` 类实例属性（第 19 行），不再是模块级变量。`clearCacheOnChange()` 机制已实现 |

### 4. CORS 过于宽松

| 项目 | 详情 |
|------|------|
| **严重度** | 🟡 P1 |
| **位置** | `backend/src/main.ts` 第 24 行 |
| **现象** | `callback(null, true)` 无条件放行所有来源 |
| **影响** | allowedOrigins 环境变量配置形同虚设，任何域名都可以跨域访问。配合 `credentials: true`，任意网站可发起带凭证的跨域请求 |
| **验证状态** | ✅ 已确认 — 第 23 行的 allowedOrigins 校验被第 24 行无条件放行覆盖 |
| **修复** | 生产环境严格校验：`if (allowedOrigins.includes(origin)) callback(null, true); else callback(new Error('CORS 拒绝'))` |
| **修复状态** | ✅ **已修复（v1.20.0+）** — 现在的逻辑：白名单校验（`FRONTEND_URL` + `capacitorOrigins`），不匹配则 `callback(new Error('CORS 拒绝'))`。`!origin` 对 Postman/curl 保留放行（设计上合理，非安全问题） |

### 5. 配货单状态枚举无 DB 约束

| 项目 | 详情 |
|------|------|
| **严重度** | 🟡 P1 |
| **位置** | `backend/prisma/schema.prisma` DistributionOrder.status |
| **现象** | status 字段为自由字符串 `String @default("draft") @db.VarChar(20)` |
| **影响** | 任何字符串值都可写入，无法保证状态机的完整性 |
| **验证状态** | ✅ 已确认 — 纯 String 字段，无 Enum 约束 |
| **修复** | 改用 Prisma Enum 或在 MySQL 层添加 CHECK 约束 |
| **修复状态** | ✅ **已修复（v1.20.0+）** — `status OrderStatus @default(draft)` 已用 Prisma Enum（`OrderStatus`） |

### 6. InventoryStock.status 无枚举约束

| 项目 | 详情 |
|------|------|
| **严重度** | 🟡 P1 |
| **位置** | `backend/prisma/schema.prisma` InventoryStock.status |
| **现象** | 同 #5，`available/reserved/shipped` 为字符串约定 |
| **验证状态** | ✅ 已确认 |
| **修复** | 改用 Prisma Enum |
| **修复状态** | ✅ **已修复** — `status StockStatus @default(available)` 已用 Prisma Enum（`StockStatus`） |

### 7. 前端 weight 类型不一致

| 项目 | 详情 |
|------|------|
| **严重度** | 🟡 P1 |
| **位置** | `frontend/src/types/index.ts` |
| **现象** | `InventoryStock.weight: string \| number`，但 `CreateInventoryDto.weight: number` |
| **影响** | Prisma Decimal 序列化可能返回 string，前端类型声明为 string\|number，但 DTO 要求 number，前后端交互时可能类型不匹配。配合 `forbidNonWhitelisted: true` 可能导致请求被拒 |
| **修复** | 统一为 string（推荐，兼容 Decimal 序列化），或在响应后做 Number 转换 |
| **修复状态** | ✅ **已修复** — 前端 `InventoryStock.weight: string | number` 已有注释说明 "Prisma Decimal 序列化后为 string，前端类型已兼容"。`CreateInventoryDto.weight: number` 在提交时自行转换 |

### 8. 软删除字段无索引

| 项目 | 详情 |
|------|------|
| **严重度** | 🟡 P1 |
| **位置** | `backend/prisma/schema.prisma` |
| **现象** | `deletedAt` 在所有高频查询中作为 WHERE 条件，但无索引 |
| **影响** | 数据量增长后查询性能下降 |
| **修复** | 添加复合索引：<br>`@@index([customerId, deletedAt, status])` 已存在，但 `distribution_customer` 的 `deletedAt` 无索引 |
| **修复状态** | ✅ **已修复** — `@@index([deletedAt])` 已添加，`@@index([customerId, deletedAt, status])` 也已存在 |

---

## 🟠 P2 — 改进建议（10 项）

### 9. Store 中 `as any` 类型断言泛滥

| 项目 | 详情 |
|------|------|
| **严重度** | 🟠 P2 |
| **位置** | `frontend/src/stores/inventory.ts`、`stores/order.ts` |
| **现象** | `await api.getInventoryList(params) as any` 放弃类型安全 |
| **验证状态** | ✅ 已确认 — inventory.ts:15、order.ts:16, 27 均有 `as any` |
| **修复** | 正确定义 Axios 实例泛型，移除 as any |
| **修复状态** | ✅ **已修复** — inventory.ts 和 order.ts 中 `as any` 已全部移除（0 处） |

### 10. Store fetch 无 error 反馈

| 项目 | 详情 |
|------|------|
| **严重度** | 🟠 P2 |
| **位置** | `frontend/src/stores/inventory.ts` `stores/order.ts` |
| **现象** | try 块中失败时不更新状态，finally 清空 loading，用户无错误提示 |
| **验证状态** | ✅ 已确认 — 无 catch 块，无 ElMessage.error，mutation 方法同样无 try/catch |
| **修复** | catch 块中添加 ElMessage.error 或状态标记 |
| **修复状态** | ⚠️ **部分修复** — catch 块已添加（处理 CanceledError 静默忽略），保留现有数据不清空。但仍无 ElMessage.error/用户可见错误提示。建议补充 |

### 11. 路由守卫不验证 Token 有效期

| 项目 | 详情 |
|------|------|
| **严重度** | 🟠 P2 |
| **位置** | `frontend/src/router/index.ts` |
| **现象** | 仅检查 localStorage 是否有 token，不验证是否过期 |
| **影响** | JWT 过期后前端仍可进入页面，直到请求 401 才跳转 |
| **验证状态** | ✅ 已确认 — 仅 `if (requiresAuth && !token)` 判断，无 JWT 解码 |
| **修复** | 路由守卫中解析 JWT 检查 exp 字段，或使用响应拦截器的 401 处理（已部分实现） |
| **修复状态** | ✅ **已修复** — 路由守卫 `beforeEach` 中已实现 `atob(token.split('.')[1])` 解码 + `payload.exp` 检查，过期时自动清除 token 并跳转至登录页 |

### 12. batchDeleteInventory 逻辑不精确

| 项目 | 详情 |
|------|------|
| **严重度** | 🟠 P2 |
| **位置** | `backend/src/distribution/distribution.service.ts` |
| **现象** | `unreferenced.length < ids.length` 判断，但传入的 id 不存在也会被跳过不报错 |
| **修复** | 精确校验每个 id 是否存在且可删除，返回具体哪些 id 不可删除 |
| **修复状态** | ✅ **已修复** — 现逻辑：逐个检查，返回 `notFound` + `referenced` 两个数组，精确告知用户哪些 id 不存在、哪些被引用 |

### 13. QwenAIService 仅重试 1 次

| 项目 | 详情 |
|------|------|
| **严重度** | 🟠 P2 |
| **位置** | `backend/src/common/services/qwen-ai.service.ts` |
| **现象** | `MAX_RETRIES = 1`，总尝试 2 次（1 初始 + 1 重试） |
| **验证状态** | ✅ 已确认 — 重试延迟：429 等 3 秒，5xx 等 1 秒，无指数退避 |
| **修复** | 改为 2-3 次，指数退避 |
| **修复状态** | ⚠️ **部分修复** — `MAX_RETRIES` 已从 1 改为 2（总尝试 3 次），重试逻辑已实现（429 单独处理、5xx 重试）。但重试延迟仍为固定值（非指数退避） |

### 14. 导出接口完全缺失

| 项目 | 详情 |
|------|------|
| **严重度** | 🟠 P2 |
| **位置** | `distribution.controller.ts` |
| **现象** | `export-inventory.dto.ts` 存在但无对应路由，半完成的功能 |
| **验证状态** | ✅ 已确认 — DTO 文件存在但 controller 无路由 |
| **修复** | 实现导出路由或移除 DTO 文件 |
| **修复状态** | ❌ **未修复** — controller 无 export 路由，`export-inventory.dto.ts` 已从仓库中移除（或从未提交）。需要时需重新实现 |

### 15. aiRecognize 中 console.log 调试代码

| 项目 | 详情 |
|------|------|
| **严重度** | 🟠 P2 |
| **位置** | `backend/src/distribution/distribution.service.ts` 第 275、279 行 |
| **现象** | `console.log('[AI] 后端收到文件:', file)` 和文件元信息日志，生产环境泄露文件路径 |
| **验证状态** | ✅ 已确认 — 两处 console.log，另有 console.error 用于清理失败（可保留） |
| **修复** | 移除或替换为正式 Logger（NestJS `LoggerService`） |
| **修复状态** | ✅ **已修复** — 已替换为 `this.logger.log()` / `this.logger.error()` |

### 16. deleteOrder 中存在死代码

| 项目 | 详情 |
|------|------|
| **严重度** | 🟠 P2 |
| **位置** | `backend/src/distribution/distribution.service.ts` 第 597 行 |
| **现象** | `const stockIds = order.items.map(i => i.stockId)` 计算了但从未使用 |
| **影响** | 代码可读性差，可能误导后续开发者认为此处需要处理库存 |
| **修复** | 移除未使用的变量 |
| **修复状态** | ✅ **已修复** — `stockIds` 在 updateOrder 和 deleteOrder 逻辑中已有实际用途（校验库存引用），不再为死代码 |

### 17. 仅 Android 平台支持

| 项目 | 详情 |
|------|------|
| **严重度** | 🟠 P2 |
| **位置** | `mobile/` |
| **现象** | iOS 平台未添加 |
| **修复** | `npx cap add ios` 并适配 iOS 特定配置 |
| **修复状态** | ❌ **未修复** — 仍为 Android 独占，iOS 平台未添加。如需 iOS 支持，需要 `npx cap add ios` 并适配 |

### 18. 无数据库迁移版本管理

| 项目 | 详情 |
|------|------|
| **严重度** | 🟠 P2 |
| **位置** | 项目配置 |
| **现象** | 使用 `prisma db push` 而非 `prisma migrate dev` |
| **影响** | 无迁移历史，团队协作/环境同步时易冲突 |
| **验证状态** | ✅ 已确认 — 无 `prisma/migrations/` 目录 |
| **修复** | 切换为 `prisma migrate` 工作流 |
| **修复状态** | ❌ **未修复** — 仍无 `migrations/` 目录，仍在使用 `db push`。建议切换但需要协调现有数据 |

---

## 🟢 P3 — 低优先级（4 项）

| # | 问题 | 位置 | 说明 | 修复状态 |
|---|------|------|------|---------|
| 19 | 无 Helmet 安全头 | `backend/src/main.ts` | 无 CSP、X-Frame-Options、HSTS 等安全响应头 | ❌ **未修复** |
| 20 | Mobile App 无离线支持 | `mobile/` | 无缓存机制，断网完全不可用 | ❌ **未修复** |
| 21 | statistics store 无 retry 机制 | `stores/statistics.ts` | 网络失败后无自动重试 | ❌ **未修复** |
| 22 | 无 API 版本控制 | 全局 | 接口无 `/api/v1/` 前缀，未来升级时可能有兼容问题 | ❌ **未修复** |

---

## ❌ 已驳回（3 项）

以下问题经代码验证后确认**不成立**，从原清单中移除：

| 原# | 原始描述 | 驳回原因 |
|-----|---------|---------|
| 原#2 | deleteOrder 未释放已取消订单的库存 | `deleteOrder` 仅允许删除 `shipped`/`cancelled` 状态的订单。`cancelOrder` 在取消时已释放库存，`shipped` 订单货物物理出库不应恢复。代码注释明确说明："已发货订单的库存不应重置为 available（货物已物理出库）"。属于设计如此，非 bug |
| 原#6 | Dashboard 图表初始化未使用 nextTick | `onMounted` 中已正确 `await fetchStatistics()` 后再调用 `initCharts()`，DOM 在 `onMounted` 时已就绪，不存在竞态 |
| 原#11 | ValidationPipe forbidNonWhitelisted 过于严格 | `forbidNonWhitelisted: true` 是正确的安全实践，防止前端发送未定义字段。不是配置问题，而是前后端 DTO 应保持对齐（见 #7 weight 类型问题） |

---

## 📊 问题统计

| 优先级 | 数量 | 占比 | 已修复 | 部分修复 | 未修复 |
|--------|------|------|--------|---------|--------|
| 🔴 P0 | 2 | 9% | 2 | 0 | 0 |
| 🟡 P1 | 6 | 27% | 6 | 0 | 0 |
| 🟠 P2 | 10 | 45% | 5 | 2 | 3 |
| 🟢 P3 | 4 | 18% | 0 | 0 | 4 |
| ❌ 驳回 | 3 | — | — | — | — |
| **合计（有效）** | **22** | 100% | **13** | **2** | **7** |

**修复率：** 13/22 = **59%** 完全修复 + 2/22 = 9% 部分修复 = **68% 进展**

---

_QA 验证由 Claude Code 完成 · 2026-05-11_
_修复状态更新由 @openclaw 完成 · 2026-05-21_
