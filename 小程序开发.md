# 镍板标码AI识别小程序开发方案

## 项目概述

基于现有平面库配货系统的AI识别能力，开发一款微信小程序，专门用于识别金川集团镍板标签标码信息。小程序通过拍照或上传图片，调用阿里云千问大模型进行OCR识别，然后根据《规则.md》中定义的标码规则进行自动比对、纠错和校验，最终输出结构化的识别结果和纠错建议。

### 核心功能
1. **图像采集**：支持拍照、从相册选择图片
2. **AI识别**：调用千问VL Plus模型识别标签字段
3. **规则比对**：根据规则文件校验批号、包号、日期等字段格式
4. **纠错建议**：自动纠正常见OCR错误（0/O、1/l/I等）
5. **条形码解析**：解析标签底部条形码，与文本字段交叉验证
6. **历史记录**：保存识别记录和纠错结果
7. **结果导出**：支持JSON、Excel格式导出

### 目标用户
- 仓库管理人员
- 质量检验人员
- 生产车间操作工

---

## 技术栈

### 前端（微信小程序）
| 组件 | 技术 | 说明 |
|------|------|------|
| 框架 | 微信小程序原生框架 | 使用WXML、WXSS、JavaScript |
| UI组件 | Vant Weapp / WeUI | 组件化开发 |
| 网络请求 | wx.request | 封装为统一API模块 |
| 文件上传 | wx.chooseImage + wx.uploadFile | 支持多图上传 |
| 图片处理 | canvas | 图片裁剪、压缩 |
| 数据存储 | wx.setStorageSync | 本地缓存识别历史 |

### 后端（基于现有NestJS系统扩展）
| 组件 | 技术 | 说明 |
|------|------|------|
| 框架 | NestJS 10 | 现有后端扩展 |
| AI服务 | 阿里云千问 qwen-vl-plus | 基于现有QwenAIService扩展 |
| 数据库 | MySQL 8.0 + Prisma 6.19.3 | 现有数据库扩展 |
| 文件存储 | 本地文件系统 | uploads/nickel-label/目录 |
| API文档 | Swagger/OpenAPI | 自动生成接口文档 |
| 身份认证 | JWT | 微信小程序登录集成 |

### 开发工具
- 微信开发者工具
- VSCode + 微信小程序插件
- Docker（本地开发环境）
- Postman（API测试）

---

## 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                   微信小程序前端                             │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ 拍照/选图│  │ AI识别  │  │ 规则比对│  │ 结果展示│        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
│       │             │             │             │           │
│       └─────────────┼─────────────┼─────────────┘           │
│                     ▼             ▼                         │
│              ┌───────────────────────────┐                  │
│              │     微信小程序API封装     │                  │
│              └───────────────────────────┘                  │
│                             │                                │
└─────────────────────────────┼────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   后端服务（NestJS）                         │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                API网关层                            │    │
│  │  • 身份验证（JWT + 微信登录）                       │    │
│  │  • 请求限流（防刷）                                │    │
│  │  • 日志记录                                        │    │
│  └─────────────────────────────────────────────────────┘    │
│                              │                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ 镍板识别服务 │  │ 规则比对服务 │  │ 历史记录服务 │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│         │               │               │                   │
│         └───────┬───────┴───────┬───────┘                   │
│                 ▼               ▼                           │
│        ┌─────────────────┐ ┌────────────┐                  │
│        │ 千问AI服务扩展  │ │ 数据库层    │                  │
│        │ • 标码专用prompt│ │ • 识别历史  │                  │
│        │ • 图像预处理    │ │ • 纠错记录  │                  │
│        └─────────────────┘ └────────────┘                  │
│                 │                                           │
│                 ▼                                           │
│        ┌─────────────────┐                                 │
│        │ 阿里云千问API   │                                 │
│        │ qwen-vl-plus    │                                 │
│        └─────────────────┘                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 前端小程序开发

### 页面结构
```
pages/
├── index/                    # 首页
│   ├── index.wxml           # 首页模板
│   ├── index.wxss           # 首页样式
│   ├── index.js             # 首页逻辑
│   └── index.json           # 页面配置
├── camera/                   # 拍照页面
│   ├── camera.wxml          # 相机组件
│   ├── camera.wxss          # 相机样式
│   ├── camera.js            # 相机逻辑
│   └── camera.json          # 页面配置
├── recognize/               # 识别结果页面
│   ├── recognize.wxml       # 结果展示模板
│   ├── recognize.wxss       # 结果展示样式
│   ├── recognize.js         # 结果展示逻辑
│   └── recognize.json       # 页面配置
├── history/                 # 历史记录页面
│   ├── history.wxml         # 历史记录模板
│   ├── history.wxss         # 历史记录样式
│   ├── history.js           # 历史记录逻辑
│   └── history.json         # 页面配置
└── profile/                 # 个人中心
    ├── profile.wxml         # 个人中心模板
    ├── profile.wxss         # 个人中心样式
    ├── profile.js           # 个人中心逻辑
    └── profile.json         # 页面配置
```

### 组件设计
1. **ImageUploader组件**：支持拍照、相册选择、图片预览
2. **ResultCard组件**：识别结果卡片展示
3. **RuleCheck组件**：规则比对结果展示
4. **BarcodeScanner组件**：条形码扫描（备用方案）

### API封装
```javascript
// utils/api.js
const BASE_URL = 'https://your-domain.com/api';

const request = (url, method, data) => {
  return new Promise((resolve, reject) => {
    wx.request({
      url: BASE_URL + url,
      method,
      data,
      header: {
        'Authorization': `Bearer ${wx.getStorageSync('token')}`,
        'Content-Type': 'application/json'
      },
      success: (res) => {
        if (res.statusCode === 200) {
          resolve(res.data);
        } else {
          reject(res.data);
        }
      },
      fail: reject
    });
  });
};

// 镍板识别API
export const nickelApi = {
  // 上传图片识别
  recognizeImage: (filePath) => {
    return new Promise((resolve, reject) => {
      wx.uploadFile({
        url: BASE_URL + '/nickel/recognize',
        filePath,
        name: 'file',
        formData: {},
        header: {
          'Authorization': `Bearer ${wx.getStorageSync('token')}`
        },
        success: (res) => {
          if (res.statusCode === 200) {
            resolve(JSON.parse(res.data));
          } else {
            reject(JSON.parse(res.data));
          }
        },
        fail: reject
      });
    });
  },
  
  // 获取识别历史
  getHistory: (params) => request('/nickel/history', 'GET', params),
  
  // 导出结果
  exportResult: (id) => request(`/nickel/export/${id}`, 'GET'),
};
```

### 核心业务流程
1. **用户登录**：微信授权登录，获取JWT token
2. **图片采集**：用户拍照或选择相册图片
3. **图片预处理**：压缩、裁剪、旋转矫正
4. **上传识别**：调用后端AI识别接口
5. **结果展示**：显示识别结果和规则比对结果
6. **纠错确认**：用户确认或修改纠错建议
7. **保存记录**：保存到本地和服务器

---

## 后端服务扩展

### 新增模块结构
```
backend/src/
├── nickel/                    # 镍板识别模块
│   ├── nickel.controller.ts   # 控制器
│   ├── nickel.service.ts      # 服务层
│   ├── nickel.module.ts       # 模块定义
│   ├── dto/                   # 数据传输对象
│   │   ├── recognize.dto.ts   # 识别请求DTO
│   │   └── correction.dto.ts  # 纠错请求DTO
│   └── entities/              # 实体定义
│       └── nickel-label.entity.ts
├── common/services/
│   ├── nickel-ai.service.ts   # 镍板专用AI服务
│   └── rule-checker.service.ts # 规则比对服务
└── prisma/
    └── schema.prisma          # 扩展数据库模型
```

### 数据库模型扩展
```prisma
// 镍板识别历史记录
model NickelRecognitionHistory {
  id           Int       @id @default(autoincrement())
  userId       Int       // 关联用户
  imageUrl     String?   // 图片存储路径
  originalResult Json?   // 原始识别结果
  correctedResult Json?  // 纠错后结果
  barcodeData  String?   // 条形码数据
  checkResults Json      // 规则检查结果
  status       String    @default("pending") // pending/success/failed/corrected
  errorMessage String?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  
  @@index([userId])
  @@index([createdAt])
}

// 规则检查明细
model NickelRuleCheckDetail {
  id          Int       @id @default(autoincrement())
  historyId   Int       // 关联识别历史
  fieldName   String    // 字段名：batchNo/packNo/date等
  originalValue String? // 原始值
  correctedValue String? // 纠正后值
  ruleType    String    // 规则类型：format/range/consistency
  passed      Boolean   // 是否通过
  message     String?   // 检查消息
  createdAt   DateTime  @default(now())
  
  @@index([historyId])
}
```

### 核心接口设计
1. **POST /api/nickel/recognize** - 上传图片识别镍板标签
2. **POST /api/nickel/correct** - 提交纠错确认
3. **GET /api/nickel/history** - 获取识别历史
4. **GET /api/nickel/history/:id** - 获取单条记录详情
5. **GET /api/nickel/export/:id** - 导出识别结果
6. **DELETE /api/nickel/history/:id** - 删除记录

---

## AI识别与规则比对算法

### 1. 镍板专用AI识别服务
基于现有`QwenAIService`扩展，针对镍板标签优化prompt：

```typescript
// nickel-ai.service.ts
export class NickelAIService {
  async recognizeNickelLabel(base64: string): Promise<NickelLabelData> {
    const prompt = `你是一个专业的镍板标签识别专家，专门识别金川集团镍板标签。

## 标签结构
标签包含以下固定字段，请按字段提取：

### 固定字段（按英文名称提取）：
1. PRODUCT (品名): 电解镍 / 电积镍
2. BRAND (品牌): JINTUO GRADE 1 或 JINTUO GRADE 1 (EW)
3. STANDARD (标准): GB/T6516-2025
4. BATCH NO (批号): 格式为 YY-N-NNNJ，如 26-1-109J
5. PACK NO (包号): 数字或数字+字母后缀(J/t/s)，如 47、47J、47t、47s
6. PIECES (块数): 整数，如 25、31
7. NET (净重): 数值（千克），如 1482.0
8. GROSS (毛重): 数值（千克），如 1488.0
9. DATE (生产日期): YYYY-MM-DD 格式
10. WEIGHT BY (计量员): 1-2位数字

## 识别要求
1. 精确匹配字段前的英文标识（如 "BATCH NO:" 后的内容）
2. 注意字符混淆：0/O、1/l/I、5/S 等
3. 批号必须保持原始格式，不要修改连字符
4. 包号可能包含字母后缀，不要遗漏
5. 重量单位是千克，保留一位小数
6. 日期统一转为 YYYY-MM-DD 格式

## 输出格式
返回JSON对象，字段名使用小驼峰：
{
  "product": { "nameCn": "电解镍", "nameEn": "Electrolytic nickel" },
  "brand": "JINTUO GRADE 1",
  "standard": "GB/T6516-2025",
  "batchNo": "26-1-109J",
  "packNo": "47",
  "pieces": 25,
  "netWeight": 1482.0,
  "grossWeight": 1488.0,
  "productionDate": "2026-04-19",
  "weightBy": 5,
  "company": "金川集团镍钴股份有限公司",
  "address": "甘肃省金昌市金川区北京路31号"
}`;

    // 调用千问API（同现有QwenAIService）
    const result = await this.qwenAI.recognizeWithPrompt(base64, prompt);
    return this.normalizeNickelResult(result);
  }
}
```

### 2. 规则比对服务
```typescript
// rule-checker.service.ts
export class RuleCheckerService {
  /**
   * 执行完整规则检查
   */
  async checkNickelLabel(data: NickelLabelData, barcode?: string): Promise<CheckResults> {
    const results: CheckResult[] = [];
    
    // 1. 批号检查
    results.push(this.checkBatchNo(data.batchNo, barcode));
    
    // 2. 包号检查
    results.push(this.checkPackNo(data.packNo, barcode));
    
    // 3. 日期格式检查
    results.push(this.checkDate(data.productionDate));
    
    // 4. 重量检查
    results.push(this.checkWeight(data.netWeight, data.grossWeight));
    
    // 5. 块数检查
    results.push(this.checkPieces(data.pieces));
    
    // 6. 条形码一致性检查（如果有条形码）
    if (barcode) {
      results.push(...this.checkBarcodeConsistency(data, barcode));
    }
    
    // 7. 字符混淆自动纠正
    const corrected = this.autoCorrectCommonErrors(data);
    
    return {
      original: data,
      corrected,
      checkResults: results,
      allPassed: results.every(r => r.passed),
      score: this.calculateScore(results)
    };
  }
  
  /**
   * 批号校验规则
   */
  private checkBatchNo(batchNo: string, barcode?: string): CheckResult {
    const rules = [
      {
        name: '格式校验',
        check: () => /^\d{2}-\d{1}-\d{3}J$/.test(batchNo),
        message: '批号格式应为 YY-N-NNNJ'
      },
      {
        name: '车间代码范围',
        check: () => {
          const match = batchNo.match(/^\d{2}-(\d{1})-\d{3}J$/);
          if (!match) return false;
          const workshopCode = parseInt(match[1]);
          return workshopCode >= 1 && workshopCode <= 7;
        },
        message: '车间代码必须在1-7范围内'
      },
      // 更多规则...
    ];
    
    return this.applyRules('batchNo', batchNo, rules);
  }
  
  /**
   * 自动纠正常见OCR错误
   */
  private autoCorrectCommonErrors(data: NickelLabelData): NickelLabelData {
    const corrected = { ...data };
    
    // 0/O 纠正
    corrected.batchNo = corrected.batchNo.replace(/O/g, '0');
    corrected.packNo = corrected.packNo.replace(/O/g, '0');
    
    // 1/l/I 纠正
    corrected.batchNo = corrected.batchNo.replace(/[lI]/g, '1');
    corrected.packNo = corrected.packNo.replace(/[lI]/g, '1');
    
    // 5/S 纠正
    corrected.pieces = corrected.pieces.toString().replace(/S/g, '5');
    
    return corrected;
  }
  
  /**
   * 条形码解析与一致性检查
   */
  private checkBarcodeConsistency(data: NickelLabelData, barcode: string): CheckResult[] {
    const results: CheckResult[] = [];
    const barcodeParts = this.parseBarcode(barcode);
    
    if (!barcodeParts) {
      return [{
        field: 'barcode',
        ruleType: 'format',
        passed: false,
        message: '条形码格式无效'
      }];
    }
    
    // 检查批号与条形码一致性
    const batchNoMatch = this.checkBatchNoBarcodeConsistency(data.batchNo, barcodeParts);
    results.push(batchNoMatch);
    
    // 检查包号与条形码一致性
    const packNoMatch = this.checkPackNoBarcodeConsistency(data.packNo, barcodeParts);
    results.push(packNoMatch);
    
    // 检查日期一致性
    const dateMatch = this.checkDateBarcodeConsistency(data.productionDate, barcodeParts);
    results.push(dateMatch);
    
    // 检查净重一致性
    const weightMatch = this.checkWeightBarcodeConsistency(data.netWeight, barcodeParts);
    results.push(weightMatch);
    
    return results;
  }
  
  /**
   * 解析条形码
   * 格式: XXX XX XX YYYYMMDD NNNNNNN NNNNN
   */
  private parseBarcode(barcode: string): BarcodeParts | null {
    const parts = barcode.trim().split(/\s+/);
    if (parts.length !== 6) return null;
    
    return {
      prefix: parts[0],          // 固定前缀 109
      month: parts[1],           // 月份
      day: parts[2],             // 日期
      productionDate: parts[3],  // 生产日期 YYMMDD
      productCode: parts[4],     // 7位产品代码
      netWeightEncoded: parts[5] // 净重×10
    };
  }
}
```

### 3. 条形码编码转换规则
```typescript
/**
 * 包号编码转换（规则5.1.2）
 */
private decodePackNo(codeStr: string): string {
  const code = parseInt(codeStr);
  
  if (code >= 0 && code <= 200) {
    // 特定产品包装，无后缀
    return code.toString();
  } else if (code >= 201 && code <= 400) {
    // 机组包装，J后缀
    return `${code - 200}J`;
  } else if (code >= 401 && code <= 600) {
    // 镍条包装，t后缀
    return `${code - 400}t`;
  } else if (code >= 601 && code <= 800) {
    // 手工班包装，s后缀
    return `${code - 600}s`;
  } else {
    throw new Error(`包号编码超出范围: ${code}`);
  }
}

/**
 * 车间代码映射（规则5.1.1）
 */
private getWorkshopName(code: number): string {
  const mapping = {
    1: '电解一车间电解镍',
    2: '电解二车间电解镍',
    3: '电解三车间电解镍',
    4: '电积一车间（128槽）电解镍',
    5: '电解三车间电积镍',
    6: '电积一车间电积镍',
    7: '电积二车间电积镍'
  };
  return mapping[code] || `未知车间(${code})`;
}
```

---

## 数据库设计扩展

### 1. 镍板识别历史表 (nickel_recognition_history)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| user_id | INT | 用户ID |
| image_url | VARCHAR(500) | 图片存储路径 |
| original_result | JSON | 原始识别结果 |
| corrected_result | JSON | 纠错后结果 |
| barcode_data | VARCHAR(100) | 条形码原始数据 |
| check_results | JSON | 规则检查结果 |
| status | ENUM | 状态：pending/success/failed/corrected |
| error_message | TEXT | 错误信息 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 2. 规则检查明细表 (nickel_rule_check_detail)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| history_id | INT | 识别历史ID |
| field_name | VARCHAR(50) | 字段名 |
| original_value | VARCHAR(100) | 原始值 |
| corrected_value | VARCHAR(100) | 纠正后值 |
| rule_type | VARCHAR(50) | 规则类型 |
| passed | BOOLEAN | 是否通过 |
| message | VARCHAR(500) | 检查消息 |
| created_at | DATETIME | 创建时间 |

### 3. 用户表扩展 (users)
添加微信相关字段：
- `wx_openid` VARCHAR(100) - 微信开放平台ID
- `wx_unionid` VARCHAR(100) - 微信开放平台UnionID
- `wx_nickname` VARCHAR(100) - 微信昵称
- `wx_avatar` VARCHAR(500) - 微信头像

---

## 部署方案

### 1. 开发环境
```yaml
# docker-compose.dev.yml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root123456
      MYSQL_DATABASE: nickel_label
    ports:
      - "3307:3306"
    volumes:
      - ./data/mysql:/var/lib/mysql
      
  backend:
    build: ./backend
    ports:
      - "3003:3002"
    environment:
      DATABASE_URL: "mysql://root:root123456@mysql:3306/nickel_label?charset=utf8mb4"
      QWEN_API_KEY: "sk-xxx"
      WX_APP_ID: "your-app-id"
      WX_APP_SECRET: "your-app-secret"
    depends_on:
      - mysql
```

### 2. 生产环境
- **前端小程序**：微信小程序平台发布
- **后端服务**：阿里云ECS/腾讯云CVM + Docker部署
- **数据库**：阿里云RDS MySQL 8.0
- **文件存储**：阿里云OSS（图片存储）
- **CDN**：阿里云CDN（静态资源加速）

### 3. 微信小程序配置
```json
// project.config.json
{
  "appid": "your-wechat-appid",
  "projectname": "镍板标码识别",
  "setting": {
    "urlCheck": false,
    "es6": true,
    "postcss": true,
    "minified": true
  },
  "libVersion": "3.0.0",
  "packOptions": {
    "ignore": []
  }
}
```

---

## 开发计划

### 第一阶段：基础框架搭建（2周）
1. 微信小程序项目初始化
2. 基础页面结构搭建
3. 后端镍板识别模块创建
4. 数据库模型设计

### 第二阶段：核心功能开发（3周）
1. 图片上传与AI识别接口
2. 规则比对算法实现
3. 条形码解析功能
4. 前端识别结果展示

### 第三阶段：用户体验优化（2周）
1. 微信登录集成
2. 历史记录功能
3. 结果导出功能
4. 错误处理与提示优化

### 第四阶段：测试与部署（1周）
1. 单元测试与集成测试
2. 性能测试与优化
3. 生产环境部署
4. 小程序提交审核

---

## 关键技术点

### 1. 图片预处理优化
- **尺寸压缩**：限制最大边长2048px
- **格式转换**：统一转为JPEG格式
- **方向校正**：基于EXIF信息自动旋转
- **质量优化**：压缩比80%，平衡质量与大小

### 2. 识别准确率提升策略
- **多模型融合**：千问VL Plus + 专用OCR模型
- **多次识别取优**：同一图片识别3次，取最一致结果
- **上下文校验**：利用字段间逻辑关系纠正错误
- **人工复核机制**：置信度低时提示人工确认

### 3. 性能优化方案
- **前端缓存**：识别结果本地缓存
- **CDN加速**：静态资源分发
- **数据库索引**：关键字段建立索引
- **异步处理**：大文件识别异步队列处理

### 4. 安全考虑
- **图片脱敏**：识别后自动删除原始图片
- **访问控制**：JWT token + 微信登录验证
- **API限流**：防止恶意调用
- **数据加密**：敏感信息加密存储

---

## 预期效果

### 识别准确率目标
- 批号识别准确率：≥99%
- 包号识别准确率：≥98%
- 重量识别准确率：≥99.5%
- 整体字段完整率：≥95%

### 性能指标
- 单张图片识别时间：≤3秒
- 规则比对时间：≤0.5秒
- 小程序冷启动时间：≤2秒
- API响应时间P95：≤500ms

### 业务价值
1. **效率提升**：人工核对时间减少90%
2. **准确性提升**：识别错误率降低95%
3. **数据标准化**：统一数据格式，便于系统对接
4. **追溯能力**：完整记录识别历史，支持质量追溯

---

## 后续扩展方向

### 1. 功能扩展
- 批量识别：一次上传多张图片
- 离线识别：边缘计算模型部署
- 语音播报：识别结果语音输出
- AR展示：通过AR展示识别结果

### 2. 技术升级
- 自研OCR模型：针对镍板标签定制训练
- 多模态融合：图像+条形码+RFID多源验证
- 区块链存证：识别结果上链存证
- AI质检：基于识别结果的自动质量判定

### 3. 生态整合
- 与ERP系统对接：识别结果自动同步
- 与仓储系统对接：自动更新库存信息
- 与生产系统对接：质量数据反馈生产端
- 与供应链系统对接：追溯信息共享

---

## 风险与应对

### 技术风险
1. **AI识别准确率不足**
   - 应对：准备多套prompt方案，人工复核机制
   
2. **微信小程序性能瓶颈**
   - 应对：图片分片上传，渐进式加载
   
3. **高并发访问压力**
   - 应对：负载均衡，异步队列，CDN加速

### 业务风险
1. **标签样式变更**
   - 应对：规则配置化，支持动态更新
   
2. **用户接受度低**
   - 应对：简化操作流程，提供培训支持
   
3. **数据安全问题**
   - 应对：数据加密，访问审计，定期备份

---

## 附录

### 1. 参考资源
- [微信小程序开发文档](https://developers.weixin.qq.com/miniprogram/dev/framework/)
- [阿里云千问API文档](https://help.aliyun.com/zh/dashscope/developer-reference)
- [NestJS官方文档](https://docs.nestjs.com/)
- [Prisma文档](https://www.prisma.io/docs)

### 2. 开发团队角色
- **产品经理**：需求分析、原型设计
- **前端开发**：微信小程序开发
- **后端开发**：NestJS服务开发
- **AI算法**：识别算法优化
- **测试工程师**：功能测试、性能测试
- **运维工程师**：部署维护、监控告警

### 3. 成功案例参考
- 现有平面库配货系统AI识别模块
- 某钢铁企业钢板标码识别系统
- 某物流企业快递面单识别系统

---

*最后更新：2026-04-22*  
*版本：1.0.0*  
*作者：基于现有平面库配货系统AI识别模块扩展*